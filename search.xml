<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>ECMAScript6--Module</title>
    <url>/2019/12/10/ECMAScript6/19%E3%80%81Module/</url>
    <content><![CDATA[<p>模块功能主要由两个命令构成：<code>export</code>和<code>import</code>。<code>export</code>命令用于规定模块的对外接口，<code>import</code>命令用于输入其他模块提供的功能。</p>
<a id="more"></a>

<h1 id="export命令"><a href="#export命令" class="headerlink" title="export命令"></a>export命令</h1><p>一个模块就是一个独立的文件。该文件内部的所有变量，外部无法获取。如果你希望外部能够读取模块内部的某个变量，就必须使用<code>export</code>关键字输出该变量。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// profile.js</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">export</span> <span class="keyword">var</span> firstName = <span class="string">'Michael'</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">export</span> <span class="keyword">var</span> lastName = <span class="string">'Jackson'</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">export</span> <span class="keyword">var</span> year = <span class="number">1958</span>;</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>用<code>export</code>命令对外部输出了三个变量。</p>
</blockquote>
<ul>
<li><p><code>export</code>的另一种写法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// profile.js</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> firstName = <span class="string">'Michael'</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> lastName = <span class="string">'Jackson'</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> year = <span class="number">1958</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">export</span> &#123; firstName, lastName, year &#125;;</span></code><br></pre></td></tr></table></figure>





</li>
</ul>
<ul>
<li><p><code>export</code>命令除了输出变量，还可以输出函数和类（class）：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">export</span> <span class="function"><span class="keyword">function</span> <span class="title">multiply</span>(<span class="params">x, y</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> x * y;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>对外输出一个函数<code>multiply</code></p>
</blockquote>
</li>
<li><p><code>export</code>输出的变量就是本来的名字，但是可以使用<code>as</code>关键字重命名。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">v1</span>(<span class="params"></span>) </span>&#123; ... &#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">v2</span>(<span class="params"></span>) </span>&#123; ... &#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">export</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">  v1 <span class="keyword">as</span> streamV1,</span></code><br><code class="highlight javascript"><span class="line">  v2 <span class="keyword">as</span> streamV2,</span></code><br><code class="highlight javascript"><span class="line">  v2 <span class="keyword">as</span> streamLatestVersion</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>



</li>
</ul>
<ul>
<li><p><code>export</code>命令规定的是对外的接口，必须与模块内部的变量建立一一对应关系。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 报错</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">export</span> <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 报错</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> m = <span class="number">1</span>; </span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">export</span> m;   <span class="comment">//通过变量`m`，还是直接输出 1。`1`只是一个值，不是接口。</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//正确写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 写法一</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">export</span> <span class="keyword">var</span> m = <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 写法二</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> m = <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">export</span> &#123;m&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 写法三</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> n = <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">export</span> &#123;n <span class="keyword">as</span> m&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">/*----------------------------------------------------*/</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 报错</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>) </span>&#123;&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">export</span> f;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 正确</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">export</span> <span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>) </span>&#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 正确</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>) </span>&#123;&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">export</span> &#123;f&#125;;</span></code><br></pre></td></tr></table></figure>



</li>
</ul>
<p><strong>注意：</strong><br><code>export</code>命令可以出现在模块的任何位置，只要处于模块顶层就可以。如果处于块级作用域内，就会报错（<code>import命令也是如此</code>）。</p>
<h1 id="import命令"><a href="#import命令" class="headerlink" title="import命令"></a>import命令</h1><p>通过<code>import</code>命令加载其他<code>JS</code>文件中<code>export</code>输出的模块。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// main.js</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">import</span> &#123; firstName, lastName, year &#125; <span class="keyword">from</span> <span class="string">'./profile.js'</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">setName</span>(<span class="params">element</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  element.textContent = firstName + <span class="string">' '</span> + lastName;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>



<ul>
<li><p><code>import</code>命令使用<code>as</code>关键字，将输入的变量重命名。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">import</span> &#123; lastName <span class="keyword">as</span> surname &#125; <span class="keyword">from</span> <span class="string">'./profile.js'</span>;</span></code><br></pre></td></tr></table></figure>
</li>
<li><p><code>import</code>命令输入的变量都是只读的，因为它的本质是输入接口。但是修改对象属性是允许的（不允许在加载模块的脚本里面改写接口）</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">import</span> &#123;a&#125; <span class="keyword">from</span> <span class="string">'./xxx.js'</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//报错</span></span></code><br><code class="highlight javascript"><span class="line">a = &#123;&#125;; <span class="comment">// Syntax Error : 'a' is read-only;</span></span></code><br></pre></td></tr></table></figure>
</li>
<li><p><code>import</code>后面的<code>from</code>指定模块文件的位置，可以是相对路径，也可以是绝对路径，<code>.js</code>后缀可以省略。如果只是模块名，不带有路径，那么必须有配置文件，告诉 JavaScript 引擎该模块的位置。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">import</span> &#123;myMethod&#125; <span class="keyword">from</span> <span class="string">'util'</span>;</span></code><br></pre></td></tr></table></figure>
</li>
<li><p><code>import</code>命令具有提升效果，会提升到整个模块的头部，首先执行。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">foo();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">import</span> &#123; foo &#125; <span class="keyword">from</span> <span class="string">'my_module'</span>;</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面的代码不会报错，因为<code>import</code>的执行早于<code>foo</code>的调用。这种行为的本质是，<code>import</code>命令是编译阶段执行的，在代码运行之前。</p>
</blockquote>
</li>
<li><p><code>import</code>是静态执行，所以不能使用表达式和变量，这些只有在运行时才能得到结果的语法结构。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 报错</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">import</span> &#123; <span class="string">'f'</span> + <span class="string">'oo'</span> &#125; <span class="keyword">from</span> <span class="string">'my_module'</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 报错</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> <span class="built_in">module</span> = <span class="string">'my_module'</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">import</span> &#123; foo &#125; <span class="keyword">from</span> <span class="built_in">module</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 报错</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">if</span> (x === <span class="number">1</span>) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">import</span> &#123; foo &#125; <span class="keyword">from</span> <span class="string">'module1'</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125; <span class="keyword">else</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">import</span> &#123; foo &#125; <span class="keyword">from</span> <span class="string">'module2'</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>
</li>
<li><p><code>import</code>语句会执行所加载的模块，因此可以有下面的写法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">import</span> <span class="string">'lodash'</span>;</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>如果多次重复执行同一句<code>import</code>语句，那么只会执行一次，而不会执行多次。</p>
</blockquote>
</li>
</ul>
<h1 id="模块的整体加载"><a href="#模块的整体加载" class="headerlink" title="模块的整体加载"></a>模块的整体加载</h1><p>除了指定加载某个输出值，还可以使用整体加载，即用星号（<code>*</code>）指定一个对象，所有输出值都加载在这个对象上面。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// circle.js</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">export</span> <span class="function"><span class="keyword">function</span> <span class="title">area</span>(<span class="params">radius</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="built_in">Math</span>.PI * radius * radius;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">export</span> <span class="function"><span class="keyword">function</span> <span class="title">circumference</span>(<span class="params">radius</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="number">2</span> * <span class="built_in">Math</span>.PI * radius;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//main.js</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//使用星号整体加载</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> circle <span class="keyword">from</span> <span class="string">'./circle'</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">console</span>.log(<span class="string">'圆面积：'</span> + circle.area(<span class="number">4</span>));</span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">console</span>.log(<span class="string">'圆周长：'</span> + circle.circumference(<span class="number">14</span>));</span></code><br></pre></td></tr></table></figure>



<h1 id="export-default命令"><a href="#export-default命令" class="headerlink" title="export default命令"></a>export default命令</h1><p>使用该命令为模块指定默认输出。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// export-default.js</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(<span class="string">'foo'</span>);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// import-default.js</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">import</span> customName <span class="keyword">from</span> <span class="string">'./export-default'</span>;</span></code><br><code class="highlight javascript"><span class="line">customName(); <span class="comment">// 'foo'</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p><code>export default</code>命令用于指定模块的默认输出。显然，一个模块只能有一个默认输出，因此<code>export default</code>命令只能使用一次。</p>
</blockquote>
<h1 id="export与import的复合写法"><a href="#export与import的复合写法" class="headerlink" title="export与import的复合写法"></a>export与import的复合写法</h1><p>如果在一个模块之中，先输入后输出同一个模块，<code>import</code>语句可以与<code>export</code>语句写在一起。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">export</span> &#123; foo, bar &#125; <span class="keyword">from</span> <span class="string">'my_module'</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 可以简单理解为</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">import</span> &#123; foo, bar &#125; <span class="keyword">from</span> <span class="string">'my_module'</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">export</span> &#123; foo, bar &#125;;</span></code><br></pre></td></tr></table></figure>

<p>上面代码中，<code>export</code>和<code>import</code>语句可以结合在一起，写成一行。但需要注意的是，写成一行以后，<code>foo</code>和<code>bar</code>实际上并没有被导入当前模块，只是相当于对外转发了这两个接口，导致当前模块不能直接使用<code>foo</code>和<code>bar</code>。</p>
<h1 id="跨模块常量"><a href="#跨模块常量" class="headerlink" title="跨模块常量"></a>跨模块常量</h1><p><code>const</code>声明的常量只在当前代码块有效。如果想设置跨模块的常量（即跨多个文件），或者说一个值要被多个模块共享，可以采用下面的写法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// constants.js 模块</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">export</span> <span class="keyword">const</span> A = <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">export</span> <span class="keyword">const</span> B = <span class="number">3</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">export</span> <span class="keyword">const</span> C = <span class="number">4</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// test1.js 模块</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> constants <span class="keyword">from</span> <span class="string">'./constants'</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">console</span>.log(constants.A); <span class="comment">// 1</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">console</span>.log(constants.B); <span class="comment">// 3</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// test2.js 模块</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">import</span> &#123;A, B&#125; <span class="keyword">from</span> <span class="string">'./constants'</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">console</span>.log(A); <span class="comment">// 1</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">console</span>.log(B); <span class="comment">// 3</span></span></code><br></pre></td></tr></table></figure>

<p>如果要使用的常量非常多，可以建一个专门的<code>constants</code>目录，将各种常量写在不同的文件里面，保存在该目录下。</p>
]]></content>
      <categories>
        <category>前端开发</category>
        <category>ES6学习</category>
      </categories>
      <tags>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript6--Class的基本使用</title>
    <url>/2019/12/07/ECMAScript6/18%E3%80%81class/</url>
    <content><![CDATA[<p>在ES6之前,想要实现类需要通过构造函数</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Point</span>(<span class="params">x, y</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">this</span>.x = x;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">this</span>.y = y;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">Point.prototype.toString = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="string">'('</span> + <span class="keyword">this</span>.x + <span class="string">', '</span> + <span class="keyword">this</span>.y + <span class="string">')'</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> p = <span class="keyword">new</span> Point(<span class="number">1</span>, <span class="number">2</span>);</span></code><br></pre></td></tr></table></figure>

<p>通过构造函数来实现类写起来不清晰，且继承起来十分麻烦，很容易出错。</p>
<p><strong>所以，他来了</strong>，ES6引入了class（类）这个概念，作为对象模板。通过class关键字，可以定义类。</p>
<a id="more"></a>

<h1 id="class关键字"><a href="#class关键字" class="headerlink" title="class关键字"></a>class关键字</h1><p>ES6的class可以看作只是一个语法糖，它的绝大部分功能，ES5都可以做到。一个简单的例子：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">constructor</span>(x, y) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">this</span>.x = x;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">this</span>.y = y;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">  toString() &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> <span class="string">'('</span> + <span class="keyword">this</span>.x + <span class="string">', '</span> + <span class="keyword">this</span>.y + <span class="string">')'</span>;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<p><strong><code>construntor</code>方法</strong>：这个方法就是类的构造方法,而this关键字则代表实例对象,简言之这里就是写类的属性的地方.</p>
<p>类里面还可以写一些方法：例如<code>toString()</code>方法</p>
<h2 id="constructor方法"><a href="#constructor方法" class="headerlink" title="constructor方法"></a>constructor方法</h2><p><code>constructor</code>方法是类的默认方法，通过new命令生成实例时，自动调用该方法。一个类必须有<code>constructor</code>方法，如果没有显式定义，一个空的<code>constructor</code>方法会被默认添加。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于</span></span></code><br><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">constructor</span>() &#123;&#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>类必须使用<code>new</code>调用,否则会报错。这是它跟普通构造函数的一个主要区别。</p>
</blockquote>
<h2 id="类的实例"><a href="#类的实例" class="headerlink" title="类的实例"></a>类的实例</h2><p>生成类的实例的写法，与ES5完全一样，也是使用<code>new</code>命令。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">// ...</span></span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 报错</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> point = Point(<span class="number">2</span>, <span class="number">3</span>);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 正确</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> point = <span class="keyword">new</span> Point(<span class="number">2</span>, <span class="number">3</span>);</span></code><br></pre></td></tr></table></figure>



<h2 id="取值函数（getter）和存值函数（setter）"><a href="#取值函数（getter）和存值函数（setter）" class="headerlink" title="取值函数（getter）和存值函数（setter）"></a>取值函数（getter）和存值函数（setter）</h2><p>在“类”的内部可以使用get和set关键字，对某个属性设置存值函数和取值函数，拦截该属性的存取行为。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Myclass</span></span>&#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">constructor</span>()&#123;</span></code><br><code class="highlight javascript"><span class="line">        <span class="keyword">this</span>.data = &#123;</span></code><br><code class="highlight javascript"><span class="line">            a:<span class="number">0</span></span></code><br><code class="highlight javascript"><span class="line">        &#125;;</span></code><br><code class="highlight javascript"><span class="line">    &#125;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">get</span> a()&#123;</span></code><br><code class="highlight javascript"><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.data.a;</span></code><br><code class="highlight javascript"><span class="line">    &#125;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">set</span> a(val)&#123;</span></code><br><code class="highlight javascript"><span class="line">        <span class="keyword">this</span>.data.a = val;</span></code><br><code class="highlight javascript"><span class="line">    &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> myclass = <span class="keyword">new</span> Myclass();</span></code><br><code class="highlight javascript"><span class="line">myclass.a = <span class="number">13</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">console</span>.log(myclass.a);</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>设置get和set的属性不能是constructor中的属性，但是可以跟属性值为对象中的属性同名。</p>
</blockquote>
<h2 id="属性表达式"><a href="#属性表达式" class="headerlink" title="属性表达式"></a>属性表达式</h2><p>类的属性名，可以采用表达式</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> methodName = <span class="string">'getArea'</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Square</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">constructor</span>(length) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="comment">// ...</span></span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">  [methodName]() &#123;</span></code><br><code class="highlight javascript"><span class="line">     <span class="built_in">console</span>.log(<span class="string">'我是动态方法名哦!!'</span>);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//调用属性表达式</span></span></code><br><code class="highlight javascript"><span class="line">Square.getArea()</span></code><br></pre></td></tr></table></figure>





<h2 id="Class表达式"><a href="#Class表达式" class="headerlink" title="Class表达式"></a>Class表达式</h2><p>可以像函数一样使用表达式定义一个类</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> MyClass = <span class="class"><span class="keyword">class</span> <span class="title">Me</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  getClassName() &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> Me.name; <span class="comment">//Me</span></span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意:使用class表达式定义类，它的name属性只能在类的内部才能访问到,name属性会返回类的名字</p>
</blockquote>
<p>立即执行的Class</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> person = <span class="keyword">new</span> <span class="class"><span class="keyword">class</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">constructor</span>(name) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">this</span>.name = name;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">  sayName() &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.name);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;(<span class="string">'张三'</span>);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">person.sayName(); <span class="comment">// "张三"</span></span></code><br></pre></td></tr></table></figure>



<h2 id="注意点"><a href="#注意点" class="headerlink" title="注意点"></a>注意点</h2><ul>
<li>严格模式</li>
</ul>
<blockquote>
<p>类的内部默认就是严格模式</p>
</blockquote>
<ul>
<li>不存在提升</li>
</ul>
<blockquote>
<p>类不存在变量提升,这一点与ES5完全不同。</p>
</blockquote>
<ul>
<li>name属性</li>
</ul>
<blockquote>
<p>name属性返回紧跟在 class 关键字后面的类名</p>
</blockquote>
<ul>
<li>Generator 方法</li>
</ul>
<blockquote>
<p>如果某个方法之前加上星号( * )，就表示该方法是一个Generator函数</p>
</blockquote>
<ul>
<li>this的指向</li>
</ul>
<blockquote>
<p>类的方法内部如果含有this，它默认指向类的实例。</p>
<p>解决this指向问题:</p>
<ol>
<li>可以在构造方法中绑定this</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Logger</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">constructor</span>() &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">this</span>.printName = <span class="keyword">this</span>.printName.bind(<span class="keyword">this</span>);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">// ...</span></span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<p>2.使用箭头函数</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Obj</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">constructor</span>() &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">this</span>.getThis = <span class="function"><span class="params">()</span> =&gt;</span> <span class="keyword">this</span>;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> myObj = <span class="keyword">new</span> Obj();</span></code><br><code class="highlight javascript"><span class="line">myObj.getThis() === myObj <span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>
</blockquote>
<h1 id="静态方法"><a href="#静态方法" class="headerlink" title="静态方法"></a>静态方法</h1><p>类相当于实例的原型,所有在类中定义的方法,都会被实例继承。</p>
<p>如果不想一个方法被继承，可以在方法前面加上static关键字，这就称为静态方法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Foo</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">static</span> bar() &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">this</span>.baz();</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">static</span> baz() &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">console</span>.log(<span class="string">'hello'</span>);</span></code><br><code class="highlight javascript"><span class="line">  &#125;    </span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">Foo.bar() <span class="comment">// hello</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> foo = <span class="keyword">new</span> Foo();</span></code><br><code class="highlight javascript"><span class="line">foo.bar()</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// TypeError: foo.bar is not a function</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>如果静态方法包含this关键字，这个this指的是类，而不是实例。<br><em>父类的静态方法可以被子类继承，也可以在super对象上调用</em></p>
</blockquote>
<h1 id="new-target-属性"><a href="#new-target-属性" class="headerlink" title="new.target 属性"></a>new.target 属性</h1><p>该属性一般用在构造函数之中，返回<code>new</code>命令作用于的那个构造函数。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params">name</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">if</span> (<span class="keyword">new</span>.target !== <span class="literal">undefined</span>) &#123;      </span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">this</span>.name = name;</span></code><br><code class="highlight javascript"><span class="line">  &#125; <span class="keyword">else</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'必须使用 new 命令生成实例'</span>);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> person = <span class="keyword">new</span> Person(<span class="string">'张三'</span>); <span class="comment">// 正确</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> notAPerson = Person.call(person, <span class="string">'张三'</span>);  <span class="comment">// 报错</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p><code>new.target</code>在Class内部调用返回当前Class</p>
<p>当子类继承父类时，<code>new.target</code>返回子类</p>
</blockquote>
<h1 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h1><p><code>Class</code>通过<code>extend</code>关键字实现继承。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<h2 id="super关键字"><a href="#super关键字" class="headerlink" title="super关键字"></a>super关键字</h2><p><code>super</code>关键字既可以当作函数使用，也可以当作对象使用。这两种情况下，它的用法完全不同。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">   p() &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> <span class="number">2</span>;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">constructor</span>() &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">super</span>();</span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">super</span>.p()); <span class="comment">// 2</span></span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> b = <span class="keyword">new</span> B();</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>ES6要求，子类的构造函数中必须执行一次super函数，即调用父类的构造函数<br><strong>注意：</strong><br>1.super虽然代表了父类的构造函数，但是返回的是子类的实例，即super内部的this指的是子类的实例。<br>2.super作为函数只能用在子类的构造函数中。<br>3.super作为对象时，在普通方法中，指向父类的原型对象；在静态方法中，指向父类。</p>
</blockquote>
]]></content>
      <categories>
        <category>前端开发</category>
        <category>ES6学习</category>
      </categories>
      <tags>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript6--async函数</title>
    <url>/2019/12/05/ECMAScript6/17%E3%80%81async%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<h1 id="含义"><a href="#含义" class="headerlink" title="含义"></a>含义</h1><p>ES2017 标准引入了 async 函数，使得异步操作变得更加方便。<br>async 函数是什么？一句话，它就是 Generator 函数的语法糖。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> asyncReadFile = <span class="keyword">async</span> <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">const</span> f1 = <span class="keyword">await</span> readFile(<span class="string">'/etc/fstab'</span>);</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">const</span> f2 = <span class="keyword">await</span> readFile(<span class="string">'/etc/shells'</span>);</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(f1.toString());</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(f2.toString());</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p><code>async</code>函数就是将 Generator 函数的星号（<code>*</code>）替换成<code>async</code>，将<code>yield</code>替换成<code>await</code>，仅此而已。</p>
</blockquote>
<p><code>async</code>函数对 Generator 函数的改进，体现在以下四点：</p>
<a id="more"></a>

<ol>
<li><p>内置执行器<br><code>async</code>函数自带执行器,也就是说,<code>async</code>函数的执行也普通函数一模一样,只要一行。不像<code>Generator</code>函数需要调用<code>next</code>方法。</p>
</li>
<li><p>更好的语义<br><code>async</code>和<code>await</code>，比起星号和<code>yield</code>，语义更清楚了。<code>async</code>表示函数里有异步操作，<code>await</code>表示紧跟在后面的表达式需要等待结果。</p>
</li>
<li><p>更广的适用性</p>
<p><code>async</code>函数的<code>await</code>命令后面，可以是 Promise 对象和原始类型的值（数值、字符串和布尔值，但这时会自动转成立即 resolved 的 Promise 对象）。</p>
</li>
<li><p>返回值是<code>Promise</code></p>
<p><code>async</code>函数的返回值是 Promise 对象，这比 Generator 函数的返回值是 Iterator 对象方便多了。你可以用<code>then</code>方法指定下一步的操作。</p>
</li>
</ol>
<blockquote>
<p> 进一步说，<code>async</code>函数完全可以看作多个异步操作，包装成的一个 Promise 对象，而<code>await</code>命令就是内部<code>then</code>命令的语法糖。</p>
</blockquote>
<h1 id="基本用法"><a href="#基本用法" class="headerlink" title="基本用法"></a>基本用法</h1><p><code>async</code>函数返回一个 Promise 对象，可以使用<code>then</code>方法添加回调函数。当函数执行的时候，一旦遇到<code>await</code>就会先返回，等到异步操作完成，再接着执行函数体内后面的语句。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">getStockPriceByName</span>(<span class="params">name</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">const</span> symbol = <span class="keyword">await</span> <span class="string">'啊'</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">const</span> stockPrice = <span class="keyword">await</span> <span class="string">'嚏'</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="string">'ok'</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line">getStockPriceByName().then(<span class="function"><span class="keyword">function</span>(<span class="params">result</span>)</span>&#123;</span></code><br><code class="highlight javascript"><span class="line">	<span class="built_in">console</span>.log(result); <span class="comment">//ok</span></span></code><br><code class="highlight javascript"><span class="line">&#125;)</span></code><br></pre></td></tr></table></figure>

<h1 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h1><h2 id="返回Promise对象"><a href="#返回Promise对象" class="headerlink" title="返回Promise对象"></a>返回Promise对象</h2><p><code>async</code>函数返回一个 Promise 对象。<br><code>async</code>函数内部<code>return</code>语句返回的值，会成为<code>then</code>方法回调函数的参数。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="string">'hello world'</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">f().then(<span class="function"><span class="params">v</span> =&gt;</span> <span class="built_in">console</span>.log(v))</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// "hello world"</span></span></code><br></pre></td></tr></table></figure>



<p><code>async</code>函数内部抛出错误，会导致返回的 Promise 对象变为<code>reject</code>状态。抛出的错误对象会被<code>catch</code>方法回调函数接收到。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'出错了'</span>);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">f().then(</span></code><br><code class="highlight javascript"><span class="line">  v =&gt; <span class="built_in">console</span>.log(v),</span></code><br><code class="highlight javascript"><span class="line">  e =&gt; <span class="built_in">console</span>.log(e)</span></code><br><code class="highlight javascript"><span class="line">)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// Error: 出错了</span></span></code><br></pre></td></tr></table></figure>



<h2 id="Promise对象的状态变化"><a href="#Promise对象的状态变化" class="headerlink" title="Promise对象的状态变化"></a>Promise对象的状态变化</h2><p><code>async</code>函数返回的 Promise 对象，必须等到内部所有<code>await</code>命令后面的 Promise 对象执行完，才会发生状态改变，除非遇到<code>return</code>语句或者抛出错误。也就是说，只有<code>async</code>函数内部的异步操作执行完，才会执行<code>then</code>方法指定的回调函数。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">getTitle</span>(<span class="params">url</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">let</span> response = <span class="keyword">await</span> fetch(url);</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">let</span> html = <span class="keyword">await</span> response.text();</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> html.match(<span class="regexp">/&lt;title&gt;([\s\S]+)&lt;\/title&gt;/i</span>)[<span class="number">1</span>];</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line">getTitle(<span class="string">'https://tc39.github.io/ecma262/'</span>).then(<span class="built_in">console</span>.log)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// "ECMAScript 2017 Language Specification"</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码中，函数<code>getTitle</code>内部有三个操作：抓取网页、取出文本、匹配页面标题。只有这三个操作全部完成，才会执行<code>then</code>方法里面的<code>console.log</code>。</p>
</blockquote>
<h2 id="await命令"><a href="#await命令" class="headerlink" title="await命令"></a>await命令</h2><p>正常情况下，<code>await</code>命令后面是一个 Promise 对象，返回该对象的结果。如果不是 Promise 对象，就直接返回对应的值。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">// 等同于</span></span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">// return 123;</span></span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="keyword">await</span> <span class="number">123</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">f().then(<span class="function"><span class="params">v</span> =&gt;</span> <span class="built_in">console</span>.log(v))</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 123</span></span></code><br></pre></td></tr></table></figure>

<p>另一种情况是，<code>await</code>命令后面是一个<code>thenable</code>对象（即定义<code>then</code>方法的对象），那么<code>await</code>会将其等同于 Promise 对象。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Sleep</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">constructor</span>(timeout) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">this</span>.timeout = timeout;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">  then(resolve, reject) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">const</span> startTime = <span class="built_in">Date</span>.now();</span></code><br><code class="highlight javascript"><span class="line">    setTimeout(</span></code><br><code class="highlight javascript"><span class="line">      () =&gt; resolve(<span class="built_in">Date</span>.now() - startTime),</span></code><br><code class="highlight javascript"><span class="line">      <span class="keyword">this</span>.timeout</span></code><br><code class="highlight javascript"><span class="line">    );</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">(<span class="keyword">async</span> () =&gt; &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">const</span> sleepTime = <span class="keyword">await</span> <span class="keyword">new</span> Sleep(<span class="number">1000</span>);</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(sleepTime);</span></code><br><code class="highlight javascript"><span class="line">&#125;)();</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 1000</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码中，<code>await</code>命令后面是一个<code>Sleep</code>对象的实例。这个实例不是 Promise 对象，但是因为定义了<code>then</code>方法，<code>await</code>会将其视为<code>Promise</code>处理。</p>
</blockquote>
<p><code>await</code>命令后面的 Promise 对象如果变为<code>reject</code>状态，则<code>reject</code>的参数会被<code>catch</code>方法的回调函数接收到。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">await</span> <span class="built_in">Promise</span>.reject(<span class="string">'出错了'</span>);</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">await</span> <span class="built_in">Promise</span>.resolve(<span class="string">'hello world'</span>); <span class="comment">// 不会执行,因为第一个await语句状态变成了reject。</span></span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">f()</span></code><br><code class="highlight javascript"><span class="line">.then(<span class="function"><span class="params">v</span> =&gt;</span> <span class="built_in">console</span>.log(v))</span></code><br><code class="highlight javascript"><span class="line">.catch(<span class="function"><span class="params">e</span> =&gt;</span> <span class="built_in">console</span>.log(e))</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 出错了</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意，上面代码中，<code>await</code>语句前面没有<code>return</code>，但是<code>reject</code>方法的参数依然传入了<code>catch</code>方法的回调函数。这里如果在<code>await</code>前面加上<code>return</code>，效果是一样的。</p>
</blockquote>
<p>有时，我们希望即使前一个异步操作失败，也不要中断后面的异步操作。这时可以将第一个<code>await</code>放在<code>try...catch</code>结构里面，这样不管这个异步操作是否成功，第二个<code>await</code>都会执行。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">try</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">await</span> <span class="built_in">Promise</span>.reject(<span class="string">'出错了'</span>);</span></code><br><code class="highlight javascript"><span class="line">  &#125; <span class="keyword">catch</span>(e) &#123;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="keyword">await</span> <span class="built_in">Promise</span>.resolve(<span class="string">'hello world'</span>);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">f()</span></code><br><code class="highlight javascript"><span class="line">.then(<span class="function"><span class="params">v</span> =&gt;</span> <span class="built_in">console</span>.log(v))</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// hello world</span></span></code><br></pre></td></tr></table></figure>

<p>另一种方法是<code>await</code>后面的 Promise 对象再跟一个<code>catch</code>方法，处理前面可能出现的错误。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">await</span> <span class="built_in">Promise</span>.reject(<span class="string">'出错了'</span>)</span></code><br><code class="highlight javascript"><span class="line">    .catch(<span class="function"><span class="params">e</span> =&gt;</span> <span class="built_in">console</span>.log(e));</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="keyword">await</span> <span class="built_in">Promise</span>.resolve(<span class="string">'hello world'</span>);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">f()</span></code><br><code class="highlight javascript"><span class="line">.then(<span class="function"><span class="params">v</span> =&gt;</span> <span class="built_in">console</span>.log(v))</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 出错了</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// hello world</span></span></code><br></pre></td></tr></table></figure>



<h2 id="错误处理"><a href="#错误处理" class="headerlink" title="错误处理"></a>错误处理</h2><p>如果<code>await</code>后面的异步操作出错，那么等同于<code>async</code>函数返回的 Promise 对象被<code>reject</code>。<br>防止出错的方法，也是将其放在<code>try...catch</code>代码块之中。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">main</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">try</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">const</span> val1 = <span class="keyword">await</span> firstStep();</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">const</span> val2 = <span class="keyword">await</span> secondStep(val1);</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">const</span> val3 = <span class="keyword">await</span> thirdStep(val1, val2);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">console</span>.log(<span class="string">'Final: '</span>, val3);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">catch</span> (err) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">console</span>.error(err);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>





<h2 id="使用注意点"><a href="#使用注意点" class="headerlink" title="使用注意点"></a>使用注意点</h2><ul>
<li><p>最好把<code>await</code>命令放在<code>try...catch</code>代码块中。</p>
</li>
<li><p>多个<code>await</code>命令后面的异步操作，如果不存在继发关系，最好让它们同时触发。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">//继发关系</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> foo = <span class="keyword">await</span> getFoo();</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> bar = <span class="keyword">await</span> getBar();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//改写</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 写法一</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [foo, bar] = <span class="keyword">await</span> <span class="built_in">Promise</span>.all([getFoo(), getBar()]);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 写法二</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> fooPromise = getFoo();</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> barPromise = getBar();</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> foo = <span class="keyword">await</span> fooPromise;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> bar = <span class="keyword">await</span> barPromise;</span></code><br></pre></td></tr></table></figure>
</li>
<li><p><code>await</code>命令只能用在<code>async</code>函数之中，如果用在普通函数，就会报错。</p>
</li>
<li><p>async 函数可以保留运行堆栈。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>前端开发</category>
        <category>ES6学习</category>
      </categories>
      <tags>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>vue2.0学习(二)---计算属性和监听属性</title>
    <url>/2019/12/03/vue2.0/Vue-Study2/</url>
    <content><![CDATA[<blockquote>
<p>两者都用来相应数据变化</p>
<p>计算属性适合于一些简单的运算</p>
<p>监听属性适用于异步加载数据或开销较大的操作</p>
</blockquote>
<a id="more"></a>

<h1 id="计算属性-computed"><a href="#计算属性-computed" class="headerlink" title="计算属性(computed)"></a>计算属性(computed)</h1><figure class="highlight html"><table><tr><td class="code"><pre><code class="highlight html"><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"example"</span>&gt;</span></span></code><br><code class="highlight html"><span class="line">	<span class="tag">&lt;<span class="name">p</span>&gt;</span>Orginal message:"&#123;&#123;message&#125;&#125;"<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span></code><br><code class="highlight html"><span class="line">	<span class="tag">&lt;<span class="name">p</span>&gt;</span>Computed reversed message:"&#123;&#123;reversedMessage&#125;&#125;"<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span></code><br><code class="highlight html"><span class="line"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></code><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><code class="highlight vue"><span class="line">var vm &#x3D; new Vue(&#123;</span></code><br><code class="highlight vue"><span class="line">	el:&quot;#example&quot;,</span></code><br><code class="highlight vue"><span class="line">	data:&#123;</span></code><br><code class="highlight vue"><span class="line">		message:&quot;hello&quot;</span></code><br><code class="highlight vue"><span class="line">	&#125;,</span></code><br><code class="highlight vue"><span class="line">	computed:&#123;</span></code><br><code class="highlight vue"><span class="line">		reversedMessage:function()&#123;</span></code><br><code class="highlight vue"><span class="line">			&#x2F;&#x2F;&#39;this&#39;指向vm实例</span></code><br><code class="highlight vue"><span class="line">			return this.message.split(&#39;&#39;),reverse().join();</span></code><br><code class="highlight vue"><span class="line">			</span></code><br><code class="highlight vue"><span class="line">			&#x2F;&#x2F;该方法的执行依赖于this.message的值是否发生改版,这里不能设置message的值,</span></code><br><code class="highlight vue"><span class="line">			&#x2F;&#x2F;例如:this.message&#x3D;&quot;改变了&quot;</span></code><br><code class="highlight vue"><span class="line">			&#x2F;&#x2F;但是获得message的值并进行一些操作</span></code><br><code class="highlight vue"><span class="line">		&#125;</span></code><br><code class="highlight vue"><span class="line">	&#125;</span></code><br><code class="highlight vue"><span class="line">&#125;);</span></code><br><code class="highlight vue"><span class="line"></span></code><br><code class="highlight vue"><span class="line">&#x2F;&#x2F;data中的message的值一旦发生改变,计算属性(computed)中的reversedMessage方法就会执行</span></code><br></pre></td></tr></table></figure>



<h2 id="计算属性与方法"><a href="#计算属性与方法" class="headerlink" title="计算属性与方法"></a>计算属性与方法</h2><ol>
<li><p>计算属性是基于他们的依赖关系进行缓存的,只有在依赖关系发生改变时他们才能更新变化。这意味着只要message没有发生变化,多次访问reverseMessage计算属性会立即返回之前的缓存结果，而不必再次执行函数,但是计算属性在页面渲染后就会执行一次</p>
</li>
<li><p>方法总会在每次触发重新渲染时调用函数</p>
</li>
</ol>
<h2 id="计算属性的-setter"><a href="#计算属性的-setter" class="headerlink" title="计算属性的 setter"></a>计算属性的 setter</h2><p>computed 属性默认只有get,不过在需要是也可以提供一个set<br>博主个人理解:计算属性可以有多个依赖属性</p>
<ul>
<li>get: 获取依赖的属性值</li>
<li>set:设置依赖的属性值</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><code class="highlight plain"><span class="line">&#x2F;&#x2F;部分代码</span></code><br><code class="highlight plain"><span class="line">computed:&#123;</span></code><br><code class="highlight plain"><span class="line">	num2:&#123;</span></code><br><code class="highlight plain"><span class="line">		get:function()&#123;</span></code><br><code class="highlight plain"><span class="line">			return this.num-1;</span></code><br><code class="highlight plain"><span class="line">		&#125;,</span></code><br><code class="highlight plain"><span class="line">		set:function(val)&#123;		&#x2F;&#x2F;接收设置的值,这里的val是一个形参,可以自己设置成别的</span></code><br><code class="highlight plain"><span class="line">			this.num &#x3D; val;</span></code><br><code class="highlight plain"><span class="line">		&#125;</span></code><br><code class="highlight plain"><span class="line">	&#125;</span></code><br><code class="highlight plain"><span class="line">&#125;</span></code><br><code class="highlight plain"><span class="line"></span></code><br><code class="highlight plain"><span class="line">vm.num &#x3D; 111;          &#x2F;&#x2F;这里会先执行set函数进行赋值,然后再执行get函数,结果为110</span></code><br><code class="highlight plain"><span class="line">注意:set函数肯定是给其依赖的属性赋值</span></code><br></pre></td></tr></table></figure>


<h1 id="侦听属性-watch"><a href="#侦听属性-watch" class="headerlink" title="侦听属性(watch)"></a>侦听属性(watch)</h1><p>通过watch来响应数据的变化,适用于异步加载数据或开销较大的操作</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><code class="highlight vue"><span class="line">var vm &#x3D; new Vue(&#123;</span></code><br><code class="highlight vue"><span class="line">  el:&quot;#test&quot;,</span></code><br><code class="highlight vue"><span class="line">  data:&#123;</span></code><br><code class="highlight vue"><span class="line">   name:&quot;lk&quot;,</span></code><br><code class="highlight vue"><span class="line">   user:&#123;</span></code><br><code class="highlight vue"><span class="line">      id:1001,</span></code><br><code class="highlight vue"><span class="line">      name:&quot;刘凯&quot;,</span></code><br><code class="highlight vue"><span class="line">      age:23,</span></code><br><code class="highlight vue"><span class="line">   &#125;</span></code><br><code class="highlight vue"><span class="line">  &#125;,</span></code><br><code class="highlight vue"><span class="line">  watch:&#123;</span></code><br><code class="highlight vue"><span class="line">     name:function(newValue,oldValue)&#123;              &#x2F;&#x2F;监视name属性</span></code><br><code class="highlight vue"><span class="line">        console.log(&quot;修改之前的值:&quot; + newValue,&quot;修改之后的值:&quot; + oldValue);</span></code><br><code class="highlight vue"><span class="line">     &#125;,</span></code><br><code class="highlight vue"><span class="line">     user:&#123;                     &#x2F;&#x2F;监视user对象</span></code><br><code class="highlight vue"><span class="line">      handler:function(newValue,oldValue)&#123;     &#x2F;&#x2F;注意这里的两个参数是对象</span></code><br><code class="highlight vue"><span class="line">		&#x2F;&#x2F;由于user是一个对象,是按引用访问的,所以这里的newValue和oldValue是两个对象</span></code><br><code class="highlight vue"><span class="line">        console.log(&quot;修改之前的值:&quot; + newValue.name,&quot;修改之后的值:&quot; + oldValue.name);</span></code><br><code class="highlight vue"><span class="line">      &#125;,</span></code><br><code class="highlight vue"><span class="line">      deep:true        &#x2F;&#x2F;用来检测对象内部的属性变化,如果不加就是监测对象引用地址的改变</span></code><br><code class="highlight vue"><span class="line">  &#125;</span></code><br><code class="highlight vue"><span class="line"> &#125;</span></code><br><code class="highlight vue"><span class="line">&#125;);</span></code><br><code class="highlight vue"><span class="line">&#x2F;&#x2F;注意:此用法不能监视对象里具体的一个属性</span></code><br><code class="highlight vue"><span class="line">	&#x2F;&#x2F;如果监视的是一个对象的话,那么回调函数里的参数就是监视的对象了</span></code><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>前端开发</category>
      </categories>
      <tags>
        <tag>vue2.0</tag>
      </tags>
  </entry>
  <entry>
    <title>vue.js2.0学习(一)——基础</title>
    <url>/2019/12/02/vue2.0/Vue-Study1/</url>
    <content><![CDATA[<p>vue.js是一套构建用户界面的渐进式框架。<br><code>Vue</code>只关注视图层，采用自底向上增量开发的设计。<br><code>Vue</code> 的目标是通过尽可能简单的 API 实现响应的数据绑定和组合的视图组件。</p>
<a id="more"></a>

<h1 id="Vue语法格式"><a href="#Vue语法格式" class="headerlink" title="Vue语法格式"></a><code>Vue</code>语法格式</h1><h2 id="基本格式："><a href="#基本格式：" class="headerlink" title="基本格式："></a>基本格式：</h2><figure class="highlight plain"><table><tr><td class="code"><pre><code class="highlight vue"><span class="line">var vm &#x3D; new Vue(&#123;</span></code><br><code class="highlight vue"><span class="line">&#x2F;&#x2F;选项</span></code><br><code class="highlight vue"><span class="line">&#125;)</span></code><br></pre></td></tr></table></figure>

<p>实例:</p>
<pre><code>&lt;div id=&quot;vue_det&quot;&gt;
       &lt;h1&gt;site : {{site}}&lt;/h1&gt;
       &lt;h1&gt;url : {{url}}&lt;/h1&gt;
       &lt;h1&gt;{{details()}}&lt;/h1&gt;
   &lt;/div&gt;
   &lt;script type=&quot;text/javascript&quot;&gt;
       var vm = new Vue({
           el: &apos;#vue_det&apos;,
           data: {
               site: &quot;菜鸟教程&quot;,
               url: &quot;www.runoob.com&quot;,
               alexa: &quot;10000&quot;,
           },
           methods: {
               details: function() {
                   return  this.site + &quot; - 学的不仅是技术，更是梦想！&quot;;
               }
           }
       })
   &lt;/script&gt;</code></pre><ol>
<li><code>el(element)</code>参数是<code>DOM</code>元素中的<code>id</code>。</li>
<li><code>data</code>用于定义属性(也可以是实例外部方法(函数)的返回值),<code>v-model</code>双向绑定时,<code>data</code>中的属性若为空的话可以用双引号[“”],会根据绑定的数据类型自动转换类型。</li>
<li><code>methods</code>用于定义函数,通过<code>return</code>来返回函数值。</li>
</ol>
<h2 id="设置属性"><a href="#设置属性" class="headerlink" title="设置属性"></a>设置属性</h2><pre><code>var data = { site: &quot;菜鸟教程&quot;, url: &quot;www.runoob.com&quot;, alexa: 10000}
var vm = new Vue({
    el: &apos;#vue_det&apos;,
    data: data
})</code></pre><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 它们引用相同的对象！</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">document</span>.write(vm.site === data.site) <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 设置属性也会影响到原始数据</span></span></code><br><code class="highlight javascript"><span class="line">vm.site = <span class="string">"Runoob"</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">document</span>.write(data.site + <span class="string">"&lt;br&gt;"</span>) <span class="comment">// Runoob</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// ……反之亦然</span></span></code><br><code class="highlight javascript"><span class="line">data.alexa = <span class="number">1234</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">document</span>.write(vm.alexa) <span class="comment">// 1234</span></span></code><br></pre></td></tr></table></figure>



<p><code>vue</code>实例还提供了一些有用的实例属性与方法.它们都有前缀$.以便与用户定义的属性区分开来</p>
<p>例如:</p>
<pre><code>//我们的数据对象
var data = { site: &quot;菜鸟教程&quot;, url: &quot;www.runoob.com&quot;, alexa: 10000}
var vm = new Vue({
    el: &apos;#vue_det&apos;,
    data: data
})

document.write(vm.$data === data) // true
document.write(&quot;&lt;br&gt;&quot;) // true
document.write(vm.$el === document.getElementById(&apos;vue_det&apos;)) // true</code></pre><h1 id="Vue-js模板语法"><a href="#Vue-js模板语法" class="headerlink" title="Vue.js模板语法"></a>Vue.js模板语法</h1><h2 id="插值"><a href="#插值" class="headerlink" title="插值"></a>插值</h2><ul>
<li>文本</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><code class="highlight vue"><span class="line">&lt;p id&#x3D;&quot;app&quot;&gt;&#123;&#123;message&#125;&#125;&lt;&#x2F;p&gt;</span></code><br><code class="highlight vue"><span class="line">var vm &#x3D; new Vue(&#123;</span></code><br><code class="highlight vue"><span class="line">	el:#app,</span></code><br><code class="highlight vue"><span class="line">	data:&#123;</span></code><br><code class="highlight vue"><span class="line">		message:&quot;hello world!&quot;</span></code><br><code class="highlight vue"><span class="line">	&#125;</span></code><br><code class="highlight vue"><span class="line">&#125;)</span></code><br></pre></td></tr></table></figure>






<pre><code>     使用这种方法可能会出现闪烁问题,即页面中的DOM和数据还没有加载完成,页面上会显示出大括号
解决:</code></pre><figure class="highlight css"><table><tr><td class="code"><pre><code class="highlight css"><span class="line"><span class="selector-attr">[v-cloak]</span> &#123;</span></code><br><code class="highlight css"><span class="line">  <span class="attribute">display</span>:none;</span></code><br><code class="highlight css"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<p>在使用了双大括号语法的标签里使用v-cloak</p>
<p>扩展: v-text<br>文本插值,其等同于Mustache语法</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><code class="highlight plain"><span class="line">&lt;p v-text&#x3D;&quot;msg&quot;&gt;&lt;&#x2F;p&gt;</span></code><br><code class="highlight plain"><span class="line">&lt;!--和下面的一样--&gt;</span></code><br><code class="highlight plain"><span class="line">&lt;p&gt;&lt;&#123;&#123;msg&#125;&#125;&#x2F;p&gt;</span></code><br></pre></td></tr></table></figure>


<p>​<br> 2.  使用<code>v-once</code>指令,可以一次性的插值,当数据改变时,插值处的内容不会更新</p>
<ul>
<li>Html</li>
</ul>
<p>使用v-html指令用于输出html代码</p>
<pre><code> &lt;div id=&quot;app&quot;&gt;
    &lt;div v-html=&quot;message&quot;&gt;&lt;/div&gt;
&lt;/div&gt;
&lt;script&gt;
    new Vue({
        el: &apos;#app&apos;,
        data: {
            message: &apos;&lt;h1&gt;菜鸟教程&lt;/h1&gt;&apos;
        }
    })
&lt;/script&gt;</code></pre><ul>
<li><p>属性</p>
<p>HTML属性中的值应使用v-bind指令(双大括号不能作用在HTML特性上)</p>
<p><code>&lt;button v-bind:disabled=&quot;isButtonDisabled&quot;&gt;Button&lt;/button&gt;</code>    </p>
<p>在布尔特性的情况下,它们的存在即暗示为true,  如果 <code>isButtonDisabled</code> 的值为 null ,<br><code>underfined</code>或 false,则disable的特性不会被包含在渲染出来的<code>&lt;button&gt;</code>元素中<br>(即不加载这个特性)</p>
</li>
<li><p>使用JavaScript表达式</p>
<p>vue.js支持JavaScript表达式(每个绑定都只能包含单个表达式)</p>
<p>JS表达式: 是由运算元和运算符(可选)构成，并产生运算结果的语法结构。</p>
</li>
</ul>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="highlight html"><span class="line"><span class="tag">&lt;<span class="name">div</span>&gt;</span>数学运算:&#123;&#123;x+y&#125;&#125;<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></code><br><code class="highlight html"><span class="line"><span class="tag">&lt;<span class="name">div</span>&gt;</span>三目运算符:&#123;&#123;ok?"yes":"no"&#125;&#125;<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></code><br><code class="highlight html"><span class="line"><span class="tag">&lt;<span class="name">div</span>&gt;</span>方法的调用&#123;&#123;msg.split(""),reverse().join()&#125;&#125;<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></code><br></pre></td></tr></table></figure>

<ul>
<li>指令</li>
</ul>
<p>指令是带有 v-前缀的特殊属性<br>    指令用于在表达式的值改变时,将某些行为应用到DOM上</p>
<p>例如 v-if v-bind  v-text……</p>
<ul>
<li>参数</li>
</ul>
<p>参数在指令后以冒号指明.<br>    例如 <code>v-bind:href = &quot;url&quot;</code></p>
<p>这里的<code>href</code>是参数,告知 <code>v-bind</code>指令该元素的<code>href</code>属性与表达式url的值绑定</p>
<p>参数除了是HTML元素的属性,也可以是监听的事件名,如<code>:click</code>事件</p>
<ul>
<li><p>修饰符</p>
<p>修饰符是以半角句号 <code>.</code> 指明的特殊后缀,用于指出一个指令应该以特殊方式绑定</p>
<p>例如: <code>&lt;form v-on:submit.prevent=&quot;onSubmit&quot;&gt;...&lt;/form&gt;</code></p>
<pre><code>`.prevent` 修饰符告诉 v-on 指令对于触发的事件调用`event.preventDefault()`</code></pre></li>
<li><p>用户输入</p>
<pre><code>使用 v-model 指令来实现双向数据绑定</code></pre></li>
</ul>
<p>实例:      </p>
<pre><code>&lt;div id=&quot;app&quot;&gt;
       &lt;p&gt;{{ message }}&lt;/p&gt;
       &lt;input v-model=&quot;message&quot;&gt;
   &lt;/div&gt;
   &lt;script&gt;
    new Vue({
        el: &apos;#app&apos;,
       data: {
         message: &apos;Runoob!&apos;
       }
     })
   &lt;/script&gt;</code></pre><p>按钮的事件可以使用 <code>v-on</code> 监听事件     例如:<code>v-on:click = &quot;click&quot;</code></p>
]]></content>
      <categories>
        <category>前端开发</category>
      </categories>
      <tags>
        <tag>vue2.0</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript6--Generator函数</title>
    <url>/2019/11/08/ECMAScript6/16%E3%80%81Generator/</url>
    <content><![CDATA[<h1 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h1><p>Generator 函数是 ES6 提供的一种异步编程解决方案，语法行为与传统函数完全不同。<br>Generator 函数有多种理解角度。语法上，首先可以把它理解成，Generator 函数是一个状态机，封装了多个内部状态。</p>
<p>执行 Generator 函数会返回一个遍历器对象，也就是说，Generator 函数除了状态机，还是一个遍历器对象生成函数。返回的遍历器对象，可以依次遍历 Generator 函数内部的每一个状态。</p>
<a id="more"></a>

<p>Generator函数的特征：</p>
<ol>
<li><code>function</code>关键字后面紧跟一个星号</li>
<li>函数体内部使用<code>yield</code>表达式</li>
</ol>
<p>例如:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">helloWorldGenerator</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="string">'hello'</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="string">'world'</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="string">'ending'</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//Generator函数内有三个状态</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> hw = helloWorldGenerator();</span></code><br><code class="highlight javascript"><span class="line">hw.next()</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// &#123; value: 'hello', done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">hw.next()</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// &#123; value: 'world', done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">hw.next()</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// &#123; value: 'ending', done: true &#125;</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>调用Generator函数后会返回一个遍历器对象，然后调用next方法将指针移向下一个状态。<br>Generator函数是分段执行的，<code>yield</code>表达式是暂停执行的标记,而<code>next</code>方法可以恢复执行。</p>
</blockquote>
<h2 id="yield表达式"><a href="#yield表达式" class="headerlink" title="yield表达式"></a>yield表达式</h2><p>由于 Generator 函数返回的遍历器对象，只有调用<code>next</code>方法才会遍历下一个内部状态，所以其实提供了一种可以暂停执行的函数。<code>yield</code>表达式就是暂停标志。</p>
<p>遍历器对象的<code>next</code>方法运行逻辑如下:</p>
<p>（1）遇到<code>yield</code>表达式，就暂停执行后面的操作，并将紧跟在<code>yield</code>后面的那个表达式的值，作为返回的对象的<code>value</code>属性值。</p>
<p>（2）下一次调用<code>next</code>方法时，再继续往下执行，直到遇到下一个<code>yield</code>表达式。</p>
<p>（3）如果没有再遇到新的<code>yield</code>表达式，就一直运行到函数结束，直到<code>return</code>语句为止，并将<code>return</code>语句后面的表达式的值，作为返回的对象的<code>value</code>属性值。</p>
<p>（4）如果该函数没有<code>return</code>语句，则返回的对象的<code>value</code>属性值为<code>undefined</code>。</p>
<p><code>yield</code>表达式与<code>return</code>语句的区别:</p>
<ul>
<li>每次遇到<code>yield</code>，函数暂停执行，下一次再从该位置继续向后执行，而<code>return</code>语句不具备位置记忆的功能。</li>
<li>一个函数里面，只能执行一次（或者说一个）<code>return</code>语句，但是可以执行多次（或者说多个）<code>yield</code>表达式。</li>
<li>正常函数只能返回一个值，因为只能执行一次<code>return</code>；</li>
</ul>
<p><code>yield</code>用法:</p>
<ul>
<li><p><code>yield</code>表达式如果用在另一个表达式之中，必须放在圆括号里面。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">demo</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(<span class="string">'Hello'</span> + <span class="keyword">yield</span>); <span class="comment">// SyntaxError</span></span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(<span class="string">'Hello'</span> + <span class="keyword">yield</span> <span class="number">123</span>); <span class="comment">// SyntaxError</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(<span class="string">'Hello'</span> + (<span class="keyword">yield</span>)); <span class="comment">// OK</span></span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(<span class="string">'Hello'</span> + (<span class="keyword">yield</span> <span class="number">123</span>)); <span class="comment">// OK</span></span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>
</li>
<li><p><code>yield</code>后面可以调用普通函数(Generator函数需要使用<code>yield</code>)</p>
</li>
<li><p><code>yield</code>表达式用作函数参数或放在赋值表达式的右边，可以不加括号。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">demo</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  foo(<span class="keyword">yield</span> <span class="string">'a'</span>, <span class="keyword">yield</span> <span class="string">'b'</span>); <span class="comment">// OK</span></span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">let</span> input = <span class="keyword">yield</span>; <span class="comment">// OK</span></span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>



</li>
</ul>
<h1 id="next方法的参数"><a href="#next方法的参数" class="headerlink" title="next方法的参数"></a>next方法的参数</h1><p><strong><code>yield</code>表达式本身没有返回值，或者说总是返回<code>undefined</code></strong>。<br><code>next</code>方法可以带一个参数，该参数就会被当作上一个<code>yield</code>表达式的返回值。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">gentrator</span>(<span class="params"></span>)</span>&#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">yield</span>);</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">let</span> first = (<span class="keyword">yield</span> (<span class="number">1</span>+<span class="number">2</span>));</span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">console</span>.log(first);</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">let</span> two = (<span class="keyword">yield</span> + first);</span></code><br><code class="highlight javascript"><span class="line">    </span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> <span class="string">'result'</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> gen = gentrator();</span></code><br><code class="highlight javascript"><span class="line">gen.next(<span class="number">1</span>); <span class="comment">//Object &#123; value: undefined, done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line">gen.next(<span class="number">1</span>); <span class="comment">//Object &#123; value: 3, done: false &#125;,first值为1</span></span></code><br></pre></td></tr></table></figure>



<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">f</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">for</span>(<span class="keyword">var</span> i = <span class="number">0</span>; <span class="literal">true</span>; i++) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">var</span> reset = <span class="keyword">yield</span> i;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">if</span>(reset) &#123; i = <span class="number">-1</span>; &#125;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> g = f();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">g.next() <span class="comment">// &#123; value: 0, done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line">g.next() <span class="comment">// &#123; value: 1, done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line">g.next(<span class="literal">true</span>) <span class="comment">// &#123; value: 0, done: false &#125;</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>如果<code>next</code>方法没有参数，每次运行到<code>yield</code>表达式，变量<code>reset</code>的值总是<code>undefined</code>。当<code>next</code>方法带一个参数<code>true</code>时，变量<code>reset</code>就被重置为这个参数（即<code>true</code>），因此<code>i</code>会等于<code>-1</code>，下一轮循环就会从<code>-1</code>开始递增。</p>
</blockquote>
<p>注意，由于<code>next</code>方法的参数表示上一个<code>yield</code>表达式的返回值，所以在第一次使用<code>next</code>方法时，传递参数是无效的。V8 引擎直接忽略第一次使用<code>next</code>方法时的参数，只有从第二次使用<code>next</code>方法开始，参数才是有效的。从语义上讲，第一个<code>next</code>方法用来启动遍历器对象，所以不用带有参数。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">dataConsumer</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(<span class="string">'Started'</span>);</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(<span class="string">`1. <span class="subst">$&#123;<span class="keyword">yield</span>&#125;</span>`</span>);</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(<span class="string">`2. <span class="subst">$&#123;<span class="keyword">yield</span>&#125;</span>`</span>);</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="string">'result'</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> genObj = dataConsumer();</span></code><br><code class="highlight javascript"><span class="line">genObj.next();</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// Started</span></span></code><br><code class="highlight javascript"><span class="line">genObj.next(<span class="string">'a'</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 1. a</span></span></code><br><code class="highlight javascript"><span class="line">genObj.next(<span class="string">'b'</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 2. b</span></span></code><br></pre></td></tr></table></figure>

<h1 id="for…of循环"><a href="#for…of循环" class="headerlink" title="for…of循环"></a>for…of循环</h1><p><code>for...of</code>循环可以自动遍历 Generator 函数运行时生成的<code>Iterator</code>对象，且此时不再需要调用<code>next</code>方法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="number">2</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="number">3</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="number">4</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="number">5</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="number">6</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> v <span class="keyword">of</span> foo()) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(v);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 1 2 3 4 5</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意，一旦<code>next</code>方法的返回对象的<code>done</code>属性为<code>true</code>，<code>for...of</code>循环就会中止，且不包含该返回对象，所以上面代码的<code>return</code>语句返回的<code>6</code>，不包括在<code>for...of</code>循环之中。</p>
</blockquote>
<h1 id="Generator-prototype-throw"><a href="#Generator-prototype-throw" class="headerlink" title="Generator.prototype.throw()"></a>Generator.prototype.throw()</h1><p>Generator 函数返回的遍历器对象，都有一个<code>throw</code>方法，可以在函数体外抛出错误，然后在 Generator 函数体内捕获。</p>
<blockquote>
<p><code>throw</code>方法抛出的错误要被内部捕获，前提是必须至少执行过一次<code>next</code>方法。</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> g = <span class="function"><span class="keyword">function</span>* (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">try</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">yield</span>;</span></code><br><code class="highlight javascript"><span class="line">  &#125; <span class="keyword">catch</span> (e) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">console</span>.log(<span class="string">'内部捕获'</span>, e);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> i = g();</span></code><br><code class="highlight javascript"><span class="line">i.next();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">try</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">  i.throw(<span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'a'</span>));</span></code><br><code class="highlight javascript"><span class="line">  i.throw(<span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'b'</span>));</span></code><br><code class="highlight javascript"><span class="line">&#125; <span class="keyword">catch</span> (e) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(<span class="string">'外部捕获'</span>, e);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 内部捕获 a</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 外部捕获 b</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>遍历器对象<code>i</code>连续抛出两个错误。第一个错误被 Generator 函数体内的<code>catch</code>语句捕获。<code>i</code>第二次抛出错误，由于 Generator 函数内部的<code>catch</code>语句已经执行过了，不会再捕捉到这个错误了，所以这个错误就被抛出了 Generator 函数体，被函数体外的<code>catch</code>语句捕获。<strong>之所以不会被Generator函数体内的catch语句捕获是因为yield表达式已经全部执行完了,即不可能再有错误发生了</strong></p>
</blockquote>
<p>如果Generator函数内部没有部署<code>try...catch</code>代码块，那么<code>throw</code>方法抛出的错误，将被外部<code>try...catch</code>代码块捕获。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> g = <span class="function"><span class="keyword">function</span>* (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">yield</span>;</span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">console</span>.log(<span class="string">'内部捕获'</span>, e);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> i = g();</span></code><br><code class="highlight javascript"><span class="line">i.next();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">try</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">  i.throw(<span class="string">'a'</span>);</span></code><br><code class="highlight javascript"><span class="line">  i.throw(<span class="string">'b'</span>);</span></code><br><code class="highlight javascript"><span class="line">&#125; <span class="keyword">catch</span> (e) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(<span class="string">'外部捕获'</span>, e);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 外部捕获 a</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>如果Generator函数内部和外部都没有部署<code>try...catch</code>代码块，那么程序将报错，直接中断执行。</p>
</blockquote>
<p><code>throw</code>方法被捕获以后，会附带执行下一条<code>yield</code>表达式。也就是说，会附带执行一次<code>next</code>方法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> gen = <span class="function"><span class="keyword">function</span>* <span class="title">gen</span>(<span class="params"></span>)</span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">try</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">yield</span> <span class="built_in">console</span>.log(<span class="string">'a'</span>);</span></code><br><code class="highlight javascript"><span class="line">  &#125; <span class="keyword">catch</span> (e) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="comment">// ...</span></span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="built_in">console</span>.log(<span class="string">'b'</span>);</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="built_in">console</span>.log(<span class="string">'c'</span>);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> g = gen();</span></code><br><code class="highlight javascript"><span class="line">g.next() <span class="comment">// a</span></span></code><br><code class="highlight javascript"><span class="line">g.throw() <span class="comment">// b</span></span></code><br><code class="highlight javascript"><span class="line">g.next() <span class="comment">// c</span></span></code><br></pre></td></tr></table></figure>

<p>Generator 函数体外抛出的错误，可以在函数体内捕获；反过来，Generator 函数体内抛出的错误，也可以被函数体外的<code>catch</code>捕获。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">var</span> x = <span class="keyword">yield</span> <span class="number">3</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">var</span> y = x.toUpperCase();</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> y;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> it = foo();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">it.next(); <span class="comment">// &#123; value:3, done:false &#125;</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">try</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">  it.next(<span class="number">42</span>);</span></code><br><code class="highlight javascript"><span class="line">&#125; <span class="keyword">catch</span> (err) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(err);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<h1 id="Generator-prototype-return"><a href="#Generator-prototype-return" class="headerlink" title="Generator.prototype.return()"></a>Generator.prototype.return()</h1><p>Generator 函数返回的遍历器对象，还有一个<code>return</code>方法，可以返回给定的值，并且终结遍历 Generator 函数。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">gen</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="number">2</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="number">3</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> g = gen();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">g.next()        <span class="comment">// &#123; value: 1, done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line">g.return(<span class="string">'foo'</span>) <span class="comment">// &#123; value: "foo", done: true &#125;</span></span></code><br><code class="highlight javascript"><span class="line">g.next()        <span class="comment">// &#123; value: undefined, done: true &#125;</span></span></code><br></pre></td></tr></table></figure>

<p>如果<code>return</code>方法调用时，不提供参数，则返回值的<code>value</code>属性为<code>undefined</code>。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">gen</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="number">2</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="number">3</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> g = gen();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">g.next()        <span class="comment">// &#123; value: 1, done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line">g.return() <span class="comment">// &#123; value: undefined, done: true &#125;</span></span></code><br></pre></td></tr></table></figure>



<p>如果 Generator 函数内部有<code>try...finally</code>代码块，且正在执行<code>try</code>代码块，那么<code>return</code>方法会导致立刻进入<code>finally</code>代码块，执行完以后，整个函数才会结束。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">numbers</span> (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">try</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">yield</span> <span class="number">2</span>;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">yield</span> <span class="number">3</span>;</span></code><br><code class="highlight javascript"><span class="line">  &#125; <span class="keyword">finally</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">yield</span> <span class="number">4</span>;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">yield</span> <span class="number">5</span>;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="number">6</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> g = numbers();</span></code><br><code class="highlight javascript"><span class="line">g.next() <span class="comment">// &#123; value: 1, done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line">g.next() <span class="comment">// &#123; value: 2, done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line">g.return(<span class="number">7</span>) <span class="comment">// &#123; value: 4, done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line">g.next() <span class="comment">// &#123; value: 5, done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line">g.next() <span class="comment">// &#123; value: 7, done: true &#125;</span></span></code><br></pre></td></tr></table></figure>

<h1 id="next-、throw-、return-的共同点"><a href="#next-、throw-、return-的共同点" class="headerlink" title="next()、throw()、return() 的共同点"></a>next()、throw()、return() 的共同点</h1><p><code>next()</code>、<code>throw()</code>、<code>return()</code>这三个方法本质上是同一件事，可以放在一起理解。它们的作用都是让 Generator 函数恢复执行，并且使用不同的语句替换<code>yield</code>表达式。</p>
<p><code>next()</code>是将<code>yield</code>表达式替换成一个值。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> g = <span class="function"><span class="keyword">function</span>* (<span class="params">x, y</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">let</span> add = <span class="keyword">yield</span> x+<span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">let</span> result = <span class="keyword">yield</span> add + y;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> result;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> gen = g(<span class="number">1</span>, <span class="number">2</span>);</span></code><br><code class="highlight javascript"><span class="line">gen.next(); <span class="comment">// Object &#123;value: 2, done: false&#125;</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">gen.next(<span class="number">4</span>); <span class="comment">// Object &#123;value: 6, done: false&#125;</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 相当于将 let add = yield x + 1</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 替换成 let add = 4;</span></span></code><br><code class="highlight javascript"><span class="line">gen.next(<span class="number">5</span>);   <span class="comment">//Object &#123; value: 5, done: true &#125;</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 相当于将 let result = yield add + y</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 替换成 let result = 5;</span></span></code><br></pre></td></tr></table></figure>

<p><code>throw()</code>是将<code>yield</code>表达式替换成一个<code>throw</code>语句。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">gen.throw(<span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'出错了'</span>)); <span class="comment">// Uncaught Error: 出错了</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 相当于将 let result = yield add + y</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 替换成 let result = throw(new Error('出错了'));</span></span></code><br></pre></td></tr></table></figure>

<p><code>return()</code>是将<code>yield</code>表达式替换成一个<code>return</code>语句。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">gen.return(<span class="number">2</span>); <span class="comment">// Object &#123;value: 2, done: true&#125;</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 相当于将 let result = yield add + y</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 替换成 let result = return 2;</span></span></code><br></pre></td></tr></table></figure>

<h1 id="yield-表达式"><a href="#yield-表达式" class="headerlink" title="yield* 表达式"></a>yield* 表达式</h1><p>如果在 Generator 函数内部，调用另一个 Generator 函数。需要在前者的函数体内部，自己手动完成遍历。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">inner</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="string">'hello!'</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">outer1</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="string">'open'</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> inner();</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="string">'close'</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> gen = outer1()</span></code><br><code class="highlight javascript"><span class="line">gen.next().value <span class="comment">// "open"</span></span></code><br><code class="highlight javascript"><span class="line">gen.next().value <span class="comment">// 返回一个遍历器对象</span></span></code><br><code class="highlight javascript"><span class="line">gen.next().value <span class="comment">// "close"</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">outer2</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="string">'open'</span></span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span>* inner()</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="string">'close'</span></span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> gen = outer2()</span></code><br><code class="highlight javascript"><span class="line">gen.next().value <span class="comment">// "open"</span></span></code><br><code class="highlight javascript"><span class="line">gen.next().value <span class="comment">// "hello!"</span></span></code><br><code class="highlight javascript"><span class="line">gen.next().value <span class="comment">// "close"</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面例子中，<code>outer2</code>使用了<code>yield*</code>，<code>outer1</code>没使用。结果就是，<code>outer1</code>返回一个遍历器对象，<code>outer2</code>返回该遍历器对象的内部值。</p>
</blockquote>
<p><code>yield*</code>后面的 Generator 函数（没有<code>return</code>语句时），等同于在 Generator 函数内部，部署一个<code>for...of</code>循环。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">concat</span>(<span class="params">iter1, iter2</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span>* iter1;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span>* iter2;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">concat</span>(<span class="params">iter1, iter2</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">for</span> (<span class="keyword">var</span> value <span class="keyword">of</span> iter1) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">yield</span> value;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">for</span> (<span class="keyword">var</span> value <span class="keyword">of</span> iter2) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">yield</span> value;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p><code>yield*</code>后面的 Generator 函数（没有<code>return</code>语句时），不过是<code>for...of</code>的一种简写形式，完全可以用后者替代前者。反之，在有<code>return</code>语句时，则需要用<code>var value = yield* iterator</code>的形式获取<code>return</code>语句的值。</p>
</blockquote>
<p>如果<code>yield*</code>后面跟着一个数组，由于数组原生支持遍历器，因此就会遍历数组成员。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">gen</span>(<span class="params"></span>)</span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span>* [<span class="string">"a"</span>, <span class="string">"b"</span>, <span class="string">"c"</span>];</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">gen().next() <span class="comment">// &#123; value:"a", done:false &#125;</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>实际上，任何数据结构只要有 Iterator 接口，就可以被<code>yield*</code>遍历。</p>
</blockquote>
<h1 id="作为对象属性的Generator函数"><a href="#作为对象属性的Generator函数" class="headerlink" title="作为对象属性的Generator函数"></a>作为对象属性的Generator函数</h1><p>如果一个对象的属性是 Generator 函数，可以简写成下面的形式。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> obj = &#123;</span></code><br><code class="highlight javascript"><span class="line">  * myGeneratorMethod() &#123;</span></code><br><code class="highlight javascript"><span class="line">    ···</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<p>上面代码中，<code>myGeneratorMethod</code>属性前面有一个星号，表示这个属性是一个 Generator 函数。</p>
<p>它的完整形式如下，与上面的写法是等价的。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> obj = &#123;</span></code><br><code class="highlight javascript"><span class="line">  myGeneratorMethod: <span class="function"><span class="keyword">function</span>* (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="comment">// ···</span></span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<h1 id="Generator函数的this"><a href="#Generator函数的this" class="headerlink" title="Generator函数的this"></a>Generator函数的this</h1><p>Generator 函数总是返回一个遍历器，ES6 规定这个遍历器是 Generator 函数的实例，也继承了 Generator 函数的<code>prototype</code>对象上的方法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">g</span>(<span class="params"></span>) </span>&#123;&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">g.prototype.hello = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="string">'hi!'</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> obj = g();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">obj <span class="keyword">instanceof</span> g <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line">obj.hello() <span class="comment">// 'hi!'</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码表明，Generator 函数<code>g</code>返回的遍历器<code>obj</code>，是<code>g</code>的实例，而且继承了<code>g.prototype</code>。但是，如果把<code>g</code>当作普通的构造函数，并不会生效，因为<code>g</code>返回的总是遍历器对象，而不是<code>this</code>对象。</p>
</blockquote>
<p>Generator 函数也不能跟<code>new</code>命令一起用，会报错。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">F</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="keyword">this</span>.x = <span class="number">2</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="keyword">this</span>.y = <span class="number">3</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">new</span> F()</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// TypeError: F is not a constructor</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p><code>new</code>命令跟构造函数<code>F</code>一起使用，结果报错，因为<code>F</code>不是构造函数。</p>
</blockquote>
<p>让 Generator 函数返回一个正常的对象实例，既可以用<code>next</code>方法，又可以获得正常的<code>this</code>的变通方法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">gen</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">this</span>.a = <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="keyword">this</span>.b = <span class="number">2</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="keyword">this</span>.c = <span class="number">3</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">F</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> gen.call(gen.prototype);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> f = <span class="keyword">new</span> F();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">f.next();  <span class="comment">// Object &#123;value: 2, done: false&#125;</span></span></code><br><code class="highlight javascript"><span class="line">f.next();  <span class="comment">// Object &#123;value: 3, done: false&#125;</span></span></code><br><code class="highlight javascript"><span class="line">f.next();  <span class="comment">// Object &#123;value: undefined, done: true&#125;</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">f.a <span class="comment">// 1</span></span></code><br><code class="highlight javascript"><span class="line">f.b <span class="comment">// 2</span></span></code><br><code class="highlight javascript"><span class="line">f.c <span class="comment">// 3</span></span></code><br></pre></td></tr></table></figure>

<h1 id="应用"><a href="#应用" class="headerlink" title="应用"></a>应用</h1><h2 id="控制流程管理"><a href="#控制流程管理" class="headerlink" title="控制流程管理"></a>控制流程管理</h2><p>如果有一个多步操作非常耗时，采用回调函数，可能会写成下面这样。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">step1(<span class="function"><span class="keyword">function</span> (<span class="params">value1</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  step2(value1, <span class="function"><span class="keyword">function</span>(<span class="params">value2</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">    step3(value2, <span class="function"><span class="keyword">function</span>(<span class="params">value3</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">      step4(value3, <span class="function"><span class="keyword">function</span>(<span class="params">value4</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">        <span class="comment">// Do something with value4</span></span></code><br><code class="highlight javascript"><span class="line">      &#125;);</span></code><br><code class="highlight javascript"><span class="line">    &#125;);</span></code><br><code class="highlight javascript"><span class="line">  &#125;);</span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br></pre></td></tr></table></figure>

<p>Generator 函数可以进一步改善代码运行流程。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">longRunningTask</span>(<span class="params">value1</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">try</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">var</span> value2 = <span class="keyword">yield</span> step1(value1);</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">var</span> value3 = <span class="keyword">yield</span> step2(value2);</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">var</span> value4 = <span class="keyword">yield</span> step3(value3);</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">var</span> value5 = <span class="keyword">yield</span> step4(value4);</span></code><br><code class="highlight javascript"><span class="line">    <span class="comment">// Do something with value4</span></span></code><br><code class="highlight javascript"><span class="line">  &#125; <span class="keyword">catch</span> (e) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="comment">// Handle any error from step1 through step4</span></span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<p>然后，使用一个函数，按次序自动执行所有步骤。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">scheduler(longRunningTask(initialValue));</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">scheduler</span>(<span class="params">task</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">var</span> taskObj = task.next(task.value);</span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">// 如果Generator函数未结束，就继续调用</span></span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">if</span> (!taskObj.done) &#123;</span></code><br><code class="highlight javascript"><span class="line">    task.value = taskObj.value</span></code><br><code class="highlight javascript"><span class="line">    scheduler(task);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意，上面这种做法，只适合同步操作，即所有的<code>task</code>都必须是同步的，不能有异步操作。因为这里的代码一得到返回值，就继续往下执行，没有判断异步操作何时完成。</p>
</blockquote>
<p>下面，利用<code>for...of</code>循环会自动依次执行<code>yield</code>命令的特性，提供一种更一般的控制流管理的方法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> steps = [step1Func, step2Func, step3Func];</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">iterateSteps</span>(<span class="params">steps</span>)</span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">for</span> (<span class="keyword">var</span> i=<span class="number">0</span>; i&lt; steps.length; i++)&#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">var</span> step = steps[i];</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">yield</span> step();</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码中，数组<code>steps</code>封装了一个任务的多个步骤，Generator 函数<code>iterateSteps</code>则是依次为这些步骤加上<code>yield</code>命令。</p>
</blockquote>
<p>将任务分解成步骤之后，还可以将项目分解成多个依次执行的任务。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> jobs = [job1, job2, job3];</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">iterateJobs</span>(<span class="params">jobs</span>)</span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">for</span> (<span class="keyword">var</span> i=<span class="number">0</span>; i&lt; jobs.length; i++)&#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">var</span> job = jobs[i];</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">yield</span>* iterateSteps(job.steps);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码中，数组<code>jobs</code>封装了一个项目的多个任务，Generator 函数<code>iterateJobs</code>则是依次为这些任务加上<code>yield*</code>命令。</p>
</blockquote>
<p>最后，就可以用<code>for...of</code>循环一次性依次执行所有任务的所有步骤。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> step <span class="keyword">of</span> iterateJobs(jobs))&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(step.id);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>再次提醒，上面的做法只能用于所有步骤都是同步操作的情况，不能有异步操作的步骤。如果想要依次执行异步的步骤，必须使用后面的《异步操作》一章介绍的方法。</p>
</blockquote>
<p><code>for...of</code>的本质是一个<code>while</code>循环，所以上面的代码实质上执行的是下面的逻辑。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> it = iterateJobs(jobs);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> res = it.next();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">while</span> (!res.done)&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">var</span> result = res.value;</span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">// ...</span></span></code><br><code class="highlight javascript"><span class="line">  res = it.next();</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>





<h2 id="部署Iterator接口"><a href="#部署Iterator接口" class="headerlink" title="部署Iterator接口"></a>部署Iterator接口</h2><p>利用 Generator 函数，可以在任意对象上部署 Iterator 接口。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">iterEntries</span>(<span class="params">obj</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">let</span> keys = <span class="built_in">Object</span>.keys(obj);</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i=<span class="number">0</span>; i &lt; keys.length; i++) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">let</span> key = keys[i];</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">yield</span> [key, obj[key]];</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> myObj = &#123; <span class="attr">foo</span>: <span class="number">3</span>, <span class="attr">bar</span>: <span class="number">7</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> [key, value] <span class="keyword">of</span> iterEntries(myObj)) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(key, value);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// foo 3</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// bar 7</span></span></code><br></pre></td></tr></table></figure>



<h2 id="作为数据结构"><a href="#作为数据结构" class="headerlink" title="作为数据结构"></a>作为数据结构</h2><p>Generator 可以看作是数据结构，更确切地说，可以看作是一个数组结构，因为 Generator 函数可以返回一系列的值，这意味着它可以对任意表达式，提供类似数组的接口。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">doStuff</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> fs.readFile.bind(<span class="literal">null</span>, <span class="string">'hello.txt'</span>);</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> fs.readFile.bind(<span class="literal">null</span>, <span class="string">'world.txt'</span>);</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> fs.readFile.bind(<span class="literal">null</span>, <span class="string">'and-such.txt'</span>);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<p>上面代码就是依次返回三个函数，但是由于使用了 Generator 函数，导致可以像处理数组那样，处理这三个返回的函数。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (task <span class="keyword">of</span> doStuff()) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">// task是一个函数，可以像回调函数那样使用它</span></span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>前端开发</category>
        <category>ES6学习</category>
      </categories>
      <tags>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript6--Iterator 和 for...of 循环</title>
    <url>/2019/11/05/ECMAScript6/15%E3%80%81Iterator%E5%92%8Cfor...of%E5%BE%AA%E7%8E%AF/</url>
    <content><![CDATA[<h1 id="Iterator（遍历器）的概念"><a href="#Iterator（遍历器）的概念" class="headerlink" title="Iterator（遍历器）的概念"></a>Iterator（遍历器）的概念</h1><p>遍历器（Iterator）是一种接口，为各种不同的数据结构提供统一的访问机制。任何数据结构只要部署 Iterator 接口，就可以完成遍历操作（即依次处理该数据结构的所有成员）。</p>
<p><strong>Iterator的作用有三个：</strong></p>
<ol>
<li>为各种数据结构，提供一个统一的、简便的访问接口；</li>
<li>使得数据结构的成员能够按某种次序排列；</li>
<li>ES6 创造了一种新的遍历命令<code>for...of</code>循环，Iterator 接口主要供<code>for...of</code>消费。</li>
</ol>
<a id="more"></a>

<p><strong>Iterator的遍历过程是这样的：</strong></p>
<ol>
<li>创建一个指针对象，指向当前数据结构的起始位置。也就是说，遍历器对象本质上，就是一个指针对象。</li>
<li>第一次调用指针对象的<code>next</code>方法，可以将指针指向数据结构的第一个成员。</li>
<li>第二次调用指针对象的<code>next</code>方法，指针就指向数据结构的第二个成员。</li>
<li>不断调用指针对象的<code>next</code>方法，直到它指向数据结构的结束位置。</li>
</ol>
<blockquote>
<p>每一次调用<code>next</code>方法，都会返回数据结构的当前成员的信息。具体来说，就是返回一个包含<code>value</code>和<code>done</code>两个属性的对象。其中，<code>value</code>属性是当前成员的值，<code>done</code>属性是一个布尔值，表示遍历是否结束。</p>
</blockquote>
<p>一个模拟<code>next</code>方法返回值的列子:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> it = makeIterator([<span class="string">'a'</span>, <span class="string">'b'</span>]);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">it.next() <span class="comment">// &#123; value: "a", done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line">it.next() <span class="comment">// &#123; value: "b", done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line">it.next() <span class="comment">// &#123; value: undefined, done: true &#125;</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">makeIterator</span>(<span class="params">array</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">var</span> nextIndex = <span class="number">0</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">    next: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">      <span class="keyword">return</span> nextIndex &lt; array.length ?</span></code><br><code class="highlight javascript"><span class="line">        &#123;<span class="attr">value</span>: array[nextIndex++], <span class="attr">done</span>: <span class="literal">false</span>&#125; :</span></code><br><code class="highlight javascript"><span class="line">        &#123;<span class="attr">value</span>: <span class="literal">undefined</span>, <span class="attr">done</span>: <span class="literal">true</span>&#125;;</span></code><br><code class="highlight javascript"><span class="line">    &#125;</span></code><br><code class="highlight javascript"><span class="line">  &#125;;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码定义了一个<code>makeIterator</code>函数，它是一个遍历器生成函数，作用就是返回一个遍历器对象。对数组<code>[&#39;a&#39;, &#39;b&#39;]</code>执行这个函数，就会返回该数组的遍历器对象（即指针对象）<code>it</code>。</p>
<p>指针对象的<code>next</code>方法，用来移动指针。开始时，指针指向数组的开始位置。然后，每次调用<code>next</code>方法，指针就会指向数组的下一个成员。第一次调用，指向<code>a</code>；第二次调用，指向<code>b</code>。</p>
<p><code>next</code>方法返回一个对象，表示当前数据成员的信息。这个对象具有<code>value</code>和<code>done</code>两个属性，<code>value</code>属性返回当前位置的成员，<code>done</code>属性是一个布尔值，表示遍历是否结束，即是否还有必要再一次调用<code>next</code>方法。</p>
<p>总之，调用指针对象的<code>next</code>方法，就可以遍历事先给定的数据结构。</p>
</blockquote>
<h1 id="默认Iterator接口"><a href="#默认Iterator接口" class="headerlink" title="默认Iterator接口"></a>默认Iterator接口</h1><p>ES6 规定，默认的 Iterator 接口部署在数据结构的<code>Symbol.iterator</code>属性，或者说，一个数据结构只要具有<code>Symbol.iterator</code>属性，就可以认为是“可遍历的”（<code>iterable</code>）。<code>Symbol.iterator</code>属性本身是一个函数，就是当前数据结构默认的遍历器生成函数。执行这个函数，就会返回一个遍历器。至于属性名<code>Symbol.iterator</code>，它是一个表达式，返回<code>Symbol</code>对象的<code>iterator</code>属性，这是一个预定义好的、类型为 Symbol 的特殊值，所以要放在方括号内。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> obj = &#123;</span></code><br><code class="highlight javascript"><span class="line">  [<span class="built_in">Symbol</span>.iterator] : <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">      next: <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">        <span class="keyword">return</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">          value: <span class="number">1</span>,</span></code><br><code class="highlight javascript"><span class="line">          done: <span class="literal">true</span></span></code><br><code class="highlight javascript"><span class="line">        &#125;;</span></code><br><code class="highlight javascript"><span class="line">      &#125;</span></code><br><code class="highlight javascript"><span class="line">    &#125;;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>对象<code>obj</code>是可遍历的（<code>iterable</code>），因为具有<code>Symbol.iterator</code>属性。执行这个属性，会返回一个遍历器对象。该对象的根本特征就是具有<code>next</code>方法。每次调用<code>next</code>方法，都会返回一个代表当前成员的信息对象，具有<code>value</code>和<code>done</code>两个属性。</p>
</blockquote>
<p>原生具备<code>Iterator</code>接口的数据结构如下：</p>
<ul>
<li>Array</li>
<li>Map</li>
<li>Set</li>
<li>String</li>
<li><code>TypedArray</code></li>
<li>函数的arguments对象</li>
<li><code>NodeList</code>对象</li>
</ul>
<p>数组的<code>Symbol.iterator</code>属性:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> arr = [<span class="string">'a'</span>, <span class="string">'b'</span>, <span class="string">'c'</span>];</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> iter = arr[<span class="built_in">Symbol</span>.iterator]();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">iter.next() <span class="comment">// &#123; value: 'a', done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line">iter.next() <span class="comment">// &#123; value: 'b', done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line">iter.next() <span class="comment">// &#123; value: 'c', done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line">iter.next() <span class="comment">// &#123; value: undefined, done: true &#125;</span></span></code><br></pre></td></tr></table></figure>



<p>对象部署遍历器接口并不是很必要，因为这时对象实际上被当作Map结构使用。</p>
<p>对象上部署遍历器接口的例子：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> obj = &#123;</span></code><br><code class="highlight javascript"><span class="line">  data: [ <span class="string">'hello'</span>, <span class="string">'world'</span> ],</span></code><br><code class="highlight javascript"><span class="line">  [<span class="built_in">Symbol</span>.iterator]() &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">const</span> self = <span class="keyword">this</span>;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">let</span> index = <span class="number">0</span>;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">      next() &#123;</span></code><br><code class="highlight javascript"><span class="line">        <span class="keyword">if</span> (index &lt; self.data.length) &#123;</span></code><br><code class="highlight javascript"><span class="line">          <span class="keyword">return</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">            value: self.data[index++],</span></code><br><code class="highlight javascript"><span class="line">            done: <span class="literal">false</span></span></code><br><code class="highlight javascript"><span class="line">          &#125;;</span></code><br><code class="highlight javascript"><span class="line">        &#125; <span class="keyword">else</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">          <span class="keyword">return</span> &#123; <span class="attr">value</span>: <span class="literal">undefined</span>, <span class="attr">done</span>: <span class="literal">true</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line">        &#125;</span></code><br><code class="highlight javascript"><span class="line">      &#125;</span></code><br><code class="highlight javascript"><span class="line">    &#125;;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>



<p>类似数组的对象部署遍历器:</p>
<blockquote>
<p>类似数组的对象：存在数值键名和<code>length</code>属性</p>
</blockquote>
<p><code>Symbol.iterator</code>方法直接引用数组的 Iterator 接口。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">NodeList.prototype[<span class="built_in">Symbol</span>.iterator] = <span class="built_in">Array</span>.prototype[<span class="built_in">Symbol</span>.iterator];</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 或者</span></span></code><br><code class="highlight javascript"><span class="line">NodeList.prototype[<span class="built_in">Symbol</span>.iterator] = [][<span class="built_in">Symbol</span>.iterator];</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">[...document.querySelectorAll(<span class="string">'div'</span>)] <span class="comment">// 可以执行了</span></span></code><br></pre></td></tr></table></figure>

<p>类似数组的对象调用数组的<code>Symbol.iterator</code>方法的例子：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> iterable = &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="number">0</span>: <span class="string">'a'</span>,</span></code><br><code class="highlight javascript"><span class="line">  <span class="number">1</span>: <span class="string">'b'</span>,</span></code><br><code class="highlight javascript"><span class="line">  <span class="number">2</span>: <span class="string">'c'</span>,</span></code><br><code class="highlight javascript"><span class="line">  length: <span class="number">3</span>,</span></code><br><code class="highlight javascript"><span class="line">  [<span class="built_in">Symbol</span>.iterator]: <span class="built_in">Array</span>.prototype[<span class="built_in">Symbol</span>.iterator]</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> item <span class="keyword">of</span> iterable) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(item); <span class="comment">// 'a', 'b', 'c'</span></span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<h1 id="调用Iterator接口的场合"><a href="#调用Iterator接口的场合" class="headerlink" title="调用Iterator接口的场合"></a>调用Iterator接口的场合</h1><h2 id="1-解构赋值"><a href="#1-解构赋值" class="headerlink" title="(1)解构赋值"></a>(1)解构赋值</h2><p>对数组和 Set 结构进行解构赋值时，会默认调用<code>Symbol.iterator</code>方法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> <span class="keyword">set</span> = new Set().add('a').add('b').add('c');</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">let [x,y] = <span class="keyword">set</span>;</span></code><br><code class="highlight javascript"><span class="line">// x='a'; y='b'</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">let [first, ...rest] = <span class="keyword">set</span>;</span></code><br><code class="highlight javascript"><span class="line">// first='a'; rest=['b','c'];</span></code><br></pre></td></tr></table></figure>



<h2 id="2-扩展运算符"><a href="#2-扩展运算符" class="headerlink" title="(2)扩展运算符"></a>(2)扩展运算符</h2><p>扩展运算符（…）也会调用默认的 Iterator 接口。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 例一</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> str = <span class="string">'hello'</span>;</span></code><br><code class="highlight javascript"><span class="line">[...str] <span class="comment">//  ['h','e','l','l','o']</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 例二</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> arr = [<span class="string">'b'</span>, <span class="string">'c'</span>];</span></code><br><code class="highlight javascript"><span class="line">[<span class="string">'a'</span>, ...arr, <span class="string">'d'</span>]</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// ['a', 'b', 'c', 'd']</span></span></code><br></pre></td></tr></table></figure>

<p><strong>（3）yield*</strong></p>
<p><code>yield*</code>后面跟的是一个可遍历的结构，它会调用该结构的遍历器接口。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> generator = <span class="function"><span class="keyword">function</span>* (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span>* [<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>];</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="number">5</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> iterator = generator();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">iterator.next() <span class="comment">// &#123; value: 1, done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line">iterator.next() <span class="comment">// &#123; value: 2, done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line">iterator.next() <span class="comment">// &#123; value: 3, done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line">iterator.next() <span class="comment">// &#123; value: 4, done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line">iterator.next() <span class="comment">// &#123; value: 5, done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line">iterator.next() <span class="comment">// &#123; value: undefined, done: true &#125;</span></span></code><br></pre></td></tr></table></figure>


<h2 id="4-其他场合"><a href="#4-其他场合" class="headerlink" title="(4)其他场合"></a>(4)其他场合</h2><p>由于数组的遍历会调用遍历器接口，所以任何接受数组作为参数的场合，其实都调用了遍历器接口。下面是一些例子。</p>
<ul>
<li><code>for...of</code></li>
<li><code>Array.from()</code></li>
<li>Map(), Set(), <code>WeakMap()</code>,<code>WeakSet()</code>（比如<code>new Map([[&#39;a&#39;,1],[&#39;b&#39;,2]])</code>）</li>
<li><code>Promise.all()</code></li>
<li><code>Promise.race()</code></li>
</ul>
<h1 id="字符串的Iterator接口"><a href="#字符串的Iterator接口" class="headerlink" title="字符串的Iterator接口"></a>字符串的Iterator接口</h1><p>字符串是一个类似数组的对象也原生具有Iterator接口。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> someString = <span class="string">"hi"</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">typeof</span> someString[<span class="built_in">Symbol</span>.iterator]</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// "function"</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> iterator = someString[<span class="built_in">Symbol</span>.iterator]();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">iterator.next()  <span class="comment">// &#123; value: "h", done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line">iterator.next()  <span class="comment">// &#123; value: "i", done: false &#125;</span></span></code><br><code class="highlight javascript"><span class="line">iterator.next()  <span class="comment">// &#123; value: undefined, done: true &#125;</span></span></code><br></pre></td></tr></table></figure>

<h1 id="Iterator接口与Generator函数"><a href="#Iterator接口与Generator函数" class="headerlink" title="Iterator接口与Generator函数"></a>Iterator接口与Generator函数</h1><p><code>Symbol.Iterator</code>方法的最简单实现,还是使用Generator函数。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> myIterable = &#123;</span></code><br><code class="highlight javascript"><span class="line">  [<span class="built_in">Symbol</span>.iterator]: <span class="function"><span class="keyword">function</span>* (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">yield</span> <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">yield</span> <span class="number">2</span>;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">yield</span> <span class="number">3</span>;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line">[...myIterable] <span class="comment">// [1, 2, 3]</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 或者采用下面的简洁写法</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> obj = &#123;</span></code><br><code class="highlight javascript"><span class="line">  * [<span class="built_in">Symbol</span>.iterator]() &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">yield</span> <span class="string">'hello'</span>;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">yield</span> <span class="string">'world'</span>;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> x <span class="keyword">of</span> obj) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(x);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// "hello"</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// "world"</span></span></code><br></pre></td></tr></table></figure>

<h1 id="遍历器对象的return-，throw"><a href="#遍历器对象的return-，throw" class="headerlink" title="遍历器对象的return()，throw()"></a>遍历器对象的return()，throw()</h1><p>遍历器对象除了具有<code>next</code>方法，还可以具有<code>return</code>方法和<code>throw</code>方法。如果自己写遍历器对象生成函数，那么<code>next</code>方法是必须部署的，<code>return</code>方法和<code>throw</code>方法是可选的。</p>
<h2 id="return-方法"><a href="#return-方法" class="headerlink" title="return()方法"></a>return()方法</h2><p>该方法的使用场合是，如果<code>for...of</code>循环提前退出（通常是因为出错，或者有<code>break</code>语句），就会调用<code>return</code>方法。如果一个对象在完成遍历前，需要清理或释放资源，就可以部署<code>return</code>方法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">readLinesSync</span>(<span class="params">file</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">    [<span class="built_in">Symbol</span>.iterator]() &#123;</span></code><br><code class="highlight javascript"><span class="line">      <span class="keyword">return</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">        next() &#123;</span></code><br><code class="highlight javascript"><span class="line">          <span class="keyword">return</span> &#123; <span class="attr">done</span>: <span class="literal">false</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line">        &#125;,</span></code><br><code class="highlight javascript"><span class="line">        <span class="keyword">return</span>() &#123;</span></code><br><code class="highlight javascript"><span class="line">          file.close();</span></code><br><code class="highlight javascript"><span class="line">          <span class="keyword">return</span> &#123; <span class="attr">done</span>: <span class="literal">true</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line">        &#125;</span></code><br><code class="highlight javascript"><span class="line">      &#125;;</span></code><br><code class="highlight javascript"><span class="line">    &#125;,</span></code><br><code class="highlight javascript"><span class="line">  &#125;;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p><code>return</code>方法必须返回一个对象</p>
</blockquote>
<h2 id="throw-方法"><a href="#throw-方法" class="headerlink" title="throw()方法"></a>throw()方法</h2><p>该方法主要是配合Generator函数使用，一般的遍历器对象用不到这个方法。</p>
<h1 id="for…of循环"><a href="#for…of循环" class="headerlink" title="for…of循环"></a>for…of循环</h1><p>for…of循环作为遍历所有数据结构的统一方法。</p>
<p>for…of循环可以使用的范围包括数组、Set和Map结构、类似数组的对象、Generator对象以及字符串。</p>
<h2 id="Set和Map结构"><a href="#Set和Map结构" class="headerlink" title="Set和Map结构"></a>Set和Map结构</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> engines = <span class="keyword">new</span> <span class="built_in">Set</span>([<span class="string">"Gecko"</span>, <span class="string">"Trident"</span>, <span class="string">"Webkit"</span>, <span class="string">"Webkit"</span>]);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> e <span class="keyword">of</span> engines) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(e);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// Gecko</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// Trident</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// Webkit</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> es6 = <span class="keyword">new</span> <span class="built_in">Map</span>();</span></code><br><code class="highlight javascript"><span class="line">es6.set(<span class="string">"edition"</span>, <span class="number">6</span>);</span></code><br><code class="highlight javascript"><span class="line">es6.set(<span class="string">"committee"</span>, <span class="string">"TC39"</span>);</span></code><br><code class="highlight javascript"><span class="line">es6.set(<span class="string">"standard"</span>, <span class="string">"ECMA-262"</span>);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> [name, value] <span class="keyword">of</span> es6) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(name + <span class="string">": "</span> + value);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// edition: 6</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// committee: TC39</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// standard: ECMA-262</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码演示了如何遍历 Set 结构和 Map 结构。值得注意的地方有两个，首先，遍历的顺序是按照各个成员被添加进数据结构的顺序。其次，Set 结构遍历时，返回的是一个值，而 Map 结构遍历时，返回的是一个数组，该数组的两个成员分别为当前 Map 成员的键名和键值。</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> map = <span class="keyword">new</span> <span class="built_in">Map</span>().set(<span class="string">'a'</span>, <span class="number">1</span>).set(<span class="string">'b'</span>, <span class="number">2</span>);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> pair <span class="keyword">of</span> map) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(pair);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// ['a', 1]</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// ['b', 2]</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> [key, value] <span class="keyword">of</span> map) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(key + <span class="string">' : '</span> + value);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// a : 1</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// b : 2</span></span></code><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>前端开发</category>
        <category>ES6学习</category>
      </categories>
      <tags>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript6--Promise对象</title>
    <url>/2019/11/03/ECMAScript6/14%E3%80%81Promise%E5%AF%B9%E8%B1%A1/</url>
    <content><![CDATA[<h1 id="Promise的含义"><a href="#Promise的含义" class="headerlink" title="Promise的含义"></a>Promise的含义</h1><p>Promise是异步编程的一种解决方案，比传统的解决方案——回调函数和事件更合理和更强大。</p>
<p>所谓Promise，简单说就是一个容器，里面保存着某个未来才会结束的事件（通常是一个异步消息）的结果。</p>
<a id="more"></a>

<p><code>Promise</code>对象有以下两个<strong>特点：</strong></p>
<p>（1）对象的状态不受外界影响。<code>Promise</code>对象代表一个异步操作，有三种状态：<code>pending</code>（进行中）、<code>fulfilled</code>（已成功）和<code>rejected</code>（已失败）。</p>
<p>（2）一旦状态改变，就不会再变，任何时候都可以得到这个结果。<code>Promise</code>对象的状态改变，只有两种可能：从<code>pending</code>变为<code>fulfilled</code>和从<code>pending</code>变为<code>rejected</code>。</p>
<p><strong>缺点：</strong></p>
<ul>
<li>无法取消<code>Promise</code></li>
<li>如果不设置回调函数，Promise内部抛出的错误，不会反应到外部。</li>
<li>当处于<code>pending</code>状态时，无法得知目前进展到哪一阶段（刚刚开始还是即将完成）</li>
</ul>
<h1 id="基本用法"><a href="#基本用法" class="headerlink" title="基本用法"></a>基本用法</h1><p><code>Promise</code>对象是一个构造函数，用来生成Promise实例。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> promise = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span>(<span class="params">resolve, reject</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">// ... some code</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">if</span> (<span class="comment">/* 异步操作成功 */</span>)&#123;</span></code><br><code class="highlight javascript"><span class="line">    resolve(value);</span></code><br><code class="highlight javascript"><span class="line">  &#125; <span class="keyword">else</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">    reject(error);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p><code>Promise</code>构造函数接收一个函数作为参数，该函数的两个参数分别是<code>resolve</code>和<code>reject</code>。它们是两个函数，由JavaScript引擎提供，不用自己部署。</p>
<p><code>resolve</code>函数的作用：<br>将<code>Promise</code>对象的状态从“未完成”变为“成功”（即从<code>pending</code>变为<code>resolved</code>），在异步操作成功时调用，并将异步操作的结果，作为参数传递出去；</p>
<p><code>reject</code>函数的作用：<br>将<code>Promise</code>对象的状态从“未完成”变为“失败”（即从<code>pending</code>变为<code>rejected</code>），在异步操作失败时调用，并将异步操作报出的错误，作为参数传递出去。</p>
</blockquote>
<p><code>Promise</code>实例生成以后，可以用<code>then</code>方法分别指定<code>resolved</code>状态和<code>rejected</code>状态的回调函数。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">promise.then(<span class="function"><span class="keyword">function</span>(<span class="params">value</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">// success</span></span></code><br><code class="highlight javascript"><span class="line">&#125;, <span class="function"><span class="keyword">function</span>(<span class="params">error</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">// failure</span></span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p><code>then</code>方法可以接受两个回调函数作为参数。第一个回调函数是<code>Promise</code>对象的状态变为<code>resolved</code>时调用，第二个回调函数是<code>Promise</code>对象的状态变为<code>rejected</code>时调用。其中，第二个函数是可选的，不一定要提供。这两个函数都接受<code>Promise</code>对象传出的值作为参数。</p>
</blockquote>
<h1 id="Promise-prototype-then"><a href="#Promise-prototype-then" class="headerlink" title="Promise.prototype.then()"></a>Promise.prototype.then()</h1><p><code>then</code>方法定义在原型对象<code>Promise.prototype</code>上的。它的作用是为<code>Promise</code>实例添加状态改变时的回调函数。<br><code>then</code>方法的第一个参数是<code>resolved</code>状态的回调函数,第二个参数(可选)是<code>rejected</code>状态的回调函数。</p>
<p><code>then</code>方法返回的是一个新的<code>Promise</code>实例。所以可以采用链式写法，即<code>then</code>方法后面在调用另一个then方法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">promise.then(<span class="function"><span class="keyword">function</span>(<span class="params">json</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> json.post;</span></code><br><code class="highlight javascript"><span class="line">&#125;).then(<span class="function"><span class="keyword">function</span>(<span class="params">post</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">// ...</span></span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br></pre></td></tr></table></figure>

<h1 id="Promise-prototype-catch"><a href="#Promise-prototype-catch" class="headerlink" title="Promise.prototype.catch()"></a>Promise.prototype.catch()</h1><p><code>catch</code>方法用于指定发生错误时的回调函数</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">promise.then(<span class="function"><span class="keyword">function</span>(<span class="params">posts</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">// ...</span></span></code><br><code class="highlight javascript"><span class="line">&#125;).catch(<span class="function"><span class="keyword">function</span>(<span class="params">error</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">// 处理 getJSON 和 前一个回调函数运行时发生的错误</span></span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(<span class="string">'发生错误！'</span>, error);</span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>如果该对象状态变为<code>resolved</code>，则会调用<code>then</code>方法指定的回调函数；如果异步操作抛出错误，状态就会变为<code>rejected</code>，就会调用<code>catch</code>方法指定的回调函数，处理这个错误。</p>
<p>如果<code>Promise</code>状态已经变成<code>resolved</code>，再抛出错误是无效的。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> promise = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span>(<span class="params">resolve, reject</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  resolve(<span class="string">'ok'</span>);</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'test'</span>);</span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br></pre></td></tr></table></figure>


</blockquote>
<h1 id="Promise-prototype-finally"><a href="#Promise-prototype-finally" class="headerlink" title="Promise.prototype.finally()"></a>Promise.prototype.finally()</h1><p><code>finally</code>方法用于指定不管Promise对象最后状态如何，都会执行的操作。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">promise</span></code><br><code class="highlight javascript"><span class="line">.then(<span class="function"><span class="params">result</span> =&gt;</span> &#123;···&#125;)</span></code><br><code class="highlight javascript"><span class="line">.catch(<span class="function"><span class="params">error</span> =&gt;</span> &#123;···&#125;)</span></code><br><code class="highlight javascript"><span class="line">.finally(<span class="function"><span class="params">()</span> =&gt;</span> &#123;···&#125;);</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p><code>finally</code>方法的回调函数不接受任何参数，所以<code>finally</code>方法里面的操作，应该是与状态无关的，不依赖于 Promise 的执行结果。</p>
</blockquote>
<h1 id="Promise-all"><a href="#Promise-all" class="headerlink" title="Promise.all()"></a>Promise.all()</h1><p><code>Promise.all()</code>方法用于将多个 Promise 实例，包装成一个新的 Promise 实例。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> p = <span class="built_in">Promise</span>.all([p1, p2, p3]);</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码中，<code>Promise.all()</code>方法接受一个数组作为参数，<code>p1</code>、<code>p2</code>、<code>p3</code>都是 Promise 实例，如果不是，就会先调用<code>Promise.resolve</code>方法，将参数转为 Promise 实例，再进一步处理。另外，<code>Promise.all()</code>方法的参数可以不是数组，但必须具有 Iterator 接口，且返回的每个成员都是 Promise 实例。</p>
</blockquote>
<p><code>p</code>的状态由<code>p1</code>、<code>p2</code>、<code>p3</code>决定，分成两种情况。</p>
<p>（1）只有<code>p1</code>、<code>p2</code>、<code>p3</code>的状态都变成<code>fulfilled</code>，<code>p</code>的状态才会变成<code>fulfilled</code>，此时<code>p1</code>、<code>p2</code>、<code>p3</code>的返回值组成一个数组，传递给<code>p</code>的回调函数。</p>
<p>（2）只要<code>p1</code>、<code>p2</code>、<code>p3</code>之中有一个被<code>rejected</code>，<code>p</code>的状态就变成<code>rejected</code>，此时第一个被<code>reject</code>的实例的返回值，会传递给<code>p</code>的回调函数。</p>
<p>如果作为参数的 Promise 实例，自己定义了<code>catch</code>方法，那么它一旦被<code>rejected</code>，并不会触发<code>Promise.all()</code>的<code>catch</code>方法。如果没有定义自己的<code>catch</code>方法，就会调用<code>Promise.all()</code>的<code>catch</code>方法。</p>
<h1 id="Promise-race"><a href="#Promise-race" class="headerlink" title="Promise.race()"></a>Promise.race()</h1><p><code>Promise.race()</code>方法同样是将多个 Promise 实例，包装成一个新的 Promise 实例。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> p = <span class="built_in">Promise</span>.race([p1, p2, p3]);</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码中，只要<code>p1</code>、<code>p2</code>、<code>p3</code>之中有一个实例率先改变状态，<code>p</code>的状态就跟着改变。那个率先改变的 Promise 实例的返回值，就传递给<code>p</code>的回调函数。</p>
</blockquote>
<p><code>Promise.race()</code>方法的参数与<code>Promise.all()</code>方法一样，如果不是 Promise 实例，就会先调用<code>Promise.resolve()</code>方法，将参数转为 Promise 实例，再进一步处理。</p>
<h1 id="Promise-allSettled"><a href="#Promise-allSettled" class="headerlink" title="Promise.allSettled()"></a>Promise.allSettled()</h1><p><code>Promise.allSettled()</code>方法接受一组 Promise 实例作为参数，包装成一个新的 Promise 实例。只有等到所有这些参数实例都返回结果，不管是<code>fulfilled</code>还是<code>rejected</code>，包装实例才会结束。该方法由 <a href="https://github.com/tc39/proposal-promise-allSettled" target="_blank" rel="noopener">ES2020</a> 引入。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> promises = [</span></code><br><code class="highlight javascript"><span class="line">  fetch(<span class="string">'/api-1'</span>),</span></code><br><code class="highlight javascript"><span class="line">  fetch(<span class="string">'/api-2'</span>),</span></code><br><code class="highlight javascript"><span class="line">  fetch(<span class="string">'/api-3'</span>),</span></code><br><code class="highlight javascript"><span class="line">];</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">await</span> <span class="built_in">Promise</span>.allSettled(promises);</span></code><br><code class="highlight javascript"><span class="line">removeLoadingIndicator();</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码对服务器发出三个请求，等到三个请求都结束，不管请求成功还是失败，加载的滚动图标就会消失。</p>
</blockquote>
<h1 id="Promise-any"><a href="#Promise-any" class="headerlink" title="Promise.any()"></a>Promise.any()</h1><p><code>Promise.any()</code>方法接受一组 Promise 实例作为参数，包装成一个新的 Promise 实例。只要参数实例有一个变成<code>fulfilled</code>状态，包装实例就会变成<code>fulfilled</code>状态；如果所有参数实例都变成<code>rejected</code>状态，包装实例就会变成<code>rejected</code>状态。该方法目前是一个第三阶段的<a href="https://github.com/tc39/proposal-promise-any" target="_blank" rel="noopener">提案</a> 。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> promises = [</span></code><br><code class="highlight javascript"><span class="line">  fetch(<span class="string">'/endpoint-a'</span>).then(<span class="function"><span class="params">()</span> =&gt;</span> <span class="string">'a'</span>),</span></code><br><code class="highlight javascript"><span class="line">  fetch(<span class="string">'/endpoint-b'</span>).then(<span class="function"><span class="params">()</span> =&gt;</span> <span class="string">'b'</span>),</span></code><br><code class="highlight javascript"><span class="line">  fetch(<span class="string">'/endpoint-c'</span>).then(<span class="function"><span class="params">()</span> =&gt;</span> <span class="string">'c'</span>),</span></code><br><code class="highlight javascript"><span class="line">];</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">try</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">const</span> first = <span class="keyword">await</span> <span class="built_in">Promise</span>.any(promises);</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(first);</span></code><br><code class="highlight javascript"><span class="line">&#125; <span class="keyword">catch</span> (error) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(error);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码中，<code>Promise.any()</code>方法的参数数组包含三个 Promise 操作。其中只要有一个变成<code>fulfilled</code>，<code>Promise.any()</code>返回的 Promise 对象就变成<code>fulfilled</code>。如果所有三个操作都变成<code>rejected</code>，那么就会<code>await</code>命令就会抛出错误。</p>
</blockquote>
<p><code>Promise.any()</code>抛出的错误，不是一个一般的错误，而是一个 AggregateError 实例。它相当于一个数组，每个成员对应一个被<code>rejected</code>的操作所抛出的错误。下面是 AggregateError 的实现示例。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">new</span> AggregateError() extends <span class="built_in">Array</span> -&gt; AggregateError</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> err = <span class="keyword">new</span> AggregateError();</span></code><br><code class="highlight javascript"><span class="line">err.push(<span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">"first error"</span>));</span></code><br><code class="highlight javascript"><span class="line">err.push(<span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">"second error"</span>));</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">throw</span> err;</span></code><br></pre></td></tr></table></figure>

<p>捕捉错误时，如果不用<code>try...catch</code>结构和 await 命令，可以像下面这样写。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Promise</span>.any(promises).then(</span></code><br><code class="highlight javascript"><span class="line">  (first) =&gt; &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="comment">// Any of the promises was fulfilled.</span></span></code><br><code class="highlight javascript"><span class="line">  &#125;,</span></code><br><code class="highlight javascript"><span class="line">  (error) =&gt; &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="comment">// All of the promises were rejected.</span></span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">);</span></code><br></pre></td></tr></table></figure>

<h1 id="Promise-resolve"><a href="#Promise-resolve" class="headerlink" title="Promise.resolve()"></a>Promise.resolve()</h1><p>有时需要将现有对象转为 Promise 对象，<code>Promise.resolve()</code>方法就起到这个作用。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> jsPromise = <span class="built_in">Promise</span>.resolve($.ajax(<span class="string">'/whatever.json'</span>));</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码将 jQuery 生成的<code>deferred</code>对象，转为一个新的 Promise 对象。</p>
</blockquote>
<p><strong><code>Promise.resolve</code>方法的参数分成四种情况：</strong></p>
<p><strong>（1）参数是一个 Promise 实例</strong></p>
<p>如果参数是 Promise 实例，那么<code>Promise.resolve</code>将不做任何修改、原封不动地返回这个实例。</p>
<p><strong>（2）参数是一个<code>thenable</code>对象</strong></p>
<p><code>thenable</code>对象指的是具有<code>then</code>方法的对象，比如下面这个对象。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> thenable = &#123;</span></code><br><code class="highlight javascript"><span class="line">  then: <span class="function"><span class="keyword">function</span>(<span class="params">resolve, reject</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">    resolve(<span class="number">42</span>);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<p><code>Promise.resolve</code>方法会将这个对象转为 Promise 对象，然后就立即执行<code>thenable</code>对象的<code>then</code>方法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> thenable = &#123;</span></code><br><code class="highlight javascript"><span class="line">  then: <span class="function"><span class="keyword">function</span>(<span class="params">resolve, reject</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">    resolve(<span class="number">42</span>);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> p1 = <span class="built_in">Promise</span>.resolve(thenable);</span></code><br><code class="highlight javascript"><span class="line">p1.then(<span class="function"><span class="keyword">function</span>(<span class="params">value</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(value);  <span class="comment">// 42</span></span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码中，<code>thenable</code>对象的<code>then</code>方法执行后，对象<code>p1</code>的状态就变为<code>resolved</code>，从而立即执行最后那个<code>then</code>方法指定的回调函数，输出 42。</p>
</blockquote>
<p><strong>（3）参数不是具有then方法的对象，或根本就不是对象</strong></p>
<p>如果参数是一个原始值，或者是一个不具有<code>then</code>方法的对象，则<code>Promise.resolve</code>方法返回一个新的 Promise 对象，状态为<code>resolved</code>。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> p = <span class="built_in">Promise</span>.resolve(<span class="string">'Hello'</span>);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">p.then(<span class="function"><span class="keyword">function</span> (<span class="params">s</span>)</span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(s)</span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// Hello</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码生成一个新的 Promise 对象的实例<code>p</code>。由于字符串<code>Hello</code>不属于异步操作（判断方法是字符串对象不具有 then 方法），返回 Promise 实例的状态从一生成就是<code>resolved</code>，所以回调函数会立即执行。<code>Promise.resolve</code>方法的参数，会同时传给回调函数。</p>
</blockquote>
<p><strong>（4）不带有任何参数</strong></p>
<p><code>Promise.resolve()</code>方法允许调用时不带参数，直接返回一个<code>resolved</code>状态的 Promise 对象。</p>
<p>所以，如果希望得到一个 Promise 对象，比较方便的方法就是直接调用<code>Promise.resolve()</code>方法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> p = <span class="built_in">Promise</span>.resolve();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">p.then(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">// ...</span></span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码的变量<code>p</code>就是一个 Promise 对象。</p>
</blockquote>
<p>需要注意的是，立即<code>resolve()</code>的 Promise 对象，是在本轮“事件循环”（event loop）的结束时执行，而不是在下一轮“事件循环”的开始时。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">setTimeout(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(<span class="string">'three'</span>);</span></code><br><code class="highlight javascript"><span class="line">&#125;, <span class="number">0</span>);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Promise</span>.resolve().then(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(<span class="string">'two'</span>);</span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">console</span>.log(<span class="string">'one'</span>);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// one</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// two</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// three</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码中，<code>setTimeout(fn, 0)</code>在下一轮“事件循环”开始时执行，<code>Promise.resolve()</code>在本轮“事件循环”结束时执行，<code>console.log(&#39;one&#39;)</code>则是立即执行，因此最先输出。</p>
</blockquote>
<h1 id="Promise-reject"><a href="#Promise-reject" class="headerlink" title="Promise.reject()"></a>Promise.reject()</h1><p><code>Promise.reject(reason)</code>方法也会返回一个新的 Promise 实例，该实例的状态为<code>rejected</code>。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> p = <span class="built_in">Promise</span>.reject(<span class="string">'出错了'</span>);</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> p = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> reject(<span class="string">'出错了'</span>))</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">p.then(<span class="literal">null</span>, <span class="function"><span class="keyword">function</span> (<span class="params">s</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(s)</span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 出错了</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码生成一个 Promise 对象的实例<code>p</code>，状态为<code>rejected</code>，回调函数会立即执行。</p>
</blockquote>
<p>注意，<code>Promise.reject()</code>方法的参数，会原封不动地作为<code>reject</code>的理由，变成后续方法的参数。这一点与<code>Promise.resolve</code>方法不一致。</p>
<h1 id="Promise-try"><a href="#Promise-try" class="headerlink" title="Promise.try()"></a>Promise.try()</h1><p>该方法是为了解决：不论函数是异步的还是同步的，都想用Promise来处理它。</p>
<p>该方法还在<a href="https://github.com/ljharb/proposal-promise-try" target="_blank" rel="noopener">提案</a>中。</p>
<p>用法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Promise</span>.try(method)</span></code><br><code class="highlight javascript"><span class="line">  .then(...)</span></code><br><code class="highlight javascript"><span class="line">  .catch(...)</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>catch可以捕获同步和异步的错误。</p>
</blockquote>
]]></content>
      <categories>
        <category>前端开发</category>
        <category>ES6学习</category>
      </categories>
      <tags>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript6--Proxy</title>
    <url>/2019/11/01/ECMAScript6/12%E3%80%81Proxy/</url>
    <content><![CDATA[<h1 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h1><p>Proxy 用于修改某些操作的默认行为，等同于在语言层面做出修改，所以属于一种“元编程”（meta programming），即对编程语言进行编程。</p>
<p>Proxy 可以理解成，在目标对象之前架设一层“拦截”，外界对该对象的访问，都必须先通过这层拦截，因此提供了一种机制，可以对外界的访问进行过滤和改写。Proxy 这个词的原意是代理，用在这里表示由它来“代理”某些操作，可以译为“代理器”。</p>
<a id="more"></a>

<p>生成Proxy实例:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> proxy = <span class="keyword">new</span> <span class="built_in">Proxy</span>(target, handler);</span></code><br></pre></td></tr></table></figure>

<ul>
<li><p>target参数表示所要拦截的目标对象</p>
</li>
<li><p>handler参数也是一个对象，用来定制拦截行为</p>
</li>
</ul>
<p>如果handler没有设置任何拦截,那就等同于直接通向原对象(target),我们可以将Proxy对象设置到<code>object.proxy</code>,从而可以在object对象上调用</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> object = &#123; <span class="attr">proxy</span>: <span class="keyword">new</span> <span class="built_in">Proxy</span>(target, handler) &#125;;</span></code><br></pre></td></tr></table></figure>

<p>Proxy实例还可以是其他对象的原型对象</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> proxy = <span class="keyword">new</span> <span class="built_in">Proxy</span>(&#123;&#125;, &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">get</span>: function(target, property) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> <span class="number">35</span>;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> obj = <span class="built_in">Object</span>.create(proxy);</span></code><br><code class="highlight javascript"><span class="line">obj.time <span class="comment">// 35</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">    (...args)。</span></code><br></pre></td></tr></table></figure>

<p>支持在handler中设置的拦截操作,一共13种:</p>
<ul>
<li><p>get(target, propKey, receiver)：</p>
<p>拦截对象属性的读取，比如<code>proxy.foo和proxy[&#39;foo&#39;]</code>。</p>
</li>
<li><p>set(target, propKey, value, receiver)：</p>
<p>拦截对象属性的设置，比如<code>proxy.foo = v或proxy[&#39;foo&#39;] = v</code>，返回一个布尔值。</p>
</li>
<li><p>has(target, propKey)：</p>
<p>拦截<code>propKey in proxy</code>的操作，返回一个布尔值。</p>
</li>
<li><p>deleteProperty(target, propKey)：</p>
<p>拦截<code>delete proxy[propKey]</code>的操作，返回一个布尔值。</p>
</li>
<li><p>ownKeys(target)：</p>
<p>拦截<code>Object.getOwnPropertyNames(proxy)</code>、<code>Object.getOwnPropertySymbols(proxy)</code>、<code>Object.keys(proxy)</code>、<code>for...in</code>循环，返回一个数组。该方法返回目标对象所有自身的属性的属性名，而<code>Object.keys()</code>的返回结果仅包括目标对象自身的可遍历属性。</p>
</li>
<li><p>getOwnPropertyDescriptor(target, propKey)：</p>
<p>​    拦截<code>Object.getOwnPropertyDescriptor(proxy, propKey)</code>，返回属性的描述对象。</p>
</li>
<li><p>defineProperty(target, propKey, propDesc)：</p>
<p>​    拦截<code>Object.defineProperty(proxy, propKey, propDesc）</code>、<code>Object.defineProperties(proxy, propDescs)</code>，返回一个布尔值。</p>
</li>
<li><p>preventExtensions(target)：</p>
<p>拦截<code>Object.preventExtensions(proxy)</code>，返回一个布尔值。</p>
</li>
<li><p>getPrototypeOf(target)：</p>
<p>拦截<code>Object.getPrototypeOf(proxy)</code>，返回一个对象。</p>
</li>
<li><p>isExtensible(target)：</p>
<p>拦截<code>Object.isExtensible(proxy)</code>，返回一个布尔值。</p>
</li>
<li><p>setPrototypeOf(target, proto)：</p>
<p>拦截<code>Object.setPrototypeOf(proxy, proto)</code>，返回一个布尔值。如果目标对象是函数，那么还有两种额外操作可以拦截。</p>
</li>
<li><p>apply(target, object, args)：</p>
<p>拦截 Proxy 实例作为函数调用的操作，比如<code>proxy(...args)</code>、<code>proxy.call(object, ...args)</code>、<code>proxy.apply(...)</code>。</p>
</li>
<li><p>construct(target, args)：</p>
<p>拦截 <code>Proxy</code>实例作为构造函数调用的操作，比如<code>new proxy</code></p>
</li>
</ul>
<h1 id="拦截方法"><a href="#拦截方法" class="headerlink" title="拦截方法"></a>拦截方法</h1><h2 id="get"><a href="#get" class="headerlink" title="get()"></a>get()</h2><p>get方法用于拦截某个属性的读取操作，可以接受三个参数，依次为目标对象、属性名和 proxy 实例本身（严格地说，是操作行为所针对的对象），其中最后一个参数可选。</p>
<p>用法:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> person = &#123;</span></code><br><code class="highlight javascript"><span class="line">  name: <span class="string">"张三"</span></span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> proxy = <span class="keyword">new</span> <span class="built_in">Proxy</span>(person, &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">get</span>: function(target, property) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">if</span> (property <span class="keyword">in</span> target) &#123;</span></code><br><code class="highlight javascript"><span class="line">      <span class="keyword">return</span> target[property];</span></code><br><code class="highlight javascript"><span class="line">    &#125; <span class="keyword">else</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">ReferenceError</span>(<span class="string">"Property \""</span> + </span></code><br><code class="highlight javascript"><span class="line">                               property + <span class="string">"\" does not exist."</span>);</span></code><br><code class="highlight javascript"><span class="line">    &#125;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">proxy.name <span class="comment">// "张三"</span></span></code><br><code class="highlight javascript"><span class="line">proxy.age <span class="comment">// 抛出一个错误</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">/*如果访问目标对象不存在的属性，会抛出一个错误。如果没有这个拦截函数，</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">访问不存在的属性，只会返回undefined。</span></span></code><br></pre></td></tr></table></figure>

<p>get方法可以继承</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> proto = <span class="keyword">new</span> <span class="built_in">Proxy</span>(&#123;&#125;, &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">get</span>(target, propertyKey, receiver) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">console</span>.log(<span class="string">'GET '</span> + propertyKey);</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> target[propertyKey];</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> obj = <span class="built_in">Object</span>.create(proto);</span></code><br><code class="highlight javascript"><span class="line">obj.foo <span class="comment">// "GET foo"</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">/*拦截操作定义在Prototype对象上面，所以如果读取obj对象继承的属性时，</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">拦截会生效。</span></span></code><br></pre></td></tr></table></figure>

<h2 id="set"><a href="#set" class="headerlink" title="set()"></a>set()</h2><p>set方法用来拦截某个属性的赋值操作，可以接受四个参数，依次为目标对象、属性名、属性值和 Proxy 实例本身，其中最后一个参数可选。</p>
<p>通过set方法可以实现数据绑定,动态更新DOM</p>
<p>注意:</p>
<ul>
<li>如果目标对象自身的某个属性，不可写且不可配置，那么set方法将不起作用。</li>
<li>严格模式下，set代理如果没有返回true，就会报错。</li>
</ul>
<h2 id="apply"><a href="#apply" class="headerlink" title="apply()"></a>apply()</h2><p>apply方法拦截函数的调用、call和apply操作。<br>apply方法可以接受三个参数，分别是目标对象、目标对象的上下文对象（this）和目标对象的参数数组。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> handler = &#123;</span></code><br><code class="highlight javascript"><span class="line">  apply (target, ctx, args) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> <span class="built_in">Reflect</span>.apply(...arguments);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<p>直接调用<code>Reflect.apply</code>方法，也会被拦截</p>
<h2 id="has"><a href="#has" class="headerlink" title="has()"></a>has()</h2><p>has方法用来拦截<code>HasProperty</code>操作，即判断对象是否具有某个属性时，这个方法会生效。典型的操作就是in运算符。<br>has方法可以接受两个参数，分别是目标对象、需查询的属性名。<br>使用has拦截隐藏某些属性,不被in运算符发现:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> handler = &#123;</span></code><br><code class="highlight javascript"><span class="line">  has (target, key) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">if</span> (key[<span class="number">0</span>] === <span class="string">'_'</span>) &#123;</span></code><br><code class="highlight javascript"><span class="line">      <span class="keyword">return</span> <span class="literal">false</span>;</span></code><br><code class="highlight javascript"><span class="line">    &#125;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> key <span class="keyword">in</span> target;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> target = &#123; <span class="attr">_prop</span>: <span class="string">'foo'</span>, <span class="attr">prop</span>: <span class="string">'foo'</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> proxy = <span class="keyword">new</span> <span class="built_in">Proxy</span>(target, handler);</span></code><br><code class="highlight javascript"><span class="line"><span class="string">'_prop'</span> <span class="keyword">in</span> proxy <span class="comment">// false</span></span></code><br></pre></td></tr></table></figure>

<p>has拦截操作对in运算符生效,但是不对for…in循环生效<br>注意:</p>
<ul>
<li>has方法拦截的是<code>HasProperty</code>操作，而不是<code>HasOwnProperty</code>操作，即has方法不判断一个属性是对象自身的属性，还是继承的属性。</li>
<li>如果原对象不可配置或者禁止扩展，这时has拦截会报错。</li>
</ul>
<h2 id="construct"><a href="#construct" class="headerlink" title="construct()"></a>construct()</h2><p> construct方法用于拦截new命令</p>
<p> construct方法可以接受两个参数:</p>
<ol>
<li><code>target</code>：目标对象</li>
<li><code>args</code>：构造函数的参数对象</li>
<li><code>newTarget</code>：创造实例对象时，new命令作用的构造函数（下面例子的p）</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> p = <span class="keyword">new</span> <span class="built_in">Proxy</span>(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;&#125;, &#123;</span></code><br><code class="highlight javascript"><span class="line">  construct: <span class="function"><span class="keyword">function</span>(<span class="params">target, args</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">console</span>.log(<span class="string">'called: '</span> + args.join(<span class="string">', '</span>));</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> &#123; <span class="attr">value</span>: args[<span class="number">0</span>] * <span class="number">10</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">(<span class="keyword">new</span> p(<span class="number">1</span>)).value</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// "called: 1"</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 10</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意:construct方法返回的必须是一个对象，否则会报错。</p>
</blockquote>
<h2 id="deleteProperty"><a href="#deleteProperty" class="headerlink" title="deleteProperty()"></a>deleteProperty()</h2><p><code>deleteProperty</code>方法用于拦截<code>delete</code>操作，如果这个方法抛出错误或者返回<code>false</code>，当前属性就无法被<code>delete</code>命令删除。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> handler = &#123;</span></code><br><code class="highlight javascript"><span class="line">  deleteProperty (target, key) &#123;</span></code><br><code class="highlight javascript"><span class="line">    invariant(key, <span class="string">'delete'</span>);</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">delete</span> target[key];</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">invariant</span> (<span class="params">key, action</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">if</span> (key[<span class="number">0</span>] === <span class="string">'_'</span>) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">`Invalid attempt to <span class="subst">$&#123;action&#125;</span> </span></span></code><br><code class="highlight javascript"><span class="line"><span class="string">							private "<span class="subst">$&#123;key&#125;</span>" property`</span>);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> target = &#123; <span class="attr">_prop</span>: <span class="string">'foo'</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> proxy = <span class="keyword">new</span> <span class="built_in">Proxy</span>(target, handler);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">delete</span> proxy._prop</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// Error: Invalid attempt to delete private "_prop" property</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意，目标对象自身的不可配置（configurable）的属性，不能被<code>deleteProperty</code>方法删除，否则报错。</p>
</blockquote>
<h2 id="defineProperty"><a href="#defineProperty" class="headerlink" title="defineProperty()"></a>defineProperty()</h2><p><code>defineProperty</code>方法拦截了<code>Object.defineProperty</code>操作。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> handler = &#123;</span></code><br><code class="highlight javascript"><span class="line">  defineProperty (target, key, descriptor) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> target = &#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> proxy = <span class="keyword">new</span> <span class="built_in">Proxy</span>(target, handler);</span></code><br><code class="highlight javascript"><span class="line">proxy.foo = <span class="string">'bar'</span> <span class="comment">// 不会生效</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意，如果目标对象不可扩展（non-extensible），则<code>defineProperty</code>不能增加目标对象上不存在的属性，否则会报错。另外，如果目标对象的某个属性不可写（writable）或不可配置（configurable），则<code>defineProperty</code>方法不得改变这两个设置。</p>
</blockquote>
<h2 id="getOwnPropertyDescriptor"><a href="#getOwnPropertyDescriptor" class="headerlink" title="getOwnPropertyDescriptor()"></a>getOwnPropertyDescriptor()</h2><p><code>getOwnPropertyDescriptor</code>方法拦截<code>Object.getOwnPropertyDescriptor()</code>，返回一个属性描述对象或者<code>undefined</code>。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">ar handler = &#123;</span></code><br><code class="highlight javascript"><span class="line">  getOwnPropertyDescriptor (target, key) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">if</span> (key[<span class="number">0</span>] === <span class="string">'_'</span>) &#123;</span></code><br><code class="highlight javascript"><span class="line">      <span class="keyword">return</span>;</span></code><br><code class="highlight javascript"><span class="line">    &#125;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> <span class="built_in">Object</span>.getOwnPropertyDescriptor(target, key);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> target = &#123; <span class="attr">_foo</span>: <span class="string">'bar'</span>, <span class="attr">baz</span>: <span class="string">'tar'</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> proxy = <span class="keyword">new</span> <span class="built_in">Proxy</span>(target, handler);</span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.getOwnPropertyDescriptor(proxy, <span class="string">'wat'</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// undefined</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.getOwnPropertyDescriptor(proxy, <span class="string">'_foo'</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// undefined</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.getOwnPropertyDescriptor(proxy, <span class="string">'baz'</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// &#123;value: 'tar',writable:true,enumerable:true,configurable:true&#125;</span></span></code><br></pre></td></tr></table></figure>

<h2 id="getPrototypeOf"><a href="#getPrototypeOf" class="headerlink" title="getPrototypeOf()"></a>getPrototypeOf()</h2><p><code>getPrototypeOf</code>方法主要用来拦截获取对象原型。具体来说，拦截下面这些操作。</p>
<ol>
<li><code>Object.prototype.__proto</code>__</li>
<li><code>Object.prototype.isPrototypeOf()</code></li>
<li><code>Object.getPrototypeOf()</code></li>
<li><code>Reflect.getPrototypeOf()</code></li>
<li><code>instanceof</code></li>
</ol>
<blockquote>
<p>注意，<code>getPrototypeOf</code>方法的返回值必须是对象或者null，否则报错。另外，如果目标对象不可扩展（non-extensible）， <code>getPrototypeOf</code>方法必须返回目标对象的原型对象。</p>
</blockquote>
<h2 id="isExtensible"><a href="#isExtensible" class="headerlink" title="isExtensible()"></a>isExtensible()</h2><p><code>isExtensible</code>方法拦截<code>Object.isExtensible</code>操作</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> p = <span class="keyword">new</span> <span class="built_in">Proxy</span>(&#123;&#125;, &#123;</span></code><br><code class="highlight javascript"><span class="line">  isExtensible: <span class="function"><span class="keyword">function</span>(<span class="params">target</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">console</span>.log(<span class="string">"called"</span>);</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.isExtensible(p)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// "called"</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意，该方法只能返回布尔值，否则返回值会被自动转为布尔值。这个方法有一个强限制，它的返回值必须与目标对象的<code>isExtensible</code>属性保持一致，否则就会抛出错误。</p>
</blockquote>
<h2 id="ownKeys"><a href="#ownKeys" class="headerlink" title="ownKeys()"></a>ownKeys()</h2><p><code>ownKeys</code>方法用来拦截对象自身属性的读取操作。具体来说，拦截以下操作</p>
<ol>
<li><code>Object.getOwnPropertyNames()</code></li>
<li><code>Object.getOwnPropertySymbols()</code></li>
<li><code>Object.keys()</code></li>
<li><code>for...in</code>循环</li>
</ol>
<p>注意，使用<code>Object.keys</code>方法时，有三类属性会被<code>ownKeys</code>方法自动过滤，不会返回。</p>
<ul>
<li>目标对象上不存在的属性</li>
<li>属性名为 Symbol 值</li>
<li>不可遍历（enumerable）的属性</li>
</ul>
<p>注意:</p>
<ul>
<li><p><code>ownKeys</code>方法返回的数组成员，只能是字符串或 Symbol 值。如果有其他类型的值，或者返回的根本不是数组，就会报错。</p>
</li>
<li><p>如果目标对象自身包含不可配置的属性，则该属性必须被<code>ownKeys</code>方法返回，否则报错。</p>
</li>
<li><p>如果目标对象是不可扩展的（non-extensible），这时<code>ownKeys</code>方法返回的数组之中，必须包含原对象的所有属性，且不能包含多余的属性，否则报错。</p>
</li>
</ul>
<h2 id="preventExtensions"><a href="#preventExtensions" class="headerlink" title="preventExtensions()"></a>preventExtensions()</h2><p><code>setPrototypeOf</code>方法主要用来拦截<code>Object.setPrototypeOf</code>方法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> handler = &#123;</span></code><br><code class="highlight javascript"><span class="line">  setPrototypeOf (target, proto) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'Changing the prototype is forbidden'</span>);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> proto = &#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> target = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> proxy = <span class="keyword">new</span> <span class="built_in">Proxy</span>(target, handler);</span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.setPrototypeOf(proxy, proto);</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// Error: Changing the prototype is forbidden</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">/*上面代码中，只要修改target的原型对象，就会报错。</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意，该方法只能返回布尔值，否则会被自动转为布尔值。另外，如果目标对象不可扩展（non-extensible），<code>setPrototypeOf</code>方法不得改变目标对象的原型。</p>
</blockquote>
<h1 id="Proxy-revocable"><a href="#Proxy-revocable" class="headerlink" title="Proxy.revocable()"></a>Proxy.revocable()</h1><p><code>Proxy.revocable</code>方法返回一个可取消的 Proxy 实例。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> target = &#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> handler = &#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> &#123;proxy, revoke&#125; = <span class="built_in">Proxy</span>.revocable(target, handler);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">proxy.foo = <span class="number">123</span>;</span></code><br><code class="highlight javascript"><span class="line">proxy.foo <span class="comment">// 123</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">revoke();     <span class="comment">//取消Proxy实例</span></span></code><br><code class="highlight javascript"><span class="line">proxy.foo <span class="comment">// TypeError: Revoked</span></span></code><br></pre></td></tr></table></figure>

<p><code>Proxy.revocable</code>的一个使用场景是，目标对象不允许直接访问，必须通过代理访问，一旦访问结束，就收回代理权，不允许再次访问。</p>
<h1 id="this问题"><a href="#this问题" class="headerlink" title="this问题"></a>this问题</h1><p>虽然 Proxy 可以代理针对目标对象的访问，但它不是目标对象的透明代理，即不做任何拦截的情况下，也无法保证与目标对象的行为一致。主要原因就是在 Proxy 代理的情况下，目标对象内部的this关键字会指向 Proxy 代理。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> target = &#123;</span></code><br><code class="highlight javascript"><span class="line">  m: <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span> === proxy);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> handler = &#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> proxy = <span class="keyword">new</span> <span class="built_in">Proxy</span>(target, handler);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">target.m() <span class="comment">// false</span></span></code><br><code class="highlight javascript"><span class="line">proxy.m()  <span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>

<p>将this绑定原始对象解决问题:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> target = <span class="keyword">new</span> <span class="built_in">Date</span>(<span class="string">'2015-01-01'</span>);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> handler = &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">get</span>(target, prop) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">if</span> (prop === <span class="string">'getDate'</span>) &#123;</span></code><br><code class="highlight javascript"><span class="line">      <span class="keyword">return</span> target.getDate.bind(target);</span></code><br><code class="highlight javascript"><span class="line">    &#125;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> <span class="built_in">Reflect</span>.get(target, prop);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> proxy = <span class="keyword">new</span> <span class="built_in">Proxy</span>(target, handler);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">proxy.getDate() <span class="comment">// 1</span></span></code><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>前端开发</category>
        <category>ES6学习</category>
      </categories>
      <tags>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript6--Reflect</title>
    <url>/2019/11/01/ECMAScript6/13%E3%80%81Reflect/</url>
    <content><![CDATA[<h1 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h1><p><code>Reflect</code>对象与<code>Proxy</code>对象一样，也是 ES6 为了操作对象而提供的新 API。`</p>
<p><code>Reflect</code>设计目的是为了优化Object的一些操作方法以及合理的返回Object操作返回的结果，对于一些命令式的Object行为，<code>Reflect</code>对象可以将其变为函数式的行为。</p>
<a id="more"></a>

<h2 id="Reflect的作用"><a href="#Reflect的作用" class="headerlink" title="Reflect的作用"></a>Reflect的作用</h2><ol>
<li><p>将语言内部的方法放到<code>Reflect</code>对象上。【现阶段，某些方法同时在<code>Object</code>和<code>Reflect</code>对象上部署，未来的新方法将只部署在<code>Reflect</code>对象上。】</p>
</li>
<li><p>修改某些<code>Object</code>方法的返回结果。</p>
</li>
<li><p>让<code>Object</code>操作都变成函数行为。例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 老写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="string">'assign'</span> <span class="keyword">in</span> <span class="built_in">Object</span> <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 新写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Reflect</span>.has(<span class="built_in">Object</span>, <span class="string">'assign'</span>) <span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>
</li>
<li><p><code>Reflect</code>对象的方法与<code>Proxy</code>对象的方法一一对应，只要是<code>Proxy</code>对象的方法，就能在<code>Reflect</code>对象上找到对应的方法。</p>
</li>
</ol>
<h1 id="静态方法"><a href="#静态方法" class="headerlink" title="静态方法"></a>静态方法</h1><p><code>Reflect</code>对象共有13个静态方法。</p>
<ul>
<li>Reflect.apply(target, thisArg, args)</li>
<li>Reflect.construct(target, args)</li>
<li>Reflect.get(target, name, receiver)</li>
<li>Reflect.set(target, name, value, receiver)</li>
<li>Reflect.defineProperty(target, name, desc)</li>
<li>Reflect.deleteProperty(target, name)</li>
<li>Reflect.has(target, name)</li>
<li>Reflect.ownKeys(target)</li>
<li>Reflect.isExtensible(target)</li>
<li>Reflect.preventExtensions(target)</li>
<li>Reflect.getOwnPropertyDescriptor(target, name)</li>
<li>Reflect.getPrototypeOf(target)</li>
<li>Reflect.setPrototypeOf(target, prototype)</li>
</ul>
<h2 id="Reflect-get-target-name-receiver"><a href="#Reflect-get-target-name-receiver" class="headerlink" title="Reflect.get(target,name,receiver)"></a>Reflect.get(target,name,receiver)</h2><p><code>Reflect.get</code>方法查找并返回<code>target</code>对象的<code>name</code>属性,如果没有该属性,则返回<code>undefined</code></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> myObject = &#123;</span></code><br><code class="highlight javascript"><span class="line">  foo: <span class="number">1</span>,</span></code><br><code class="highlight javascript"><span class="line">  bar: <span class="number">2</span>,</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">get</span> baz() &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.foo + <span class="keyword">this</span>.bar;</span></code><br><code class="highlight javascript"><span class="line">  &#125;,</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> myReceiverObject = &#123;</span></code><br><code class="highlight javascript"><span class="line">  foo: <span class="number">4</span>,</span></code><br><code class="highlight javascript"><span class="line">  bar: <span class="number">4</span>,</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Reflect</span>.get(myObject, <span class="string">'foo'</span>) <span class="comment">// 1</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Reflect</span>.get(myObject, <span class="string">'bar'</span>) <span class="comment">// 2</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//如果name属性部署了读取函数（getter），则读取函数的this绑定receiver。</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Reflect</span>.get(myObject, <span class="string">'baz'</span>, myReceiverObject) <span class="comment">// 8</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>第一个参数必须是对象,否则会报错</p>
</blockquote>
<h2 id="Reflect-set-target-name-value-receiver"><a href="#Reflect-set-target-name-value-receiver" class="headerlink" title="Reflect.set(target,name,value,receiver)"></a>Reflect.set(target,name,value,receiver)</h2><p><code>Reflect.set</code>方法设置<code>target</code>对象的<code>name</code>属性等于<code>value</code>。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> myObject = &#123;</span></code><br><code class="highlight javascript"><span class="line">  foo: <span class="number">4</span>,</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">set</span> bar(value) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.foo = value;</span></code><br><code class="highlight javascript"><span class="line">  &#125;,</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> myReceiverObject = &#123;</span></code><br><code class="highlight javascript"><span class="line">  foo: <span class="number">0</span>,</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//如果name属性设置了赋值函数，则赋值函数的this绑定receiver。</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Reflect</span>.set(myObject, <span class="string">'bar'</span>, <span class="number">1</span>, myReceiverObject);</span></code><br><code class="highlight javascript"><span class="line">myObject.foo <span class="comment">// 4</span></span></code><br><code class="highlight javascript"><span class="line">myReceiverObject.foo <span class="comment">// 1</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>第一个参数必须是对象,否则会报错</p>
</blockquote>
<p>注意：如果 <code>Proxy</code>对象和 <code>Reflect</code>对象联合使用，前者拦截赋值操作，后者完成赋值的默认行为，而且传入了<code>receiver</code>，那么<code>Reflect.set</code>会触发<code>Proxy.defineProperty</code>拦截。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> p = &#123;</span></code><br><code class="highlight javascript"><span class="line">  a: <span class="string">'a'</span></span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> handler = &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">set</span>(target, key, value, receiver) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">console</span>.log(<span class="string">'set'</span>);</span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">Reflect</span>.set(target, key, value, receiver)</span></code><br><code class="highlight javascript"><span class="line">  &#125;,</span></code><br><code class="highlight javascript"><span class="line">  defineProperty(target, key, attribute) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">console</span>.log(<span class="string">'defineProperty'</span>);</span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">Reflect</span>.defineProperty(target, key, attribute);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> obj = <span class="keyword">new</span> <span class="built_in">Proxy</span>(p, handler);</span></code><br><code class="highlight javascript"><span class="line">obj.a = <span class="string">'A'</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// set</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// defineProperty</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码中，<code>Proxy.set</code>拦截里面使用了<code>Reflect.set</code>，而且传入了<code>receiver</code>，导致触发<code>Proxy.defineProperty</code>拦截。这是因为<code>Proxy.set</code>的<code>receiver</code>参数总是指向当前的 <code>Proxy</code>实例（即上例的<code>obj</code>），而<code>Reflect.set</code>一旦传入<code>receiver</code>，就会将属性赋值到<code>receiver</code>上面（即<code>obj</code>），导致触发<code>defineProperty</code>拦截。如果<code>Reflect.set</code>没有传入<code>receiver</code>，那么就不会触发<code>defineProperty</code>拦截。</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> p = &#123;</span></code><br><code class="highlight javascript"><span class="line">  a: <span class="string">'a'</span></span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> handler = &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">set</span>(target, key, value, receiver) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">console</span>.log(<span class="string">'set'</span>);</span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">Reflect</span>.set(target, key, value)</span></code><br><code class="highlight javascript"><span class="line">  &#125;,</span></code><br><code class="highlight javascript"><span class="line">  defineProperty(target, key, attribute) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">console</span>.log(<span class="string">'defineProperty'</span>);</span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">Reflect</span>.defineProperty(target, key, attribute);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> obj = <span class="keyword">new</span> <span class="built_in">Proxy</span>(p, handler);</span></code><br><code class="highlight javascript"><span class="line">obj.a = <span class="string">'A'</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// set</span></span></code><br></pre></td></tr></table></figure>

<h2 id="Reflect-has-obj-name"><a href="#Reflect-has-obj-name" class="headerlink" title="Reflect.has(obj,name)"></a>Reflect.has(obj,name)</h2><p><code>Reflect.has</code>方法对应<code>name in obj</code>里面的<code>in</code>运算符。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> myObject = &#123;</span></code><br><code class="highlight javascript"><span class="line">  foo: <span class="number">1</span>,</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 旧写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="string">'foo'</span> <span class="keyword">in</span> myObject <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 新写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Reflect</span>.has(myObject, <span class="string">'foo'</span>) <span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>如果<code>Reflect.has()</code>方法的第一个参数不是对象，会报错。</p>
</blockquote>
<h2 id="Reflect-deleteProperty-obj-name"><a href="#Reflect-deleteProperty-obj-name" class="headerlink" title="Reflect.deleteProperty(obj,name)"></a>Reflect.deleteProperty(obj,name)</h2><p><code>Reflect.deleteProperty</code>方法等同于<code>delete obj[name]</code>，用于删除对象的属性。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> myObj = &#123; <span class="attr">foo</span>: <span class="string">'bar'</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 旧写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">delete</span> myObj.foo;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 新写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Reflect</span>.deleteProperty(myObj, <span class="string">'foo'</span>);</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>如果<code>Reflect.deleteProperty()</code>方法的第一个参数不是对象，会报错。</p>
</blockquote>
<h2 id="Reflect-construct-target-args"><a href="#Reflect-construct-target-args" class="headerlink" title="Reflect.construct(target,args)"></a>Reflect.construct(target,args)</h2><p><code>Reflect.construct</code>方法等同于<code>new target(...args)</code>，这提供了一种不使用<code>new</code>，来调用构造函数的方法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Greeting</span>(<span class="params">name</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">this</span>.name = name;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// new 的写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> instance = <span class="keyword">new</span> Greeting(<span class="string">'张三'</span>);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// Reflect.construct 的写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> instance = <span class="built_in">Reflect</span>.construct(Greeting, [<span class="string">'张三'</span>]);</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>如果<code>Reflect.construct()</code>方法的第一个参数不是函数，会报错。</p>
</blockquote>
<h2 id="Reflect-getPrototype-obj"><a href="#Reflect-getPrototype-obj" class="headerlink" title="Reflect.getPrototype(obj)"></a>Reflect.getPrototype(obj)</h2><p><code>Reflect.getPrototypeOf</code>方法用于读取对象的<code>__proto__</code>属性，对应<code>Object.getPrototypeOf(obj)</code>。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> myObj = <span class="keyword">new</span> FancyThing();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 旧写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.getPrototypeOf(myObj) === FancyThing.prototype;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 新写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Reflect</span>.getPrototypeOf(myObj) === FancyThing.prototype;</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>如果参数不是对象，<code>Object.getPrototypeOf</code>会将这个参数转为对象，然后再运行，而<code>Reflect.getPrototypeOf</code>会报错。</p>
</blockquote>
<h2 id="Reflect-setPrototypeOf-obj-newProto"><a href="#Reflect-setPrototypeOf-obj-newProto" class="headerlink" title="Reflect.setPrototypeOf(obj,newProto)"></a>Reflect.setPrototypeOf(obj,newProto)</h2><p><code>Reflect.setPrototypeOf</code>方法用于设置目标对象的原型（prototype），对应<code>Object.setPrototypeOf(obj, newProto)</code>方法。它返回一个布尔值，表示是否设置成功。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> myObj = &#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 旧写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.setPrototypeOf(myObj, <span class="built_in">Array</span>.prototype);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 新写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Reflect</span>.setPrototypeOf(myObj, <span class="built_in">Array</span>.prototype);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">myObj.length <span class="comment">// 0</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<ul>
<li>如果无法设置目标对象的原型,<code>Reflect.setPrototypeOf</code>方法返回<code>false</code>。</li>
<li>如果第一个参数不是对象，<code>Object.setPrototypeOf</code>会返回第一个参数本身，而<code>Reflect.setPrototypeOf</code>会报错。</li>
<li>如果第一个参数是<code>undefined</code>或<code>null</code>，<code>Object.setPrototypeOf</code>和<code>Reflect.setPrototypeOf</code>都会报错。</li>
</ul>
</blockquote>
<h2 id="Reflect-apply-func-thisArg-args"><a href="#Reflect-apply-func-thisArg-args" class="headerlink" title="Reflect.apply(func,thisArg,args)"></a>Reflect.apply(func,thisArg,args)</h2><p><code>Reflect.apply</code>方法等同于<code>Function.prototype.apply.call(func, thisArg, args)</code>，用于绑定<code>this</code>对象后执行给定函数。</p>
<p>一般来说，如果要绑定一个函数的<code>this</code>对象，可以这样写<code>fn.apply(obj, args)</code>，但是如果函数定义了自己的<code>apply</code>方法，就只能写成<code>Function.prototype.apply.call(fn, obj, args)</code>，采用<code>Reflect</code>对象可以简化这种操作。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> ages = [<span class="number">11</span>, <span class="number">33</span>, <span class="number">12</span>, <span class="number">54</span>, <span class="number">18</span>, <span class="number">96</span>];</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 旧写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> youngest = <span class="built_in">Math</span>.min.apply(<span class="built_in">Math</span>, ages);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> oldest = <span class="built_in">Math</span>.max.apply(<span class="built_in">Math</span>, ages);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> type = <span class="built_in">Object</span>.prototype.toString.call(youngest);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 新写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> youngest = <span class="built_in">Reflect</span>.apply(<span class="built_in">Math</span>.min, <span class="built_in">Math</span>, ages);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> oldest = <span class="built_in">Reflect</span>.apply(<span class="built_in">Math</span>.max, <span class="built_in">Math</span>, ages);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> type = <span class="built_in">Reflect</span>.apply(<span class="built_in">Object</span>.prototype.toString, youngest, []);</span></code><br></pre></td></tr></table></figure>

<h2 id="Reflect-defineProperty-target-propertyKey-attributes"><a href="#Reflect-defineProperty-target-propertyKey-attributes" class="headerlink" title="Reflect.defineProperty(target, propertyKey, attributes)"></a>Reflect.defineProperty(target, propertyKey, attributes)</h2><p><code>Reflect.defineProperty</code>方法基本等同于<code>Object.defineProperty</code>，用来为对象定义属性。未来，后者会被逐渐废除，请从现在开始就使用<code>Reflect.defineProperty</code>代替它。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">MyDate</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">/*…*/</span></span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 旧写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.defineProperty(MyDate, <span class="string">'now'</span>, &#123;</span></code><br><code class="highlight javascript"><span class="line">  value: <span class="function"><span class="params">()</span> =&gt;</span> <span class="built_in">Date</span>.now()</span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 新写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Reflect</span>.defineProperty(MyDate, <span class="string">'now'</span>, &#123;</span></code><br><code class="highlight javascript"><span class="line">  value: <span class="function"><span class="params">()</span> =&gt;</span> <span class="built_in">Date</span>.now()</span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>如果<code>Reflect.defineProperty</code>的第一个参数不是对象，就会抛出错误，比如<code>Reflect.defineProperty(1, &#39;foo&#39;)</code>。</p>
</blockquote>
<h2 id="Reflect-getOwnPropertyDescriptor-target-propertyKey"><a href="#Reflect-getOwnPropertyDescriptor-target-propertyKey" class="headerlink" title="Reflect.getOwnPropertyDescriptor(target, propertyKey)"></a>Reflect.getOwnPropertyDescriptor(target, propertyKey)</h2><p><code>Reflect.getOwnPropertyDescriptor</code>基本等同于<code>Object.getOwnPropertyDescriptor</code>，用于得到指定属性的描述对象，将来会替代掉后者。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> myObject = &#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.defineProperty(myObject, <span class="string">'hidden'</span>, &#123;</span></code><br><code class="highlight javascript"><span class="line">  value: <span class="literal">true</span>,</span></code><br><code class="highlight javascript"><span class="line">  enumerable: <span class="literal">false</span>,</span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 旧写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> theDescriptor = <span class="built_in">Object</span>.getOwnPropertyDescriptor(myObject, <span class="string">'hidden'</span>);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 新写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> theDescriptor = <span class="built_in">Reflect</span>.getOwnPropertyDescriptor(myObject, <span class="string">'hidden'</span>);</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>如果第一个参数不是对象，<code>Object.getOwnPropertyDescriptor(1, &#39;foo&#39;)</code>不报错，返回<code>undefined</code>，而<code>Reflect.getOwnPropertyDescriptor(1, &#39;foo&#39;)</code>会抛出错误，表示参数非法。</p>
</blockquote>
<h2 id="Reflect-isExtensible-target"><a href="#Reflect-isExtensible-target" class="headerlink" title="Reflect.isExtensible (target)"></a>Reflect.isExtensible (target)</h2><p><code>Reflect.isExtensible</code>方法对应<code>Object.isExtensible</code>，返回一个布尔值，表示当前对象是否可扩展。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> myObject = &#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 旧写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.isExtensible(myObject) <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 新写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Reflect</span>.isExtensible(myObject) <span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>如果参数不是对象，<code>Object.isExtensible</code>会返回<code>false</code>，因为非对象本来就是不可扩展的，而<code>Reflect.isExtensible</code>会报错。</p>
</blockquote>
<h2 id="Reflect-preventExtensions-target"><a href="#Reflect-preventExtensions-target" class="headerlink" title="Reflect.preventExtensions(target)"></a>Reflect.preventExtensions(target)</h2><p><code>Reflect.preventExtensions</code>对应<code>Object.preventExtensions</code>方法，用于让一个对象变为不可扩展。它返回一个布尔值，表示是否操作成功。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> myObject = &#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 旧写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.preventExtensions(myObject) <span class="comment">// Object &#123;&#125;</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 新写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Reflect</span>.preventExtensions(myObject) <span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>如果参数不是对象，<code>Object.preventExtensions</code>在 ES5 环境报错，在 ES6 环境返回传入的参数，而<code>Reflect.preventExtensions</code>会报错。</p>
</blockquote>
<h2 id="Reflect-ownKeys-target"><a href="#Reflect-ownKeys-target" class="headerlink" title="Reflect.ownKeys (target)"></a>Reflect.ownKeys (target)</h2><p><code>Reflect.ownKeys</code>方法用于返回对象的所有属性，基本等同于<code>Object.getOwnPropertyNames</code>与<code>Object.getOwnPropertySymbols</code>之和。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> myObject = &#123;</span></code><br><code class="highlight javascript"><span class="line">  foo: <span class="number">1</span>,</span></code><br><code class="highlight javascript"><span class="line">  bar: <span class="number">2</span>,</span></code><br><code class="highlight javascript"><span class="line">  [<span class="built_in">Symbol</span>.for(<span class="string">'baz'</span>)]: <span class="number">3</span>,</span></code><br><code class="highlight javascript"><span class="line">  [<span class="built_in">Symbol</span>.for(<span class="string">'bing'</span>)]: <span class="number">4</span>,</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 旧写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.getOwnPropertyNames(myObject)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// ['foo', 'bar']</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.getOwnPropertySymbols(myObject)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//[Symbol(baz), Symbol(bing)]</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 新写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Reflect</span>.ownKeys(myObject)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// ['foo', 'bar', Symbol(baz), Symbol(bing)]</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>如果<code>Reflect.ownKeys()</code>方法的第一个参数不是对象，会报错</p>
</blockquote>
]]></content>
      <categories>
        <category>前端开发</category>
        <category>ES6学习</category>
      </categories>
      <tags>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript6--Set和Map数据结构</title>
    <url>/2019/10/22/ECMAScript6/11%E3%80%81Set%E5%92%8CMap%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/</url>
    <content><![CDATA[<h1 id="Set"><a href="#Set" class="headerlink" title="Set"></a>Set</h1><h2 id="基本用法"><a href="#基本用法" class="headerlink" title="基本用法"></a>基本用法</h2><p><code>Set</code>数据结构类似于数组,但是成员的值都是唯一的，没有重复的值。<br><code>Set</code>本身是一个构造函数,用来生成<code>Set</code>数据结构</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> s = <span class="keyword">new</span> <span class="built_in">Set</span>();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">[<span class="number">2</span>, <span class="number">3</span>, <span class="number">5</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">2</span>, <span class="number">2</span>].forEach(<span class="function"><span class="params">x</span> =&gt;</span> s.add(x));</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> i <span class="keyword">of</span> s) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(i);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 2 3 5 4</span></span></code><br></pre></td></tr></table></figure>

<p>Set函数可以接受一个数组（或者具有 <code>iterable</code> 接口的其他数据结构）作为参数，用来初始化。</p>
<a id="more"></a>

<p><strong>注意:</strong></p>
<ul>
<li>向 <code>Set</code> 加入值的时候，不会发生类型转换,所以 5 和 ‘5’是两个不同的值</li>
<li>Set内部,<code>NaN</code>是等于自身的</li>
<li>两个对象总是不相等的(空对象除外)</li>
</ul>
<h2 id="Set实例的属性和方法"><a href="#Set实例的属性和方法" class="headerlink" title="Set实例的属性和方法"></a>Set实例的属性和方法</h2><h3 id="属性"><a href="#属性" class="headerlink" title="属性"></a>属性</h3><ul>
<li><code>Set.prototype.constructor</code>：构造函数，默认就是Set函数。</li>
<li><code>Set.prototype.size</code>：返回Set实例的成员总数。</li>
</ul>
<h3 id="方法"><a href="#方法" class="headerlink" title="方法"></a>方法</h3><p>分为两大类：操作方法（用于操作数据）和遍历方法（用于遍历成员）</p>
<h4 id="操作方法"><a href="#操作方法" class="headerlink" title="操作方法:"></a>操作方法:</h4><ul>
<li><code>Set.prototype.add(value)</code>：添加某个值，返回 Set 结构本身。</li>
<li><code>Set.prototype.delete(value)</code>：删除某个值，返回一个布尔值，表示删除是否成功。</li>
<li><code>Set.prototype.has(value)</code>：返回一个布尔值，表示该值是否为Set的成员。</li>
<li><code>Set.prototype.clear()</code>：清除所有成员，没有返回值。</li>
</ul>
<h4 id="遍历方法"><a href="#遍历方法" class="headerlink" title="遍历方法:"></a>遍历方法:</h4><ul>
<li><code>Set.prototype.keys()</code>：返回键名的遍历器</li>
<li><code>Set.prototype.values()</code>：返回键值的遍历器</li>
<li><code>Set.prototype.entries()</code>：返回键值对的遍历器</li>
<li><code>Set.prototype.forEach()</code>：使用回调函数遍历每个成员</li>
</ul>
<blockquote>
<p>需要注意的是:Set的遍历顺序就是插入顺序</p>
</blockquote>
<p><strong>keys , values , entries</strong></p>
<p>keys() , values() , entries() 返回的都是遍历器对象<br>由于Set结构没有键名,只有键值(或者说键名和键值是同一个值),所以keys和values方法的行为完全一致</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">//Set 结构的实例默认可遍历，它的默认遍历器生成函数就是它的values方法。</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Set</span>.prototype[<span class="built_in">Symbol</span>.iterator] === <span class="built_in">Set</span>.prototype.values <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//可以省略values方法，直接用for...of循环遍历 Set。</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> <span class="keyword">set</span> = new Set(['red', 'green', 'blue']);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">for (let x of <span class="keyword">set</span>) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(x);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// red</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// green</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// blue</span></span></code><br></pre></td></tr></table></figure>

<p><strong>foreach</strong></p>
<p><code>foreach</code>接收两个参数:</p>
<ul>
<li>一: 函数, 函数接收三个参数 键值(values) , 键名(keys) , 集合本身 </li>
<li>二:表示绑定处理函数内部的this对象</li>
</ul>
<h4 id="遍历的应用"><a href="#遍历的应用" class="headerlink" title="遍历的应用"></a>遍历的应用</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">//扩展运算符（...）内部使用for...of循环，可以用于 Set 结构。</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> <span class="keyword">set</span> = new Set(['red', 'green', 'blue']);</span></code><br><code class="highlight javascript"><span class="line">let arr = [...<span class="keyword">set</span>];</span></code><br><code class="highlight javascript"><span class="line">// ['red', 'green', 'blue']</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">//数组的map和filter方法也可以间接用于 Set </span></code><br><code class="highlight javascript"><span class="line">let <span class="keyword">set</span> = new Set([1, 2, 3]);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">set</span> = new Set([...<span class="keyword">set</span>].map(x =&gt; x * 2));</span></code><br><code class="highlight javascript"><span class="line">// 返回Set结构：&#123;<span class="number">2</span>, <span class="number">4</span>, <span class="number">6</span>&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> <span class="keyword">set</span> = new Set([1, 2, 3, 4, 5]);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">set</span> = new Set([...<span class="keyword">set</span>].filter(x =&gt; (x % 2) == 0));</span></code><br><code class="highlight javascript"><span class="line">// 返回Set结构：&#123;<span class="number">2</span>, <span class="number">4</span>&#125;</span></code><br></pre></td></tr></table></figure>

<p>在遍历操作中，同步改变原来的 <code>Set</code> 结构，有两种方法。一种是利用原 <code>Set</code> 结构映射出一个新的结构，然后赋值给原来的<code>Set</code> 结构；另一种是利用<code>Array.from</code>方法</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">/ 方法一</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> <span class="keyword">set</span> = new Set([1, 2, 3]);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">set</span> = new Set([...<span class="keyword">set</span>].map(val =&gt; val * 2));</span></code><br><code class="highlight javascript"><span class="line">// <span class="keyword">set</span>的值是2, 4, 6</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">// 方法二</span></code><br><code class="highlight javascript"><span class="line">let <span class="keyword">set</span> = new Set([1, 2, 3]);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">set</span> = new Set(Array.from(<span class="keyword">set</span>, val =&gt; val * 2));</span></code><br><code class="highlight javascript"><span class="line">// <span class="keyword">set</span>的值是2, 4, 6</span></code><br></pre></td></tr></table></figure>

<h1 id="WeakSet"><a href="#WeakSet" class="headerlink" title="WeakSet"></a>WeakSet</h1><p><code>WeakSet</code>与<code>Set</code>类似,也是不重复的值的集合。但是，它与 Set 有两个区别。<br>首先，<code>WeakSet</code> 的成员只能是对象，而不能是其他类型的值。<br>向<code>WeakSet</code>中放入对象以外的值会报错:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> ws = <span class="keyword">new</span> <span class="built_in">WeakSet</span>();</span></code><br><code class="highlight javascript"><span class="line">ws.add(<span class="number">1</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// TypeError: Invalid value used in weak set</span></span></code><br><code class="highlight javascript"><span class="line">ws.add(<span class="built_in">Symbol</span>())</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// TypeError: invalid value used in weak set</span></span></code><br></pre></td></tr></table></figure>

<p>其次，<code>WeakSet</code> 中的对象都是弱引用，即垃圾回收机制不考虑 <code>WeakSet</code>对该对象的引用，也就是说，如果其他对象都不再引用该对象，那么垃圾回收机制会自动回收该对象所占用的内存，不考虑该对象还存在于 <code>WeakSet</code>之中。</p>
<p>这是因为垃圾回收机制依赖引用计数，如果一个值的引用次数不为0,垃圾回收机制就不会释放这块内存。结束使用该值之后，有时会忘记取消引用，导致内存无法释放，进而可能会引发内存泄漏。<code>WeakSet</code>里面的引用，都不计入垃圾回收机制，所以就不存在这个问题。因此，<code>WeakSet</code>适合临时存放一组对象，以及存放跟对象绑定的信息。只要这些对象在外部消失，它在<code>WeakSet</code>里面的引用就会自动消失。</p>
<p>由于上面这个特点，<code>WeakSet</code>的成员是不适合引用的，因为它会随时消失。另外，由于 <code>WeakSet</code>内部有多少个成员，取决于垃圾回收机制有没有运行，运行前后很可能成员个数是不一样的，而垃圾回收机制何时运行是不可预测的，因此 ES6 规定<code>WeakSet</code>不可遍历。</p>
<blockquote>
<p>这些特点同样适用于 <code>WeakMap</code> 结构。</p>
</blockquote>
<h2 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h2><p><code>WeakSet</code>是一个构造函数,可以使用new命令,创建<code>WeakSet</code>数据结构</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> ws = <span class="keyword">new</span> <span class="built_in">WeakSet</span>();</span></code><br></pre></td></tr></table></figure>

<p>作为构造函数，<code>WeakSet</code> 可以接受一个数组或类似数组的对象作为参数。（实际上，任何具有 <code>Iterable</code> 接口的对象，都可以作为 <code>WeakSet</code> 的参数。）该数组的所有成员，都会自动成为 <code>WeakSet</code>实例对象的成员。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> a = [[<span class="number">1</span>, <span class="number">2</span>], [<span class="number">3</span>, <span class="number">4</span>]];</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> ws = <span class="keyword">new</span> <span class="built_in">WeakSet</span>(a);</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// WeakSet &#123;[1, 2], [3, 4]&#125;</span></span></code><br></pre></td></tr></table></figure>

<p>作为构造函数，<code>WeakSet</code> 可以接受一个数组或类似数组的对象(成员也要是对象)作为参数(实际上，任何具有 <code>Iterable</code>接口的对象，都可以作为 <code>WeakSet</code> 的参数。）该数组的所有成员，都会自动成为 <code>WeakSet</code> 实例对象的成员。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> a = [[<span class="number">1</span>, <span class="number">2</span>], [<span class="number">3</span>, <span class="number">4</span>]];</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> ws = <span class="keyword">new</span> <span class="built_in">WeakSet</span>(a);</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// WeakSet &#123;[1, 2], [3, 4]&#125;</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意: 数组或类似数组的对象参数中的成员也要是对象</p>
</blockquote>
<h2 id="方法-1"><a href="#方法-1" class="headerlink" title="方法"></a>方法</h2><ul>
<li><code>WeakSet.prototype.add(value)</code>：向 <code>WeakSet</code>实例添加一个新成员。</li>
<li><code>WeakSet.prototype.delete(value)</code>：清除 <code>WeakSet</code>实例的指定成员。</li>
<li><code>WeakSet.prototype.has(value)</code>：返回一个布尔值，表示某个值是否在 </li>
</ul>
<h1 id="Map"><a href="#Map" class="headerlink" title="Map"></a>Map</h1><p>JavaScript 的对象（Object），本质上是键值对的集合（Hash 结构），但是传统上只能用字符串当作键。这给它的使用带来了很大的限制。</p>
<p>Map数据结构。它类似于对象，也是键值对的集合，但是“键”的范围不限于字符串，各种类型的值（包括对象）都可以当作键。也就是说，Object结构提供了“字符串—值”的对应，Map 结构提供了“值—值”的对应，是一种更完善的 Hash 结构实现。如果你需要“键值对”的数据结构，Map 比 Object 更合适。</p>
<p>Map 也可以接受一个数组作为参数。该数组的成员是一个个表示键值对的数组。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> map = <span class="keyword">new</span> <span class="built_in">Map</span>([</span></code><br><code class="highlight javascript"><span class="line">  [<span class="string">'name'</span>, <span class="string">'张三'</span>],</span></code><br><code class="highlight javascript"><span class="line">  [<span class="string">'title'</span>, <span class="string">'Author'</span>]</span></code><br><code class="highlight javascript"><span class="line">]);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">map.size <span class="comment">// 2</span></span></code><br><code class="highlight javascript"><span class="line">map.has(<span class="string">'name'</span>) <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line">map.get(<span class="string">'name'</span>) <span class="comment">// "张三"</span></span></code><br><code class="highlight javascript"><span class="line">map.has(<span class="string">'title'</span>) <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line">map.get(<span class="string">'title'</span>) <span class="comment">// "Author"</span></span></code><br></pre></td></tr></table></figure>

<p>Map构造函数接受数组作为参数，实际上执行的是下面的算法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> items = [</span></code><br><code class="highlight javascript"><span class="line">  [<span class="string">'name'</span>, <span class="string">'张三'</span>],</span></code><br><code class="highlight javascript"><span class="line">  [<span class="string">'title'</span>, <span class="string">'Author'</span>]</span></code><br><code class="highlight javascript"><span class="line">];</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> map = <span class="keyword">new</span> <span class="built_in">Map</span>();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">items.forEach(</span></code><br><code class="highlight javascript"><span class="line">  ([key, value]) =&gt; map.set(key, value)</span></code><br><code class="highlight javascript"><span class="line">);</span></code><br></pre></td></tr></table></figure>

<p>事实上，不仅仅是数组，任何具有 Iterator 接口、且每个成员都是一个双元素的数组的数据结构（详见《Iterator》一章）都可以当作Map构造函数的参数。这就是说，Set和Map都可以用来生成新的Map.</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> <span class="keyword">set</span> = new Set([</span></code><br><code class="highlight javascript"><span class="line">  ['foo', 1],</span></code><br><code class="highlight javascript"><span class="line">  ['bar', 2]</span></code><br><code class="highlight javascript"><span class="line">]);</span></code><br><code class="highlight javascript"><span class="line">const m1 = new Map(<span class="keyword">set</span>);</span></code><br><code class="highlight javascript"><span class="line">m1.<span class="keyword">get</span>('foo') // 1</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">const m2 = new Map([['baz', 3]]);</span></code><br><code class="highlight javascript"><span class="line">const m3 = new Map(m2);</span></code><br><code class="highlight javascript"><span class="line">m3.<span class="keyword">get</span>('baz') // 3</span></code><br></pre></td></tr></table></figure>

<p><strong>注意:</strong></p>
<ul>
<li><p>如果对同一个键多次赋值，后面的值将覆盖前面的值。</p>
</li>
<li><p>如果读取一个未知的键，则返回<code>undefined</code></p>
</li>
<li><p>只有对同一个对象的引用，<code>Map</code> 结构才将其视为同一个键。<br><code>Map</code> 的键实际上是跟内存地址绑定的，只要内存地址不一样，就视为两个键。比如连个数组的值虽然相同,但是他们的内存地址不同,就是不同的键<br>如果 <code>Map</code>的键是一个简单类型的值（数字、字符串、布尔值），则只要两个值严格相等，<code>Map</code> 将其视为一个键<code>undefined</code>和<code>null</code>是两个不同的键</p>
<p>虽然<code>NaN</code>不严格相等于自身，但 Map 将其视为同一个键。</p>
</li>
</ul>
<h2 id="实例的属性和操作方法"><a href="#实例的属性和操作方法" class="headerlink" title="实例的属性和操作方法"></a>实例的属性和操作方法</h2><ol>
<li><p>size</p>
<p>返回 Map 结构的成员总数</p>
</li>
<li><p>Map.prototype.set(key, value)</p>
<p><code>set</code>方法设置键名<code>key</code>对应的键值为<code>value</code>，然后返回整个<code>Map</code> 结构。如果<code>key</code>已经有值，则键值会被更新，否则就新生成该键。</p>
</li>
<li><p>Map.prototype.get(key)</p>
<p><code>get</code>方法读取<code>key</code>对应的键值，如果找不到<code>key</code>，返回<code>undefined</code>。</p>
</li>
<li><p>Map.prototype.has(key)</p>
<p><code>has</code>方法返回一个布尔值，表示某个键是否在当前 Map 对象之中</p>
</li>
<li><p>Map.prototype.delete(key)<br><code>delete</code>方法删除某个键，返回true。如果删除失败，返回false。</p>
</li>
<li><p>Map.prototype.clear()</p>
<p><code>clear</code>方法清除所有成员，没有返回值。</p>
</li>
</ol>
<h2 id="遍历方法-1"><a href="#遍历方法-1" class="headerlink" title="遍历方法"></a>遍历方法</h2><p>Map 结构原生提供三个遍历器生成函数和一个遍历方法。</p>
<ul>
<li>​    <code>Map.prototype.keys()</code>：返回键名的遍历器。</li>
<li>​    <code>Map.prototype.values()</code>：返回键值的遍历器。</li>
<li>​    <code>Map.prototype.entries()</code>：返回所有成员的遍历器。</li>
<li>​    <code>Map.prototype.forEach()</code>：遍历 Map 的所有成员。</li>
</ul>
<blockquote>
<p>注意:  Map 的遍历顺序就是插入顺序</p>
</blockquote>
<p><code>foreach</code>接收两个参数:</p>
<ul>
<li>一: 函数, 函数接收三个参数 键值(values) , 键名(keys) , 集合本身 (map)</li>
<li>二:表示绑定处理函数内部的this对象</li>
</ul>
<h2 id="与其他数据结构的互相转换"><a href="#与其他数据结构的互相转换" class="headerlink" title="与其他数据结构的互相转换"></a>与其他数据结构的互相转换</h2><h3 id="Map转为数组"><a href="#Map转为数组" class="headerlink" title="Map转为数组"></a>Map转为数组</h3><p>Map 转为数组最方便的方法，就是使用扩展运算符( … )</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> myMap = <span class="keyword">new</span> <span class="built_in">Map</span>()</span></code><br><code class="highlight javascript"><span class="line">  .set(<span class="literal">true</span>, <span class="number">7</span>)</span></code><br><code class="highlight javascript"><span class="line">  .set(&#123;<span class="attr">foo</span>: <span class="number">3</span>&#125;, [<span class="string">'abc'</span>]);</span></code><br><code class="highlight javascript"><span class="line">[...myMap]</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// [ [ true, 7 ], [ &#123; foo: 3 &#125;, [ 'abc' ] ] ]</span></span></code><br></pre></td></tr></table></figure>

<h3 id="数组转为Map"><a href="#数组转为Map" class="headerlink" title="数组转为Map"></a>数组转为Map</h3><p>将数组传入 Map 构造函数，就可以转为 Map。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">new</span> <span class="built_in">Map</span>([</span></code><br><code class="highlight javascript"><span class="line">  [<span class="literal">true</span>, <span class="number">7</span>],</span></code><br><code class="highlight javascript"><span class="line">  [&#123;<span class="attr">foo</span>: <span class="number">3</span>&#125;, [<span class="string">'abc'</span>]]</span></code><br><code class="highlight javascript"><span class="line">])</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// Map &#123;</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//   true =&gt; 7,</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//   Object &#123;foo: 3&#125; =&gt; ['abc']</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// &#125;</span></span></code><br></pre></td></tr></table></figure>

<h3 id="Map转为对象"><a href="#Map转为对象" class="headerlink" title="Map转为对象"></a>Map转为对象</h3><p>如果所有 Map 的键都是字符串，它可以无损地转为对象。</p>
<p>如果有非字符串的键名，那么这个键名会被转成字符串，再作为对象的键名。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">strMapToObj</span>(<span class="params">strMap</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">let</span> obj = <span class="built_in">Object</span>.create(<span class="literal">null</span>);</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> [k,v] <span class="keyword">of</span> strMap) &#123;</span></code><br><code class="highlight javascript"><span class="line">    obj[k] = v;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> obj;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> myMap = <span class="keyword">new</span> <span class="built_in">Map</span>()</span></code><br><code class="highlight javascript"><span class="line">  .set(<span class="string">'yes'</span>, <span class="literal">true</span>)</span></code><br><code class="highlight javascript"><span class="line">  .set(<span class="string">'no'</span>, <span class="literal">false</span>);</span></code><br><code class="highlight javascript"><span class="line">strMapToObj(myMap)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// &#123; yes: true, no: false &#125;</span></span></code><br></pre></td></tr></table></figure>

<h3 id="对象转为Map"><a href="#对象转为Map" class="headerlink" title="对象转为Map"></a>对象转为Map</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">objToStrMap</span>(<span class="params">obj</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">let</span> strMap = <span class="keyword">new</span> <span class="built_in">Map</span>();</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> k <span class="keyword">of</span> <span class="built_in">Object</span>.keys(obj)) &#123;</span></code><br><code class="highlight javascript"><span class="line">    strMap.set(k, obj[k]);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> strMap;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">objToStrMap(&#123;<span class="attr">yes</span>: <span class="literal">true</span>, <span class="attr">no</span>: <span class="literal">false</span>&#125;)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// Map &#123;"yes" =&gt; true, "no" =&gt; false&#125;</span></span></code><br></pre></td></tr></table></figure>

<h3 id="Map转为JSON"><a href="#Map转为JSON" class="headerlink" title="Map转为JSON"></a>Map转为JSON</h3><p>Map 转为 JSON 要区分两种情况。一种情况是，Map 的键名都是字符串，这时可以选择转为对象 JSON。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">strMapToJson</span>(<span class="params">strMap</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="built_in">JSON</span>.stringify(strMapToObj(strMap));</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> myMap = <span class="keyword">new</span> <span class="built_in">Map</span>().set(<span class="string">'yes'</span>, <span class="literal">true</span>).set(<span class="string">'no'</span>, <span class="literal">false</span>);</span></code><br><code class="highlight javascript"><span class="line">strMapToJson(myMap)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// '&#123;"yes":true,"no":false&#125;'</span></span></code><br></pre></td></tr></table></figure>

<p>另一种情况是，Map 的键名有非字符串，这时可以选择转为数组 JSON。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">mapToArrayJson</span>(<span class="params">map</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="built_in">JSON</span>.stringify([...map]);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> myMap = <span class="keyword">new</span> <span class="built_in">Map</span>().set(<span class="literal">true</span>, <span class="number">7</span>).set(&#123;<span class="attr">foo</span>: <span class="number">3</span>&#125;, [<span class="string">'abc'</span>]);</span></code><br><code class="highlight javascript"><span class="line">mapToArrayJson(myMap)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// '[[true,7],[&#123;"foo":3&#125;,["abc"]]]'</span></span></code><br></pre></td></tr></table></figure>

<h3 id="JSON转为Map"><a href="#JSON转为Map" class="headerlink" title="JSON转为Map"></a>JSON转为Map</h3><p>JSON 转为 Map，正常情况下，所有键名都是字符串。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">jsonToStrMap</span>(<span class="params">jsonStr</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> objToStrMap(<span class="built_in">JSON</span>.parse(jsonStr));</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">jsonToStrMap(<span class="string">'&#123;"yes": true, "no": false&#125;'</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// Map &#123;'yes' =&gt; true, 'no' =&gt; false&#125;</span></span></code><br></pre></td></tr></table></figure>

<p>但是，有一种特殊情况，整个 JSON 就是一个数组，且每个数组成员本身，又是一个有两个成员的数组。这时，它可以一一对应地转为 Map。这往往是 Map 转为数组 JSON 的逆操作。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">jsonToMap</span>(<span class="params">jsonStr</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Map</span>(<span class="built_in">JSON</span>.parse(jsonStr));</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">jsonToMap(<span class="string">'[[true,7],[&#123;"foo":3&#125;,["abc"]]]'</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// Map &#123;true =&gt; 7, Object &#123;foo: 3&#125; =&gt; ['abc']&#125;</span></span></code><br></pre></td></tr></table></figure>

<h1 id="WeakWap"><a href="#WeakWap" class="headerlink" title="WeakWap"></a>WeakWap</h1><p><code>WeakWap</code>结构与<code>Wap</code>结构类似,也是用于生成键值对的集合。</p>
<p><code>WeakWap与Wap</code>的区别:</p>
<ul>
<li><code>WeakMap</code>只接受对象作为键名（<code>null</code>除外），不接受其他类型的值作为键名。</li>
<li><code>WeakMap</code>的键名所指向的对象，不计入垃圾回收机制。</li>
</ul>
<p>总之，<code>WeakMap</code>的专用场合就是，它的键所对应的对象，可能会在将来消失。<code>WeakMap</code>结构有助于防止内存泄漏。<br>注意，<code>WeakMap</code> 弱引用的只是键名，而不是键值。键值依然是正常引用</p>
<h2 id="WeakMap语法"><a href="#WeakMap语法" class="headerlink" title="WeakMap语法"></a>WeakMap语法</h2><p><code>WeakMap</code>没有遍历操作和size属性,只有以下四个方法可用:</p>
<ul>
<li><code>get()</code></li>
<li><code>set()</code></li>
<li><code>has()</code></li>
<li><code>delete()</code></li>
</ul>
<h2 id="WeakMap的用途"><a href="#WeakMap的用途" class="headerlink" title="WeakMap的用途"></a>WeakMap的用途</h2><p><code>WeakMap</code> 应用的典型场合就是 DOM 节点作为键名</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> myElement = <span class="built_in">document</span>.getElementById(<span class="string">'logo'</span>);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> myWeakmap = <span class="keyword">new</span> <span class="built_in">WeakMap</span>();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">myWeakmap.set(myElement, &#123;<span class="attr">timesClicked</span>: <span class="number">0</span>&#125;);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">myElement.addEventListener(<span class="string">'click'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">let</span> logoData = myWeakmap.get(myElement);</span></code><br><code class="highlight javascript"><span class="line">  logoData.timesClicked++;</span></code><br><code class="highlight javascript"><span class="line">&#125;, <span class="literal">false</span>);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">/*myElement是一个 DOM 节点，每当发生click事件，就更新一下状态。</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">我们将这个状态作为键值放在 WeakMap 里，对应的键名就是myElement。</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">一旦这个 DOM 节点删除，该状态就会自动消失，不存在内存泄漏风险。</span></span></code><br></pre></td></tr></table></figure>

<p><code>WeakMap</code>的另一个用处是部署私有属性。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> _counter = <span class="keyword">new</span> <span class="built_in">WeakMap</span>();</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> _action = <span class="keyword">new</span> <span class="built_in">WeakMap</span>(); </span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Countdown</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">constructor</span>(counter, action) &#123;</span></code><br><code class="highlight javascript"><span class="line">    _counter.set(<span class="keyword">this</span>, counter);</span></code><br><code class="highlight javascript"><span class="line">    _action.set(<span class="keyword">this</span>, action);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">  dec() &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">let</span> counter = _counter.get(<span class="keyword">this</span>);</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">if</span> (counter &lt; <span class="number">1</span>) <span class="keyword">return</span>;</span></code><br><code class="highlight javascript"><span class="line">    counter--;</span></code><br><code class="highlight javascript"><span class="line">    _counter.set(<span class="keyword">this</span>, counter);</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">if</span> (counter === <span class="number">0</span>) &#123;</span></code><br><code class="highlight javascript"><span class="line">      _action.get(<span class="keyword">this</span>)();</span></code><br><code class="highlight javascript"><span class="line">    &#125;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> c = <span class="keyword">new</span> Countdown(<span class="number">2</span>, () =&gt; <span class="built_in">console</span>.log(<span class="string">'DONE'</span>));</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">c.dec()</span></code><br><code class="highlight javascript"><span class="line">c.dec()</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// DONE</span></span></code><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>前端开发</category>
        <category>ES6学习</category>
      </categories>
      <tags>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript6--Symbol</title>
    <url>/2019/10/21/ECMAScript6/10%E3%80%81Symbol/</url>
    <content><![CDATA[<h1 id="概述"><a href="#概述" class="headerlink" title="概述"></a><strong>概述</strong></h1><p><code>ES5</code>的对象属性名都是字符串，这容易造成属性名的冲突。比如，你使用了一个他人提供的对象，但又想为这个对象添加新的方法（mixin 模式），新方法的名字就有可能与现有方法产生冲突。如果有一种机制，保证每个属性的名字都是独一无二的就好了，这样就从根本上防止属性名的冲突。</p>
<a id="more"></a>

<p><code>Symbol</code>表示独一无二的值</p>
<p><code>Symbol</code>值通过Symbol函数生成。这就是说，对象的属性名现在可以有两种类型，一种是原来就有的字符串，另一种就是新增的<code>Symbol</code> 类型。凡是属性名属于 <code>Symbol</code>类型，就都是独一无二的，可以保证不会与其他属性名产生冲突。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> s = <span class="built_in">Symbol</span>();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">typeof</span> s</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// "symbol"</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意：Symbol函数前不能使用new命令，否则会报错。这是因为生成的Symbol是一个原始类型的值，不是对象，也就是说，由于Symbol值不是对象，所以不能添加属性。基本上，它是一种类似于字符串的原始数据类型。</p>
</blockquote>
<p>Symbol函数可以接受一个字符串作为参数，表示对Symbol实例的描述，主要是为了在控制台显示，或者转为字符串时，比较容易区分。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> s1 = <span class="built_in">Symbol</span>(<span class="string">'foo'</span>);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> s2 = <span class="built_in">Symbol</span>(<span class="string">'bar'</span>);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">s1 <span class="comment">// Symbol(foo)</span></span></code><br><code class="highlight javascript"><span class="line">s2 <span class="comment">// Symbol(bar)</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">s1.toString() <span class="comment">// "Symbol(foo)"</span></span></code><br><code class="highlight javascript"><span class="line">s2.toString() <span class="comment">// "Symbol(bar)"</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意：</p>
<ul>
<li>Symbol函数的参数只是表示对当前Symbol值得描述，因此相同参数的Symbol函数返回的返回值是不相等的</li>
<li>Symbol值不能与其它类型的值进行运算，会报错</li>
<li>Symbol值可以显示转为字符串，也可以转为布尔值</li>
</ul>
</blockquote>
<h1 id="Symbol-prototype-description"><a href="#Symbol-prototype-description" class="headerlink" title="Symbol.prototype.description"></a>Symbol.prototype.description</h1><p>通过该属性可以获取到<code>Symbol</code>的描述</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> sym = <span class="built_in">Symbol</span>(<span class="string">'foo'</span>);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">sym.description <span class="comment">// "foo"</span></span></code><br></pre></td></tr></table></figure>

<h1 id="作为属性名的Symbol"><a href="#作为属性名的Symbol" class="headerlink" title="作为属性名的Symbol"></a>作为属性名的Symbol</h1><p><code>Symbol</code>作为属性名的用法:</p>
<ul>
<li>在对象的外部，通过方括号进行赋值，切记不能使用 . 符号赋值【因为点运算符后面总是字符串，所以不会读取<code>Symbol</code>实例作为标识所指代的那个值】</li>
<li>通过<code>object.defineProperty</code>赋值</li>
<li>在对象内部，<code>Symbol</code>值定义属性时，<code>Symbol</code>值必须放在方括号之中</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> mySymbol = <span class="built_in">Symbol</span>();</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 第一种写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> a = &#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line">a[mySymbol] = <span class="string">'Hello!'</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 第二种写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> a = &#123;</span></code><br><code class="highlight javascript"><span class="line">  [mySymbol]: <span class="string">'Hello!'</span></span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 第三种写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> a = &#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.defineProperty(a, mySymbol, &#123; <span class="attr">value</span>: <span class="string">'Hello!'</span> &#125;);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 以上写法都得到同样结果</span></span></code><br><code class="highlight javascript"><span class="line">a[mySymbol] <span class="comment">// "Hello!"</span></span></code><br></pre></td></tr></table></figure>

<h1 id="魔术字符串"><a href="#魔术字符串" class="headerlink" title="魔术字符串"></a>魔术字符串</h1><p>魔术字符串指的是，在代码之中多次出现、与代码形成强耦合的某一个具体的字符串或者数值。风格良好的代码，应该尽量消除魔术字符串，改由含义清晰的变量代替。</p>
<h1 id="属性名的遍历"><a href="#属性名的遍历" class="headerlink" title="属性名的遍历"></a>属性名的遍历</h1><p><code>Symbol</code>作为属性名，该属性不会出现在<code>for...in</code>、<code>for...of</code>循环中，也不会被<code>Object.keys()</code>、<code>Object.getOwnPropertyNames()</code>、<code>JSON.stringify()</code>返回。但是，它也不是私有属性，有一个<code>Object.getOwnPropertySymbols</code>方法，可以获取指定对象的所有<code>Symbol</code>属性名。</p>
<p><code>Object.getOwnPropertySymbols</code>方法返回一个数组，成员是当前对象的所有用作属性名的<code>Symbol</code>值。</p>
<p><code>Reflect.ownKeys</code>方法可以返回所有类型的键名，包括常规键名和<code>Symbol</code>键名。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> obj = &#123;</span></code><br><code class="highlight javascript"><span class="line">  [<span class="built_in">Symbol</span>(<span class="string">'my_key'</span>)]: <span class="number">1</span>,</span></code><br><code class="highlight javascript"><span class="line">  enum: <span class="number">2</span>,</span></code><br><code class="highlight javascript"><span class="line">  nonEnum: <span class="number">3</span></span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.getOwnPropertySymbols(obj)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// [Symbol(my_key)]</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Reflect</span>.ownKeys(obj)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//  ["enum", "nonEnum", Symbol(my_key)]</span></span></code><br></pre></td></tr></table></figure>

<h1 id="Symbol-for-，Symbol-keyFor"><a href="#Symbol-for-，Symbol-keyFor" class="headerlink" title="Symbol.for()，Symbol.keyFor()"></a>Symbol.for()，Symbol.keyFor()</h1><p> 有时，我们希望重新使用同一个<code>Symbol</code>值，<code>Symbol.for</code>方法可以做到这一点。它接受一个字符串作为参数，然后所有有没有以该参数作为名称的Symbol值。如果有，就返回这个<code>Symbol</code>值，否则就新建并返回一个以该字符串为名称的<code>Symbol</code>值。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> s1 = <span class="built_in">Symbol</span>.for(<span class="string">'foo'</span>);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> s2 = <span class="built_in">Symbol</span>.for(<span class="string">'foo'</span>);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">s1 === s2 <span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意：Symbol.for()与Symbol()这两种写法。都会生成新的Symbol。它们的区别就是，前者会被登记在全局环境中供搜索，后者不会。</p>
</blockquote>
<p><code>Symbol.keyFor</code>方法返回一个已登记的Symbol类型值的<code>key</code>。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> s1 = <span class="built_in">Symbol</span>.for(<span class="string">"foo"</span>);</span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Symbol</span>.keyFor(s1) <span class="comment">// "foo"</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> s2 = <span class="built_in">Symbol</span>(<span class="string">"foo"</span>);</span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Symbol</span>.keyFor(s2) <span class="comment">// undefined</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意：Symbol.for为Symbol值登记的名字，是全局环境的，可以在不同的iframe或service work中取到同一个值</p>
</blockquote>
<h1 id="内置的Symbol值"><a href="#内置的Symbol值" class="headerlink" title="内置的Symbol值"></a>内置的Symbol值</h1><p>除了定义自己使用的 <code>Symbol</code>值以外，ES6 还提供了 11 个内置的 <code>Symbol</code>值，指向语言内部使用的方法。</p>
<h2 id="Symbol-hasInstance"><a href="#Symbol-hasInstance" class="headerlink" title="Symbol.hasInstance"></a>Symbol.hasInstance</h2><p>对象的<code>Symbol.hasInstance</code>属性,指向一个内部方法。当其他对象使用<code>instanceof</code>运算符，判断是否为该对象的实例时，会调用这个方法。比如，foo instanceof Foo在语言内部，实际调用的是<code>Foo[Symbol.hasInstance](foo)</code></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyClass</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  [<span class="built_in">Symbol</span>.hasInstance](foo) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> foo <span class="keyword">instanceof</span> <span class="built_in">Array</span>;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>] <span class="keyword">instanceof</span> <span class="keyword">new</span> MyClass() <span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码中，MyClass是一个类，new MyClass()会返回一个实例。该实例的Symbol.hasInstance方法，会在进行instanceof运算时自动调用，判断左侧的运算子是否为Array的实例。</p>
</blockquote>
<h2 id="Symbol-isConcatSpreadable"><a href="#Symbol-isConcatSpreadable" class="headerlink" title="Symbol.isConcatSpreadable"></a>Symbol.isConcatSpreadable</h2><p>对象<code>Symbol.isConcatSpreadable</code>属性等于一个布尔值表示该对象用于<code>Array.prototype.concat()</code>时,是否可以展开。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> arr1 = [<span class="string">'c'</span>, <span class="string">'d'</span>];</span></code><br><code class="highlight javascript"><span class="line">[<span class="string">'a'</span>, <span class="string">'b'</span>].concat(arr1, <span class="string">'e'</span>) <span class="comment">// ['a', 'b', 'c', 'd', 'e']</span></span></code><br><code class="highlight javascript"><span class="line">arr1[<span class="built_in">Symbol</span>.isConcatSpreadable] <span class="comment">// undefined</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> arr2 = [<span class="string">'c'</span>, <span class="string">'d'</span>];</span></code><br><code class="highlight javascript"><span class="line">arr2[<span class="built_in">Symbol</span>.isConcatSpreadable] = <span class="literal">false</span>;</span></code><br><code class="highlight javascript"><span class="line">[<span class="string">'a'</span>, <span class="string">'b'</span>].concat(arr2, <span class="string">'e'</span>) <span class="comment">// ['a', 'b', ['c','d'], 'e']</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码说明，数组的默认行为是可以展开，Symbol.isConcatSpreadable默认等于undefined。该属性等于true时，也有展开效果</p>
</blockquote>
<p>类似数组的对象正好相反，默认不展开。它的<code>Symbol.isConcatSpreadable</code>属性设为<code>true</code>，才可以展开。</p>
<h2 id="Symbol-species"><a href="#Symbol-species" class="headerlink" title="Symbol.species"></a>Symbol.species</h2><p>对象的<code>Symbol.species</code>属性，指向一个构造函数。创造衍生对象时，会使用该属性。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyArray</span> <span class="keyword">extends</span> <span class="title">Array</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> a = <span class="keyword">new</span> MyArray(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> b = a.map(<span class="function"><span class="params">x</span> =&gt;</span> x);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> c = a.filter(<span class="function"><span class="params">x</span> =&gt;</span> x &gt; <span class="number">1</span>);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">b <span class="keyword">instanceof</span> MyArray <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line">c <span class="keyword">instanceof</span> MyArray <span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码中，子类MyArray继承了父类Array，a时MyArray的实例，b和c是a的衍生对象。虽然b和c都是调用数组方法生成的，但是实际上它们是MyArray的实例，而不是数组（Array）的实例</p>
</blockquote>
<p><code>Symbol.species</code>属性就是为了解决这个问题而提供的。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyArray</span> <span class="keyword">extends</span> <span class="title">Array</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">static</span> <span class="keyword">get</span> [Symbol.species]() &#123; <span class="keyword">return</span> <span class="built_in">Array</span>; &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//定义Symbol.species属性要采用get取值器。</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//默认的Symbol.species属性等同于下面的写法。</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">static</span> <span class="keyword">get</span> [Symbol.species]() &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="keyword">this</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<h2 id="Symbol-match"><a href="#Symbol-match" class="headerlink" title="Symbol.match"></a>Symbol.match</h2><p>对象<code>Symbol.match</code>属性，指向一个函数。当执行<code>str.match(myObject)</code>时，如果该属性存在，会调用它，返回该方法的返回值。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">String</span>.prototype.match(regexp)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于</span></span></code><br><code class="highlight javascript"><span class="line">regexp[<span class="built_in">Symbol</span>.match](<span class="keyword">this</span>)</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyMatcher</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  [<span class="built_in">Symbol</span>.match](string) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> <span class="string">'hello world'</span>.indexOf(string);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="string">'e'</span>.match(<span class="keyword">new</span> MyMatcher()) <span class="comment">// 1</span></span></code><br></pre></td></tr></table></figure>

<h2 id="Symbol-replace"><a href="#Symbol-replace" class="headerlink" title="Symbol.replace"></a>Symbol.replace</h2><p>对象的<code>Symbol.replace</code>属性，指向一个方法，当该对象被<code>String.prototype.replace</code>方法调用时，会返回该方法的返回值。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">String</span>.prototype.replace(searchValue, replaceValue)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于</span></span></code><br><code class="highlight javascript"><span class="line">searchValue[<span class="built_in">Symbol</span>.replace](<span class="keyword">this</span>, replaceValue)</span></code><br></pre></td></tr></table></figure>

<p>例子:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> x = &#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line">x[<span class="built_in">Symbol</span>.replace] = <span class="function">(<span class="params">...s</span>) =&gt;</span> <span class="built_in">console</span>.log(s);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="string">'Hello'</span>.replace(x, <span class="string">'World'</span>) <span class="comment">// ["Hello", "World"]</span></span></code><br></pre></td></tr></table></figure>

<p><code>Symbol.replace</code>方法会收到两个参数，第一个参数是<code>replace</code>方法正在作用的对象，上面例子是<code>Hello</code>，第二个参数是替换后的值，上面例子是<code>World</code>。</p>
<h2 id="Symbol-search"><a href="#Symbol-search" class="headerlink" title="Symbol.search"></a>Symbol.search</h2><p>对象的<code>Symbol.search</code>属性，指向一个方法，当该对象被<code>String.prototype.search</code>方法调用时，会返回该方法的返回值。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">String</span>.prototype.search(regexp)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于</span></span></code><br><code class="highlight javascript"><span class="line">regexp[<span class="built_in">Symbol</span>.search](<span class="keyword">this</span>)</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MySearch</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">constructor</span>(value) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">this</span>.value = value;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">  [<span class="built_in">Symbol</span>.search](string) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> string.indexOf(<span class="keyword">this</span>.value);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="string">'foobar'</span>.search(<span class="keyword">new</span> MySearch(<span class="string">'foo'</span>)) <span class="comment">// 0</span></span></code><br></pre></td></tr></table></figure>

<h2 id="Symbol-split"><a href="#Symbol-split" class="headerlink" title="Symbol.split"></a>Symbol.split</h2><p>对象的<code>Symbol.split</code>属性，指向一个方法，当该对象被<code>String.prototype.split</code>方法调用时，会返回该方法的返回值。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">String</span>.prototype.split(separator, limit)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于</span></span></code><br><code class="highlight javascript"><span class="line">separator[<span class="built_in">Symbol</span>.split](<span class="keyword">this</span>, limit)</span></code><br></pre></td></tr></table></figure>

<p>例子:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MySplitter</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">constructor</span>(value) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">this</span>.value = value;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">  [<span class="built_in">Symbol</span>.split](string) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">let</span> index = string.indexOf(<span class="keyword">this</span>.value);</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">if</span> (index === <span class="number">-1</span>) &#123;</span></code><br><code class="highlight javascript"><span class="line">      <span class="keyword">return</span> string;</span></code><br><code class="highlight javascript"><span class="line">    &#125;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> [</span></code><br><code class="highlight javascript"><span class="line">      string.substr(<span class="number">0</span>, index),</span></code><br><code class="highlight javascript"><span class="line">      string.substr(index + <span class="keyword">this</span>.value.length)</span></code><br><code class="highlight javascript"><span class="line">    ];</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="string">'foobar'</span>.split(<span class="keyword">new</span> MySplitter(<span class="string">'foo'</span>))</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// ['', 'bar']</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="string">'foobar'</span>.split(<span class="keyword">new</span> MySplitter(<span class="string">'bar'</span>))</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// ['foo', '']</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="string">'foobar'</span>.split(<span class="keyword">new</span> MySplitter(<span class="string">'baz'</span>))</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 'foobar'</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面方法使用Symbol.split方法，重新定义了字符串对象的split方法的行为</p>
</blockquote>
<h2 id="Symbol-Iterator"><a href="#Symbol-Iterator" class="headerlink" title="Symbol.Iterator"></a>Symbol.Iterator</h2><p>对象的<code>Symbol.iterator</code>属性，指向该对象的默认遍历器方法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> myIterable = &#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line">myIterable[<span class="built_in">Symbol</span>.iterator] = <span class="function"><span class="keyword">function</span>* (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="number">2</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="number">3</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">[...myIterable] <span class="comment">// [1, 2, 3]</span></span></code><br></pre></td></tr></table></figure>

<h2 id="Symbol-toPrimitive"><a href="#Symbol-toPrimitive" class="headerlink" title="Symbol.toPrimitive"></a>Symbol.toPrimitive</h2><p>对象的<code>Symbol.toPrimitive</code>属性，指向一个方法。该对象被转为原始类型的值时，会调用这个方法，返回该对象对应的原始类型值。</p>
<p><code>Symbol.toPrimitive</code>被调用时，会接受一个字符串参数，表示当前运算的模式，一共有三种模式。</p>
<ul>
<li><code>Number</code>：该场合需要转成数值</li>
<li><code>String</code>：该场合需要转成字符串</li>
<li><code>Default</code>：该场合可以转成数值，也可以转成字符串</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> obj = &#123;</span></code><br><code class="highlight javascript"><span class="line">  [<span class="built_in">Symbol</span>.toPrimitive](hint) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">switch</span> (hint) &#123;</span></code><br><code class="highlight javascript"><span class="line">      <span class="keyword">case</span> <span class="string">'number'</span>:</span></code><br><code class="highlight javascript"><span class="line">        <span class="keyword">return</span> <span class="number">123</span>;</span></code><br><code class="highlight javascript"><span class="line">      <span class="keyword">case</span> <span class="string">'string'</span>:</span></code><br><code class="highlight javascript"><span class="line">        <span class="keyword">return</span> <span class="string">'str'</span>;</span></code><br><code class="highlight javascript"><span class="line">      <span class="keyword">case</span> <span class="string">'default'</span>:</span></code><br><code class="highlight javascript"><span class="line">        <span class="keyword">return</span> <span class="string">'default'</span>;</span></code><br><code class="highlight javascript"><span class="line">      <span class="keyword">default</span>:</span></code><br><code class="highlight javascript"><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>();</span></code><br><code class="highlight javascript"><span class="line">     &#125;</span></code><br><code class="highlight javascript"><span class="line">   &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="number">2</span> * obj <span class="comment">// 246</span></span></code><br><code class="highlight javascript"><span class="line"><span class="number">3</span> + obj <span class="comment">// '3default'</span></span></code><br><code class="highlight javascript"><span class="line">obj == <span class="string">'default'</span> <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">String</span>(obj) <span class="comment">// 'str'</span></span></code><br></pre></td></tr></table></figure>

<h2 id="Symbol-toStringTag"><a href="#Symbol-toStringTag" class="headerlink" title="Symbol.toStringTag"></a>Symbol.toStringTag</h2><p>对象的<code>Symbol.toStringTag</code>属性，指向一个方法。在该对象上面调用<code>Object.prototype.toString</code>方法时，如果这个属性存在，它的返回值会出现在<code>toString</code>方法返回的字符串之中，表示对象的类型。也就是说，这个属性可以用来定制<code>[object Object]</code>或<code>[object Array]</code>中<code>object</code>后面的那个字符串。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 例一</span></span></code><br><code class="highlight javascript"><span class="line">(&#123;[<span class="built_in">Symbol</span>.toStringTag]: <span class="string">'Foo'</span>&#125;.toString())</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// "[object Foo]"</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 例二</span></span></code><br><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Collection</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">get</span> [Symbol.toStringTag]() &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> <span class="string">'xxx'</span>;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> x = <span class="keyword">new</span> Collection();</span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.prototype.toString.call(x) <span class="comment">// "[object xxx]"</span></span></code><br></pre></td></tr></table></figure>

<p>ES6 新增内置对象的Symbol.toStringTag属性值如下。</p>
<ul>
<li><code>JSON[Symbol.toStringTag]</code>：’JSON’</li>
<li><code>Math[Symbol.toStringTag]</code>：’Math’</li>
<li><code>Module 对象M[Symbol.toStringTag]</code>：’Module’</li>
<li><code>ArrayBuffer.prototype[Symbol.toStringTag]</code>：’ArrayBuffer’</li>
<li><code>DataView.prototype[Symbol.toStringTag]</code>：’DataView’</li>
<li><code>Map.prototype[Symbol.toStringTag]</code>：’Map’</li>
<li><code>Promise.prototype[Symbol.toStringTag]</code>：’Promise’</li>
<li><code>Set.prototype[Symbol.toStringTag]</code>：’Set’</li>
<li><code>%TypedArray%.prototype[Symbol.toStringTag]</code>：’Uint8Array’等</li>
<li><code>WeakMap.prototype[Symbol.toStringTag]</code>：’WeakMap’</li>
<li><code>WeakSet.prototype[Symbol.toStringTag]</code>：’WeakSet’</li>
<li><code>%MapIteratorPrototype%[Symbol.toStringTag]</code>：’Map Iterator’</li>
<li><code>%SetIteratorPrototype%[Symbol.toStringTag]</code>：’Set Iterator’</li>
<li><code>%StringIteratorPrototype%[Symbol.toStringTag]</code>：’String Iterator’</li>
<li><code>Symbol.prototype[Symbol.toStringTag]</code>：’Symbol’</li>
<li><code>Generator.prototype[Symbol.toStringTag]</code>：’Generator’</li>
<li><code>GeneratorFunction.prototype[Symbol.toStringTag]</code>：’GeneratorFunction’</li>
</ul>
<h2 id="Symbol-unscopables"><a href="#Symbol-unscopables" class="headerlink" title="Symbol.unscopables"></a>Symbol.unscopables</h2><p>对象的<code>Symbol.unscopables</code>属性，指向一个对象。该对象指定了使用with关键字时，哪些属性会被<code>with</code>环境排除。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Array</span>.prototype[<span class="built_in">Symbol</span>.unscopables]</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// &#123;</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//   copyWithin: true,</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//   entries: true,</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//   fill: true,</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//   find: true,</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//   findIndex: true,</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//   includes: true,</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//   keys: true</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// &#125;</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.keys(<span class="built_in">Array</span>.prototype[<span class="built_in">Symbol</span>.unscopables])</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">/* ['copyWithin', 'entries', 'fill', 'find', 'findIndex', </span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">	'includes', 'keys']</span></span></code><br></pre></td></tr></table></figure>

<p>上面代码说明，数组有7个属性，会被<code>with</code>命令排除。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 没有 unscopables 时</span></span></code><br><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyClass</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  foo() &#123; <span class="keyword">return</span> <span class="number">1</span>; &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> foo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123; <span class="keyword">return</span> <span class="number">2</span>; &#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">with</span> (MyClass.prototype) &#123;</span></code><br><code class="highlight javascript"><span class="line">  foo(); <span class="comment">// 1</span></span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 有 unscopables 时</span></span></code><br><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyClass</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  foo() &#123; <span class="keyword">return</span> <span class="number">1</span>; &#125;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">get</span> [Symbol.unscopables]() &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> &#123; <span class="attr">foo</span>: <span class="literal">true</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> foo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123; <span class="keyword">return</span> <span class="number">2</span>; &#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">with</span> (MyClass.prototype) &#123;</span></code><br><code class="highlight javascript"><span class="line">  foo(); <span class="comment">// 2</span></span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>上面代码通过指定Symbol.unscopables属性，使得with语法块不会在当前作用域寻找foo属性，即foo将指向外层作用域的变量。</p>
</blockquote>
]]></content>
      <categories>
        <category>前端开发</category>
        <category>ES6学习</category>
      </categories>
      <tags>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript6--对象的扩展</title>
    <url>/2019/10/20/ECMAScript6/9%E3%80%81%E5%AF%B9%E8%B1%A1%E7%9A%84%E6%89%A9%E5%B1%95/</url>
    <content><![CDATA[<h1 id="属性的简洁表示法"><a href="#属性的简洁表示法" class="headerlink" title="属性的简洁表示法"></a>属性的简洁表示法</h1><p>ES6允许直接写入变量和函数,作为对象的属性和方法.(这在之前是不允许的)<br>直接写入变量:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> foo = <span class="string">'bar'</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> baz = &#123;foo&#125;;</span></code><br><code class="highlight javascript"><span class="line">baz <span class="comment">// &#123;foo: "bar"&#125;</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> baz = &#123;<span class="attr">foo</span>: foo&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//当属性名与变量名相等时可以只写一个属性名</span></span></code><br></pre></td></tr></table></figure>

<a id="more"></a>

<p>方法的简写</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> o = &#123;</span></code><br><code class="highlight javascript"><span class="line">  method() &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> <span class="string">"Hello!"</span>;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> o = &#123;</span></code><br><code class="highlight javascript"><span class="line">  method: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> <span class="string">"Hello!"</span>;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<p>如果某个方法的值是一个Generator函数,前面需要加上星号</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> obj = &#123;</span></code><br><code class="highlight javascript"><span class="line">  * m() &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">yield</span> <span class="string">'hello world'</span>;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<h1 id="属性名表达式"><a href="#属性名表达式" class="headerlink" title="属性名表达式"></a>属性名表达式</h1><p>JavaScript定义对象的属性:</p>
<ul>
<li>方法一: <code>obj.foo = true</code>;</li>
<li>方法二(属性名表达式): <code>obj[&#39;a&#39; + &#39;bc&#39;] = 123</code>;</li>
</ul>
<p>中括号中还可以放入变量名<br>ES6之前,使用字面量方式定义对象(使用大括号),只能使用方法一的形式:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> obj = &#123;</span></code><br><code class="highlight javascript"><span class="line">  foo: <span class="literal">true</span>,</span></code><br><code class="highlight javascript"><span class="line">  abc: <span class="number">123</span></span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<p>ES6允许字面量定义对象时,用方法二作为对象的属性名,即把表达式放在方括号内:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> propKey = <span class="string">'foo'</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> obj = &#123;</span></code><br><code class="highlight javascript"><span class="line">  [propKey]: <span class="literal">true</span>,</span></code><br><code class="highlight javascript"><span class="line">  [<span class="string">'a'</span> + <span class="string">'bc'</span>]: <span class="number">123</span></span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<p>使用方法二定义方法名:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> obj = &#123;</span></code><br><code class="highlight javascript"><span class="line">  [<span class="string">'h'</span> + <span class="string">'ello'</span>]() &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> <span class="string">'hi'</span>;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">obj.hello() <span class="comment">// hi</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意:</p>
<ul>
<li>属性名表达式不能与简洁表达式同时使用</li>
<li>属性名表达式如果是一个对象,默认情况下会自动转为字符串[object,object],(会覆盖掉别的属性名表达式为对象的值)</li>
</ul>
</blockquote>
<h1 id="方法的name属性"><a href="#方法的name属性" class="headerlink" title="方法的name属性"></a>方法的name属性</h1><p>函数的name属性,返回函数名. 对象方法也是函数,因此也有name属性</p>
<p>如果对象的方法使用了取值函数(getter)和存值函数(setter),则name属性不是在该方法上面,而是该方法的属性的描述对象get和set属性上面,返回值是方法名前加上get和set</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> obj = &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">get</span> foo() &#123;&#125;,</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">set</span> foo(x) &#123;&#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">obj.foo.name</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// TypeError: Cannot read property 'name' of undefined</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> descriptor = <span class="built_in">Object</span>.getOwnPropertyDescriptor(obj, <span class="string">'foo'</span>);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">descriptor.get.name <span class="comment">// "get foo"</span></span></code><br><code class="highlight javascript"><span class="line">descriptor.set.name <span class="comment">// "set foo"</span></span></code><br></pre></td></tr></table></figure>

<p>特殊情况:</p>
<ul>
<li>bind方法创造的函数,name属性返回bound加上原函数的名字</li>
<li>Function构造函数创造的函数,name属性返回anonymous</li>
<li>如果对象的方法是一个Symbol,那么name属性返回的是这个Symbol值的描述</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> key1 = <span class="built_in">Symbol</span>(<span class="string">'description'</span>);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> key2 = <span class="built_in">Symbol</span>();</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> obj = &#123;</span></code><br><code class="highlight javascript"><span class="line">  [key1]() &#123;&#125;,</span></code><br><code class="highlight javascript"><span class="line">  [key2]() &#123;&#125;,</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line">obj[key1].name <span class="comment">// "[description]"</span></span></code><br><code class="highlight javascript"><span class="line">obj[key2].name <span class="comment">// ""</span></span></code><br></pre></td></tr></table></figure>

<h1 id="属性的可枚举性和遍历"><a href="#属性的可枚举性和遍历" class="headerlink" title="属性的可枚举性和遍历"></a>属性的可枚举性和遍历</h1><h2 id="可枚举性"><a href="#可枚举性" class="headerlink" title="可枚举性"></a>可枚举性</h2><p>对象的每个属性都有一个描述对象(Descriptor),用来控制该属性的行为.</p>
<p>ES5的<code>Object.getOwnPropertyDescriptor</code>方法可以获取该属性的描述对象.</p>
<p>描述对象的enumerable属性,称为”可枚举性”,如果该属性为false,就表示某些操作会忽略当前属性</p>
<p>目前,有四个操作会忽略enumerable为false的属性:</p>
<ul>
<li><code>for...in</code>循环：只遍历对象自身的和继承的可枚举的属性。</li>
<li><code>Object.keys()</code>：返回对象自身的所有可枚举的属性的键名。</li>
<li><code>JSON.stringify()</code>：只串行化对象自身的可枚举的属性。</li>
<li><code>Object.assign()</code>： 忽略enumerable为false的属性，只拷贝对象自身的可枚举的属性。</li>
</ul>
<p>上面四个方法中,只有for…in会返回继承的属性,其他三个方法都会忽略继承的属性,只处理对象自身的属性<br><strong>注意:</strong><br>所有 Class 的原型的方法都是不可枚举的。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.getOwnPropertyDescriptor(<span class="class"><span class="keyword">class</span></span>&#123;foo()&#123;&#125;&#125;.prototype, <span class="string">'foo'</span>)</span></code><br><code class="highlight javascript"><span class="line">.enumerable</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// false</span></span></code><br></pre></td></tr></table></figure>

<h2 id="属性的遍历"><a href="#属性的遍历" class="headerlink" title="属性的遍历"></a>属性的遍历</h2><p>ES6一共有5中方法可以遍历对象的属性</p>
<h3 id="for…in"><a href="#for…in" class="headerlink" title="for…in"></a>for…in</h3><p><code>for...in</code>循环遍历对象自身的和继承的可枚举属性（不含 Symbol 属性）</p>
<h3 id="Object-keys-obj"><a href="#Object-keys-obj" class="headerlink" title="Object.keys(obj)"></a>Object.keys(obj)</h3><p><code>Object.keys</code>返回一个数组，包括对象自身的（不含继承的）所有可枚举属性（不含 Symbol 属性）的键名。</p>
<h3 id="Object-getOwnPropertyNames-obj"><a href="#Object-getOwnPropertyNames-obj" class="headerlink" title="Object.getOwnPropertyNames(obj)"></a>Object.getOwnPropertyNames(obj)</h3><p><code>Object.getOwnPropertyNames</code>返回一个数组，包含对象自身的所有属性（不含 Symbol 属性，但是包括不可枚举属性）的键名。</p>
<h3 id="Object-getOwnPropertySymbols-obj"><a href="#Object-getOwnPropertySymbols-obj" class="headerlink" title="Object.getOwnPropertySymbols(obj)"></a>Object.getOwnPropertySymbols(obj)</h3><p><code>Object.getOwnPropertySymbols</code>返回一个数组，包含对象自身的所有 Symbol 属性的键名。</p>
<h3 id="Reflect-ownKeys-obj"><a href="#Reflect-ownKeys-obj" class="headerlink" title="Reflect.ownKeys(obj)"></a>Reflect.ownKeys(obj)</h3><p><code>Reflect.ownKeys</code>返回一个数组，包含对象自身的所有键名，不管键名是 Symbol 或字符串，也不管是否可枚举。<br>以上的5种方法遍历对象的键名,都遵守同样的属性遍历的次序规则:</p>
<ul>
<li>首先遍历所有数值键，按照数值升序排列。</li>
<li>其次遍历所有字符串键，按照加入时间升序排列。</li>
<li>最后遍历所有 Symbol 键，按照加入时间升序排列。</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Reflect</span>.ownKeys(&#123; [<span class="built_in">Symbol</span>()]:<span class="number">0</span>, <span class="attr">b</span>:<span class="number">0</span>, <span class="number">10</span>:<span class="number">0</span>, <span class="number">2</span>:<span class="number">0</span>, <span class="attr">a</span>:<span class="number">0</span> &#125;)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// ['2', '10', 'b', 'a', Symbol()]</span></span></code><br></pre></td></tr></table></figure>

<h1 id="super关键字"><a href="#super关键字" class="headerlink" title="super关键字"></a>super关键字</h1><p>super关键字指向当前对象的原型对象</p>
<p><strong>注意:</strong><br>该关键字只有用在对象方法的简写法里面才有效,否则会报错</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 报错</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> obj = &#123;</span></code><br><code class="highlight javascript"><span class="line">  foo: <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> <span class="keyword">super</span>.foo</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//方法简写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> proto = &#123;</span></code><br><code class="highlight javascript"><span class="line">  foo: <span class="string">'hello'</span></span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> obj = &#123;</span></code><br><code class="highlight javascript"><span class="line">  foo: <span class="string">'world'</span>,</span></code><br><code class="highlight javascript"><span class="line">  find() &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> <span class="keyword">super</span>.foo;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.setPrototypeOf(obj, proto);</span></code><br><code class="highlight javascript"><span class="line">obj.find() <span class="comment">// "hello"</span></span></code><br></pre></td></tr></table></figure>

<h1 id="对象的扩展运算符"><a href="#对象的扩展运算符" class="headerlink" title="对象的扩展运算符"></a>对象的扩展运算符</h1><h2 id="解构赋值"><a href="#解构赋值" class="headerlink" title="解构赋值"></a>解构赋值</h2><p>扩展运算符用于解构赋值,必须位于最后一个参数,会将所有为被读取的属性和值一起拷贝到新对象上面</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> &#123; x, y, ...z &#125; = &#123; <span class="attr">x</span>: <span class="number">1</span>, <span class="attr">y</span>: <span class="number">2</span>, <span class="attr">a</span>: <span class="number">3</span>, <span class="attr">b</span>: <span class="number">4</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line">x <span class="comment">// 1</span></span></code><br><code class="highlight javascript"><span class="line">y <span class="comment">// 2</span></span></code><br><code class="highlight javascript"><span class="line">z <span class="comment">// &#123; a: 3, b: 4 &#125;</span></span></code><br></pre></td></tr></table></figure>

<p><strong>注意:</strong></p>
<ul>
<li>由于解构赋值要求等号右边是一个对象，所以如果等号右边是undefined或null,就会报错,因为它们无法转为对象</li>
<li>扩展运算符的解构赋值不能复制继承自原型的属性</li>
<li>解构赋值的拷贝是浅拷贝</li>
</ul>
<p>解构赋值还可以用于扩展某个函数的参数:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">baseFunction</span>(<span class="params">&#123; a, b &#125;</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">// ...</span></span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">wrapperFunction</span>(<span class="params">&#123; x, y, ...restConfig &#125;</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">// 使用 x 和 y 参数进行操作</span></span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">// 其余参数传给原始函数</span></span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> baseFunction(restConfig);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<h2 id="扩展运算符"><a href="#扩展运算符" class="headerlink" title="扩展运算符"></a>扩展运算符</h2><p>对象的扩展运算符 ( … ) 用于取出参数对象的所有可遍历属性,拷贝到当前对象之中</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> z = &#123; <span class="attr">a</span>: <span class="number">3</span>, <span class="attr">b</span>: <span class="number">4</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> n = &#123; ...z &#125;;</span></code><br><code class="highlight javascript"><span class="line">n <span class="comment">// &#123; a: 3, b: 4 &#125;</span></span></code><br></pre></td></tr></table></figure>

<p>由于数组是特殊的对象,所以对象的扩展运算符也可以用于数组</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> foo = &#123; ...[<span class="string">'a'</span>, <span class="string">'b'</span>, <span class="string">'c'</span>] &#125;;</span></code><br><code class="highlight javascript"><span class="line">foo</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// &#123;0: "a", 1: "b", 2: "c"&#125;</span></span></code><br></pre></td></tr></table></figure>

<p><strong>规则:</strong></p>
<ul>
<li>如果扩展运算符后面是一个空对象,则没有任何效果</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">&#123;...&#123;&#125;, <span class="attr">a</span>: <span class="number">1</span>&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// &#123; a: 1 &#125;</span></span></code><br></pre></td></tr></table></figure>

<ul>
<li>如果扩展运算符后面不是对象,则会自动将其转为对象</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 等同于 &#123;...Object(1)&#125;</span></span></code><br><code class="highlight javascript"><span class="line">&#123;..<span class="number">.1</span>&#125; <span class="comment">// &#123;&#125;</span></span></code><br></pre></td></tr></table></figure>

<ul>
<li>如果扩展运算符后面是字符串,它会自动转成一个类似数组的对象</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">&#123;...<span class="string">'hello'</span>&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// &#123;0: "h", 1: "e", 2: "l", 3: "l", 4: "o"&#125;</span></span></code><br></pre></td></tr></table></figure>

<p>上面的例子只是拷贝了对象实例的属性，如果想完整克隆一个对象，还拷贝对象原型的属性，可以采用下面的写法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 写法一</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> clone1 = &#123;</span></code><br><code class="highlight javascript"><span class="line">  __proto__: <span class="built_in">Object</span>.getPrototypeOf(obj),</span></code><br><code class="highlight javascript"><span class="line">  ...obj</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 写法二</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> clone2 = <span class="built_in">Object</span>.assign(</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">Object</span>.create(<span class="built_in">Object</span>.getPrototypeOf(obj)),</span></code><br><code class="highlight javascript"><span class="line">  obj</span></code><br><code class="highlight javascript"><span class="line">);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 写法三</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> clone3 = <span class="built_in">Object</span>.create(</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">Object</span>.getPrototypeOf(obj),</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">Object</span>.getOwnPropertyDescriptors(obj)</span></code><br><code class="highlight javascript"><span class="line">)</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//推荐使用写法一和写法二</span></span></code><br></pre></td></tr></table></figure>

<p>扩展运算符等同于使用<code>Object.assign()</code>方法</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> aClone = &#123; ...a &#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> aClone = <span class="built_in">Object</span>.assign(&#123;&#125;, a);</span></code><br></pre></td></tr></table></figure>

<p>扩展运算符可以用于合并两个对象:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> ab = &#123; ...a, ...b &#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> ab = <span class="built_in">Object</span>.assign(&#123;&#125;, a, b);</span></code><br></pre></td></tr></table></figure>

<p>注意:</p>
<ul>
<li>如果用户自定义的属性,放在扩展运算符后面,则扩展运算符内部的同名属性会被覆盖掉</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> a = &#123;<span class="attr">x</span>:<span class="number">3</span>,<span class="attr">y</span>:<span class="number">4</span>&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> aWithOverrides = &#123; ...a, <span class="attr">x</span>: <span class="number">1</span>, <span class="attr">y</span>: <span class="number">2</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line">x    <span class="comment">//1</span></span></code><br><code class="highlight javascript"><span class="line">y    <span class="comment">//2</span></span></code><br></pre></td></tr></table></figure>

<ul>
<li>如果把自定义属性放在扩展运算符前面，就变成了设置新对象的默认属性值。</li>
<li>与数组的扩展运算符一样，对象的扩展运算符后面可以跟表达式。</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> obj = &#123;</span></code><br><code class="highlight javascript"><span class="line">  ...(x &gt; <span class="number">1</span> ? &#123;<span class="attr">a</span>: <span class="number">1</span>&#125; : &#123;&#125;),</span></code><br><code class="highlight javascript"><span class="line">  b: <span class="number">2</span>,</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>



<p>​             <img src="https://qqadapt.qpic.cn/txdocpic/0/dc0f131b73a08bbf08b5e96faaf768e6/0" alt="img">             </p>
<h1 id="对象的新增方法"><a href="#对象的新增方法" class="headerlink" title="对象的新增方法"></a>对象的新增方法</h1><h2 id="Object-is"><a href="#Object-is" class="headerlink" title="Object.is()"></a>Object.is()</h2><p>ES5 比较两个值是否相等，只有两个运算符：相等运算符 ( == ) 和严格相等运算符 ( === ) ,它们都有缺点，前者会自动转换数据类型,后者的<code>NaN</code>不等于自身,以及+0 等于 -0</p>
<p>JavaScript缺乏一种算法在所有环境中，只要两个值是一样的，它们就应该相等。<br>ES6 提出“Same-value equality”（同值相等）算法，用来解决这个问题。<br><code>Object.is()</code>就是部署这个算法的新方法。它用来比较两个值是否严格相等，与严格比较运算符（===）的行为基本一致。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.is(<span class="string">'foo'</span>, <span class="string">'foo'</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.is(&#123;&#125;, &#123;&#125;)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// false</span></span></code><br></pre></td></tr></table></figure>

<p><strong>注意:</strong></p>
<ul>
<li>该方法中 +0不等于-0</li>
<li><code>NaN</code>等于自身</li>
</ul>
<h2 id="Object-assign"><a href="#Object-assign" class="headerlink" title="Object.assign()"></a>Object.assign()</h2><h3 id="基本用法"><a href="#基本用法" class="headerlink" title="基本用法"></a><strong>基本用法</strong></h3><p><code>Object.assign</code>方法用于对象的合并,将源对象（source）的所有可枚举属性，复制到目标对象（target）。<br><code>Object.assign</code>方法的第一个参数是目标对象,后面的所有参数都是源对象</p>
<ul>
<li>如果目标对象与源对象有同名属性，或多个源对象有同名属性，则后面的属性会覆盖前面的属性。</li>
<li>如果只有一个参数,Object.assign方法会直接返回该参数</li>
<li>如果该参数不是对象，则会先转成对象，然后返回。</li>
<li>由于undefined和null无法转为对象,所以如果他们作为参数(首参数)会报错</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">//首参数</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.assign(<span class="literal">undefined</span>) <span class="comment">// 报错</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.assign(<span class="literal">null</span>) <span class="comment">// 报错</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//非首参数</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> obj = &#123;<span class="attr">a</span>: <span class="number">1</span>&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.assign(obj, <span class="literal">undefined</span>) === obj <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.assign(obj, <span class="literal">null</span>) === obj <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//非对象作为非首参数传入时,这些参数都会转为对象,如果无法转为对象,就会跳过</span></span></code><br></pre></td></tr></table></figure>

<ul>
<li>其他类型的值（即数值、字符串和布尔值）不在首参数，也不会报错,除了字符串会以数组的形式拷贝入对象,其他值都不会产生效果</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> v1 = <span class="string">'abc'</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> v2 = <span class="literal">true</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> v3 = <span class="number">10</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> obj = <span class="built_in">Object</span>.assign(&#123;&#125;, v1, v2, v3);</span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">console</span>.log(obj); <span class="comment">// &#123; "0": "a", "1": "b", "2": "c" &#125;</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//Object(true) // &#123;[[PrimitiveValue]]: true&#125;</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//Object(10)  //  &#123;[[PrimitiveValue]]: 10&#125;</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//Object('abc')//&#123;0:"a",1:"b",2:"c",length:3,[[PrimitiveValue]]:"abc"&#125;</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//PrimitiveValue不可以被枚举</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//上面三种类型的值转换为包装对象后,只有字符串有可枚举的实义属性</span></span></code><br></pre></td></tr></table></figure>



<p><strong>注意:</strong></p>
<ul>
<li>浅拷贝: 如果源对象某个属性的值是对象，那么目标对象拷贝得到的是这个对象的引用。</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> obj1 = &#123;<span class="attr">a</span>: &#123;<span class="attr">b</span>: <span class="number">1</span>&#125;&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> obj2 = <span class="built_in">Object</span>.assign(&#123;&#125;, obj1);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">obj1.a.b = <span class="number">2</span>;</span></code><br><code class="highlight javascript"><span class="line">obj2.a.b <span class="comment">// 2</span></span></code><br></pre></td></tr></table></figure>

<ul>
<li>同名属性的替换: 目标对象与一个或多个源对象有同名属性时,后面的属性会覆盖前面的</li>
<li>数组的处理: <code>Object.assign</code>方法会把数组当作对象处理(数组的索引即为对象的属性名)</li>
<li>取值函数的处理:<code>Object.assign</code>方法只能进行值的复制，如果要复制的值是一个取值函数，那么将求值后再复制。</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> source = &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">get</span> foo() &#123; <span class="keyword">return</span> <span class="number">1</span> &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> target = &#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.assign(target, source)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// &#123; foo: 1 &#125;</span></span></code><br></pre></td></tr></table></figure>

<ul>
<li><code>Object.assign</code>方法拷贝的属性是有限制的，只拷贝源对象的自身属性（不拷贝继承属性），也不拷贝不可枚举的属性(<code>enumerable:false</code>)</li>
<li>属性名为 Symbol 值的属性，也会被<code>Object.assign()</code>拷贝</li>
</ul>
<h3 id="常见用途"><a href="#常见用途" class="headerlink" title="常见用途"></a>常见用途</h3><p>1.为对象添加属性</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">constructor</span>(x, y) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="built_in">Object</span>.assign(<span class="keyword">this</span>, &#123;x, y&#125;);</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<p>2.为对象添加方法</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.assign(SomeClass.prototype, &#123;</span></code><br><code class="highlight javascript"><span class="line">  someMethod(arg1, arg2) &#123;</span></code><br><code class="highlight javascript"><span class="line">    ···</span></code><br><code class="highlight javascript"><span class="line">  &#125;,</span></code><br><code class="highlight javascript"><span class="line">  anotherMethod() &#123;</span></code><br><code class="highlight javascript"><span class="line">    ···</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于下面的写法</span></span></code><br><code class="highlight javascript"><span class="line">SomeClass.prototype.someMethod = <span class="function"><span class="keyword">function</span> (<span class="params">arg1, arg2</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  ···</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line">SomeClass.prototype.anotherMethod = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  ···</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<p>3.克隆对象</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">clone</span>(<span class="params">origin</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="built_in">Object</span>.assign(&#123;&#125;, origin);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<p>上面代码将原始对象拷贝到一个空对象，就得到了原始对象的克隆。</p>
<p>不过，采用这种方法克隆，只能克隆原始对象自身的值，不能克隆它继承的值。如果想要保持继承链，可以采用下面的代码。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">clone</span>(<span class="params">origin</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">let</span> originProto = <span class="built_in">Object</span>.getPrototypeOf(origin);</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="built_in">Object</span>.assign(<span class="built_in">Object</span>.create(originProto), origin);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<p>4.合并多个对象</p>
<p>将多个对象合并到某个对象</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> merge =</span></code><br><code class="highlight javascript"><span class="line">  (target, ...sources) =&gt; <span class="built_in">Object</span>.assign(target, ...sources);</span></code><br></pre></td></tr></table></figure>

<p>5.为属性指定默认值</p>
<p>利用同名属性替换的特点,可以将默认值对象写在前面,后面跟源对象</p>
<h2 id="Object-getOwnPropertyDescriptors"><a href="#Object-getOwnPropertyDescriptors" class="headerlink" title="Object.getOwnPropertyDescriptors()"></a>Object.getOwnPropertyDescriptors()</h2><p>ES5的<code>Object.getOwnPropertyDescriptor()</code>方法会返回某个对象属性的描述对象(descriptor)<br>ES2017引入了<code>Object.getOwnPropertyDescriptors()</code>方法,返回指定对象所有自身属性(非继承属性)的描述对象</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> obj = &#123;</span></code><br><code class="highlight javascript"><span class="line">  foo: <span class="number">123</span>,</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">get</span> bar() &#123; <span class="keyword">return</span> <span class="string">'abc'</span> &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.getOwnPropertyDescriptors(obj)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// &#123; foo:</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//    &#123; value: 123,</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//      writable: true,</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//      enumerable: true,</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//      configurable: true &#125;,</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//   bar:</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//    &#123; get: [Function: get bar],</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//      set: undefined,</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//      enumerable: true,</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//      configurable: true &#125; &#125;</span></span></code><br></pre></td></tr></table></figure>

<p><strong>用处一:</strong></p>
<p>该方法的引入目的,主要是为了解决<code>Object.assign()</code>无法正确拷贝get属性和set属性的问题</p>
<p>使用<code>Object.getOwnPropertyDescriptor()</code>方法配合<code>Object.defineProperties()</code>方法,就可以实现正确拷贝:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><code class="highlight plain"><span class="line">const source &#x3D; &#123;</span></code><br><code class="highlight plain"><span class="line">  set foo(value) &#123;</span></code><br><code class="highlight plain"><span class="line">    console.log(value);</span></code><br><code class="highlight plain"><span class="line">  &#125;</span></code><br><code class="highlight plain"><span class="line">&#125;;</span></code><br><code class="highlight plain"><span class="line">const target2 &#x3D; &#123;&#125;;</span></code><br><code class="highlight plain"><span class="line">Object.defineProperties(target2, Object.getOwnPropertyDescriptors(source));</span></code><br><code class="highlight plain"><span class="line">Object.getOwnPropertyDescriptor(target2, &#39;foo&#39;)</span></code><br><code class="highlight plain"><span class="line">&#x2F;&#x2F; &#123; get: undefined,</span></code><br><code class="highlight plain"><span class="line">&#x2F;&#x2F;   set: [Function: set foo],</span></code><br><code class="highlight plain"><span class="line">&#x2F;&#x2F;   enumerable: true,</span></code><br><code class="highlight plain"><span class="line">&#x2F;&#x2F;   configurable: true &#125;</span></code><br></pre></td></tr></table></figure>

<p>上面代码中,两个对象合并的逻辑可以写成一个函数:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> shallowMerge = <span class="function">(<span class="params">target, source</span>) =&gt;</span> <span class="built_in">Object</span>.defineProperties(</span></code><br><code class="highlight javascript"><span class="line">  target,</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">Object</span>.getOwnPropertyDescriptors(source)</span></code><br><code class="highlight javascript"><span class="line">);</span></code><br></pre></td></tr></table></figure>

<p><strong>用处二:</strong></p>
<p>配合<code>Object.create()</code>方法,将对象属性克隆到一个新对象(这属于浅拷贝).</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> clone = <span class="built_in">Object</span>.create(<span class="built_in">Object</span>.getPrototypeOf(obj),</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">Object</span>.getOwnPropertyDescriptors(obj));</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 或者</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> shallowClone = <span class="function">(<span class="params">obj</span>) =&gt;</span> <span class="built_in">Object</span>.create(</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">Object</span>.getPrototypeOf(obj),</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">Object</span>.getOwnPropertyDescriptors(obj)</span></code><br><code class="highlight javascript"><span class="line">);</span></code><br></pre></td></tr></table></figure>

<p><strong>用处三:</strong></p>
<p>实现一个对象继承另一个对象</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> obj = <span class="built_in">Object</span>.create(</span></code><br><code class="highlight javascript"><span class="line">  prot,</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">Object</span>.getOwnPropertyDescriptors(&#123;</span></code><br><code class="highlight javascript"><span class="line">    foo: <span class="number">123</span>,</span></code><br><code class="highlight javascript"><span class="line">  &#125;)</span></code><br><code class="highlight javascript"><span class="line">);</span></code><br></pre></td></tr></table></figure>

<p><strong>用处四:</strong></p>
<p><code>Object.getOwnPropertyDescriptors()</code>可以实现<code>Mixin</code>(混入)模式</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> mix = <span class="function">(<span class="params">object</span>) =&gt;</span> (&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">with</span>: <span class="function">(<span class="params">...mixins</span>) =&gt;</span> mixins.reduce(</span></code><br><code class="highlight javascript"><span class="line">    (c, mixin) =&gt; <span class="built_in">Object</span>.create(</span></code><br><code class="highlight javascript"><span class="line">      c, <span class="built_in">Object</span>.getOwnPropertyDescriptors(mixin)</span></code><br><code class="highlight javascript"><span class="line">    ), object)</span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// multiple mixins example</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> a = &#123;<span class="attr">a</span>: <span class="string">'a'</span>&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> b = &#123;<span class="attr">b</span>: <span class="string">'b'</span>&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> c = &#123;<span class="attr">c</span>: <span class="string">'c'</span>&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> d = mix(c).with(a, b);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">d.c <span class="comment">// "c"</span></span></code><br><code class="highlight javascript"><span class="line">d.b <span class="comment">// "b"</span></span></code><br><code class="highlight javascript"><span class="line">d.a <span class="comment">// "a"</span></span></code><br></pre></td></tr></table></figure>

<h2 id="proto-属性-Object-setPrototypeOf-Object-getPrototypeOf"><a href="#proto-属性-Object-setPrototypeOf-Object-getPrototypeOf" class="headerlink" title="_proto_属性,Object.setPrototypeOf(),Object.getPrototypeOf()"></a>_proto_属性,Object.setPrototypeOf(),Object.getPrototypeOf()</h2><h3 id="proto"><a href="#proto" class="headerlink" title="proto"></a><em>proto</em></h3><p>_proto_属性(前后各两个下划线),用来读取或设置当前对象的prototype对象,所有浏览器都部署了这个属性<br>ES6规定,只有浏览器必须部署这个属性,其他运行环境不一定需要部署,而且新的代码最好认为这个属性是不存在的.   因此最好不要使用这个属性.</p>
<p>用下面的方法代替:</p>
<ul>
<li><code>Object.setPrototypeOf()</code>   (写操作)</li>
<li><code>Object.getPrototypeOf()</code>   (读操作)</li>
<li><code>Object.create()</code>     (生成操作)</li>
</ul>
<h3 id="Object-setPrototypeOf"><a href="#Object-setPrototypeOf" class="headerlink" title="Object.setPrototypeOf()"></a>Object.setPrototypeOf()</h3><p><code>Object.setPrototypeOf</code>方法的作用于_proto_相同,用来设置一个对象的prototype对象,返回参数对象本身.   它是ES6正式推荐的设置原型对象的方法</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 格式</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.setPrototypeOf(object, prototype)</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 用法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> proto = &#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> obj = &#123; <span class="attr">x</span>: <span class="number">10</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.setPrototypeOf(obj, proto);</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//将proto对象设为obj对象的原型</span></span></code><br></pre></td></tr></table></figure>

<p><strong>注意:</strong></p>
<ul>
<li>如果第一个参数不是对象,会自动转为对象.  但是由于返回的还是第一个参数,所以这个操作不会产生任何效果:</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.setPrototypeOf(<span class="number">1</span>, &#123;&#125;) === <span class="number">1</span> <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.setPrototypeOf(<span class="string">'foo'</span>, &#123;&#125;) === <span class="string">'foo'</span> <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.setPrototypeOf(<span class="literal">true</span>, &#123;&#125;) === <span class="literal">true</span> <span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>

<ul>
<li>由于undefined和null无法转为对象,所以如果第一个参数是undefined或null,就会报错</li>
</ul>
<h3 id="Object-getPrototypeOf"><a href="#Object-getPrototypeOf" class="headerlink" title="Object.getPrototypeOf()"></a>Object.getPrototypeOf()</h3><p>该方法与<code>Object.setPrototypeOf()</code>方法配套,用于读取一个对象的原型对象</p>
<p>用法: <code>Object.getPrototypeOf(obj)</code></p>
<p><strong>注意:</strong></p>
<ul>
<li>如果参数不是对象,会自动转为对象</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 等同于 Object.getPrototypeOf(Number(1))</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.getPrototypeOf(<span class="number">1</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// Number &#123;[[PrimitiveValue]]: 0&#125;</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于 Object.getPrototypeOf(String('foo'))</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.getPrototypeOf(<span class="string">'foo'</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// String &#123;length: 0, [[PrimitiveValue]]: ""&#125;</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于 Object.getPrototypeOf(Boolean(true))</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.getPrototypeOf(<span class="literal">true</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// Boolean &#123;[[PrimitiveValue]]: false&#125;</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.getPrototypeOf(<span class="number">1</span>) === <span class="built_in">Number</span>.prototype <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.getPrototypeOf(<span class="string">'foo'</span>) === <span class="built_in">String</span>.prototype <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.getPrototypeOf(<span class="literal">true</span>) === <span class="built_in">Boolean</span>.prototype <span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>

<ul>
<li>如果参数是undefined或null,他们无法转为对象,所以报错.</li>
</ul>
<h2 id="Object-keys-Object-values-Object-entries"><a href="#Object-keys-Object-values-Object-entries" class="headerlink" title="Object.keys() , Object.values() , Object.entries()"></a>Object.keys() , Object.values() , Object.entries()</h2><h3 id="Object-keys"><a href="#Object-keys" class="headerlink" title="Object.keys()"></a>Object.keys()</h3><p>该方法返回一个数组,成员是参数对象自身的(不含继承的)所有可遍历属性的键名</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> obj = &#123; <span class="attr">foo</span>: <span class="string">'bar'</span>, <span class="attr">baz</span>: <span class="number">42</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.keys(obj)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// ["foo", "baz"]</span></span></code><br></pre></td></tr></table></figure>

<h3 id="Object-values"><a href="#Object-values" class="headerlink" title="Object.values()"></a>Object.values()</h3><p>该方法返回一个数组,成员是参数对象自身的(不含继承的)所有可遍历属性的键值</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> obj = &#123; <span class="attr">foo</span>: <span class="string">'bar'</span>, <span class="attr">baz</span>: <span class="number">42</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.values(obj)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// ["bar", 42]</span></span></code><br></pre></td></tr></table></figure>

<p>返回数组的成员顺序为:</p>
<ul>
<li>首先遍历所有数值键，按照数值升序排列。</li>
<li>其次遍历所有字符串键，按照加入时间升序排列。</li>
<li>最后遍历所有 Symbol 键，按照加入时间升序排列。</li>
</ul>
<p><strong>注意:</strong></p>
<ul>
<li><code>Object.values</code>只返回对象自身的可遍历属性[不是显示声明的属性默认是不可遍历的]</li>
<li><code>Object.values</code>会过滤属性名为Symbol值的属性</li>
<li>如果<code>Object.values</code>方法的参数是一个字符串,会返回各个字符组成的一个数组</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.values(<span class="string">'foo'</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// ['f', 'o', 'o']</span></span></code><br></pre></td></tr></table></figure>

<ul>
<li>如果参数不是对象,<code>Object.values</code>会先将其转为对象,由于数值和布尔值的包装对象,都不会为实例添加非继承的属性,所以会返回空数组</li>
</ul>
<h3 id="Object-entries"><a href="#Object-entries" class="headerlink" title="Object.entries()"></a>Object.entries()</h3><p>该方法返回一个数组,成员是参数对象自身(不含继承的)所有可遍历属性的键值对数组<br>除了返回值不一样,该方法的行为与<code>Object.values</code>基本一致.</p>
<p><strong>用处一:</strong></p>
<p>将对象转为真正的Map结构</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> obj = &#123; <span class="attr">foo</span>: <span class="string">'bar'</span>, <span class="attr">baz</span>: <span class="number">42</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> map = <span class="keyword">new</span> <span class="built_in">Map</span>(<span class="built_in">Object</span>.entries(obj));</span></code><br><code class="highlight javascript"><span class="line">map <span class="comment">// Map &#123; foo: "bar", baz: 42 &#125;</span></span></code><br></pre></td></tr></table></figure>

<h2 id="Object-fromEntries"><a href="#Object-fromEntries" class="headerlink" title="Object.fromEntries()"></a>Object.fromEntries()</h2><p>该方法是<code>Object.entries()</code>的逆操作.用于将一个键值对数组转为对象.</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.fromEntries([</span></code><br><code class="highlight javascript"><span class="line">  [<span class="string">'foo'</span>, <span class="string">'bar'</span>],</span></code><br><code class="highlight javascript"><span class="line">  [<span class="string">'baz'</span>, <span class="number">42</span>]</span></code><br><code class="highlight javascript"><span class="line">])</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// &#123; foo: "bar", baz: 42 &#125;</span></span></code><br></pre></td></tr></table></figure>

<p>该方法的目的是将键值对的数据结构还原为对象,因此特别适合将Map结构转为对象</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 例一</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> entries = <span class="keyword">new</span> <span class="built_in">Map</span>([</span></code><br><code class="highlight javascript"><span class="line">  [<span class="string">'foo'</span>, <span class="string">'bar'</span>],</span></code><br><code class="highlight javascript"><span class="line">  [<span class="string">'baz'</span>, <span class="number">42</span>]</span></code><br><code class="highlight javascript"><span class="line">]);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.fromEntries(entries)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// &#123; foo: "bar", baz: 42 &#125;</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 例二</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> map = <span class="keyword">new</span> <span class="built_in">Map</span>().set(<span class="string">'foo'</span>, <span class="literal">true</span>).set(<span class="string">'bar'</span>, <span class="literal">false</span>);</span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.fromEntries(map)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// &#123; foo: true, bar: false &#125;</span></span></code><br></pre></td></tr></table></figure>

<p><strong>用处二:</strong></p>
<p>配合<code>URLSearchParams</code>对象,将查询字符串转为对象</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.fromEntries(<span class="keyword">new</span> URLSearchParams(<span class="string">'foo=bar&amp;baz=qux'</span>))</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// &#123; foo: "bar", baz: "qux" &#125;</span></span></code><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>前端开发</category>
        <category>ES6学习</category>
      </categories>
      <tags>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript6--数组的扩展</title>
    <url>/2019/10/18/ECMAScript6/7%E3%80%81%E6%95%B0%E7%BB%84%E7%9A%84%E6%89%A9%E5%B1%95/</url>
    <content><![CDATA[<h1 id="扩展运算符"><a href="#扩展运算符" class="headerlink" title="扩展运算符"></a>扩展运算符</h1><h2 id="含义"><a href="#含义" class="headerlink" title="含义"></a>含义</h2><p>扩展运算符(spread)是三个点( … )<br>用来将一个数组转为用逗号分隔的参数序列</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">console</span>.log(...[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>])</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 1 2 3</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">console</span>.log(<span class="number">1</span>, ...[<span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>], <span class="number">5</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 1 2 3 4 5</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">[...document.querySelectorAll(<span class="string">'div'</span>)]</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// [&lt;div&gt;, &lt;div&gt;, &lt;div&gt;]</span></span></code><br></pre></td></tr></table></figure>

<a id="more"></a>

<p>扩展运算符后面还可以放置表达式:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> arr = [</span></code><br><code class="highlight javascript"><span class="line">  ...(x &gt; <span class="number">0</span> ? [<span class="string">'a'</span>] : []),</span></code><br><code class="highlight javascript"><span class="line">  <span class="string">'b'</span>,</span></code><br><code class="highlight javascript"><span class="line">];</span></code><br></pre></td></tr></table></figure>

<p>注意:</p>
<ul>
<li>只有函数调用时,扩展运算符才可以放在圆括号中,否则会报错</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">(...[<span class="number">1</span>, <span class="number">2</span>])</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// Uncaught SyntaxError: Unexpected number</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">console</span>.log((...[<span class="number">1</span>, <span class="number">2</span>]))</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// Uncaught SyntaxError: Unexpected number</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">console</span>.log(...[<span class="number">1</span>, <span class="number">2</span>])</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 1 2</span></span></code><br></pre></td></tr></table></figure>

<ul>
<li>如果扩展运算符后面是一个空数组,则不产生任何效果</li>
</ul>
<h2 id="替代函数的apply方法"><a href="#替代函数的apply方法" class="headerlink" title="替代函数的apply方法"></a>替代函数的apply方法</h2><p>apply方法的作用是改变this的指向,然后再调用该函数:</p>
<p>第一个参数是this所要指向的那个对象,</p>
<p>第二个参数是一个数组,该数组的所有成员会依次作为参数,传入原函数</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// ES5 的写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">x, y, z</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">// ...</span></span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> args = [<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>];</span></code><br><code class="highlight javascript"><span class="line">f.apply(<span class="literal">null</span>, args);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// ES6的写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">x, y, z</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">// ...</span></span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> args = [<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>];</span></code><br><code class="highlight javascript"><span class="line">f(...args);</span></code><br></pre></td></tr></table></figure>

<h2 id="扩展运算符的应用"><a href="#扩展运算符的应用" class="headerlink" title="扩展运算符的应用"></a>扩展运算符的应用</h2><h3 id="复制数组"><a href="#复制数组" class="headerlink" title="复制数组"></a>复制数组</h3><p>数组是复合的数据类型,直接复制的话,只是复制了指向底层数据结构的指针,而不是克隆一个全新的数组</p>
<p>使用扩展运算符赋值数组:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> a1 = [<span class="number">1</span>, <span class="number">2</span>];</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 写法一</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> a2 = [...a1];</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 写法二</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> [...a2] = a1;</span></code><br></pre></td></tr></table></figure>

<h3 id="合并数组"><a href="#合并数组" class="headerlink" title="合并数组"></a>合并数组</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> arr1 = [<span class="string">'a'</span>, <span class="string">'b'</span>];</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> arr2 = [<span class="string">'c'</span>];</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> arr3 = [<span class="string">'d'</span>, <span class="string">'e'</span>];</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// ES5 的合并数组</span></span></code><br><code class="highlight javascript"><span class="line">arr1.concat(arr2, arr3);</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// [ 'a', 'b', 'c', 'd', 'e' ]</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// ES6 的合并数组</span></span></code><br><code class="highlight javascript"><span class="line">[...arr1, ...arr2, ...arr3]</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// [ 'a', 'b', 'c', 'd', 'e' ]</span></span></code><br></pre></td></tr></table></figure>

<p>上面的两种方法都是浅拷贝,如果修改了原数组的成员,会同步反映到新数组.</p>
<p>“浅拷贝”，指的是如果数组成员包括复合类型的值（比如对象），则新数组拷贝的是该值的引用。</p>
<h3 id="与解构赋值结合"><a href="#与解构赋值结合" class="headerlink" title="与解构赋值结合"></a>与解构赋值结合</h3><p>扩展运算符可以与解构赋值结合起来，用于生成数组。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> [first, ...rest] = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>];</span></code><br><code class="highlight javascript"><span class="line">first <span class="comment">// 1</span></span></code><br><code class="highlight javascript"><span class="line">rest  <span class="comment">// [2, 3, 4, 5]</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> [first, ...rest] = [];</span></code><br><code class="highlight javascript"><span class="line">first <span class="comment">// undefined</span></span></code><br><code class="highlight javascript"><span class="line">rest  <span class="comment">// []</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> [first, ...rest] = [<span class="string">"foo"</span>];</span></code><br><code class="highlight javascript"><span class="line">first  <span class="comment">// "foo"</span></span></code><br><code class="highlight javascript"><span class="line">rest   <span class="comment">// []</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意:如果将扩展运算符用于数组赋值,只能放在参数的最后一位,否则会报错</p>
</blockquote>
<h3 id="字符串"><a href="#字符串" class="headerlink" title="字符串"></a>字符串</h3><p>扩展运算符还可以将字符串转为真正的数组。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">[...<span class="string">'hello'</span>]</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// [ "h", "e", "l", "l", "o" ]</span></span></code><br></pre></td></tr></table></figure>

<p>使用扩展运算符能够正确识别四个字节的Unicode字符</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="string">'x\uD83D\uDE80y'</span>.length <span class="comment">// 4</span></span></code><br><code class="highlight javascript"><span class="line">[...<span class="string">'x\uD83D\uDE80y'</span>].length <span class="comment">// 3</span></span></code><br></pre></td></tr></table></figure>

<h3 id="实现了Iterator接口的对象"><a href="#实现了Iterator接口的对象" class="headerlink" title="实现了Iterator接口的对象"></a>实现了Iterator接口的对象</h3><p>任何定义了遍历器（Iterator）接口的对象，都可以用扩展运算符转为真正的数组。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">//querySelectorAll方法返回的是一个NodeList对象(类似数组的对象),但是</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//NodeList对象实现了Iterator</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> nodeList = <span class="built_in">document</span>.querySelectorAll(<span class="string">'div'</span>);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> array = [...nodeList];</span></code><br></pre></td></tr></table></figure>

<h3 id="Map-和-Set-结构-Generator函数"><a href="#Map-和-Set-结构-Generator函数" class="headerlink" title="Map 和 Set 结构,Generator函数"></a>Map 和 Set 结构,Generator函数</h3><p>扩展运算符内部调用的是数据结构的 Iterator 接口，因此只要具有 Iterator 接口的对象，都可以使用扩展运算符，比如 Map 结构。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> map = <span class="keyword">new</span> <span class="built_in">Map</span>([</span></code><br><code class="highlight javascript"><span class="line">  [<span class="number">1</span>, <span class="string">'one'</span>],</span></code><br><code class="highlight javascript"><span class="line">  [<span class="number">2</span>, <span class="string">'two'</span>],</span></code><br><code class="highlight javascript"><span class="line">  [<span class="number">3</span>, <span class="string">'three'</span>],</span></code><br><code class="highlight javascript"><span class="line">]);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> arr = [...map.keys()]; <span class="comment">// [1, 2, 3]</span></span></code><br></pre></td></tr></table></figure>

<p>Generator 函数运行后，返回一个遍历器对象，因此也可以使用扩展运算符。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> go = <span class="function"><span class="keyword">function</span>*(<span class="params"></span>)</span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="number">2</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">yield</span> <span class="number">3</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">[...go()] <span class="comment">// [1, 2, 3]</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意:如果对没有Iterator接口的对象使用扩展运算符,将会报错</p>
</blockquote>
<h1 id="Array-from"><a href="#Array-from" class="headerlink" title="Array.from()"></a>Array.from()</h1><p>该方法用于将两类对象转为真正的数组:</p>
<ul>
<li>类似数组的对象（array-like object）</li>
</ul>
<p>所谓类似数组的对象,本质特征只有一点,即必须有length属性</p>
<ul>
<li>可遍历（<code>iterable</code>）的对象（包括 ES6 新增的数据结构 <code>Set</code> 和 <code>Map</code>）</li>
</ul>
<p>实际应用中,常见的类似数组的对象:</p>
<ul>
<li>DOM操作返回的<code>NodeList</code>集合</li>
<li>函数内部arguments对象</li>
</ul>
<p>注意:</p>
<ul>
<li>只要是部署了Iterator接口的数据机构,<code>Array.from</code>都能将其转为数组</li>
<li>如果参数是一个真正的数组,<code>Array.from</code>会返回一个一模一样的新数组</li>
<li>没有部署该方法的浏览器,可以用<code>Array.prototype.slice</code>方法替代</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> toArray = <span class="function">(<span class="params">(</span>) =&gt;</span></span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">Array</span>.from ? <span class="built_in">Array</span>.from : <span class="function"><span class="params">obj</span> =&gt;</span> [].slice.call(obj)</span></code><br><code class="highlight javascript"><span class="line">)();</span></code><br></pre></td></tr></table></figure>

<p><code>Array.from</code>还可以接受第二个参数,作用类似于数组的map方法,用来对每个元素进行处理,将处理后的值放入返回的数组</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Array</span>.from(arrayLike, x =&gt; x * x);</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Array</span>.from(arrayLike).map(<span class="function"><span class="params">x</span> =&gt;</span> x * x);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Array</span>.from([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>], (x) =&gt; x * x)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// [1, 4, 9]</span></span></code><br></pre></td></tr></table></figure>

<p>如果map函数里面用到了this关键字,还可以传入<code>Array.from</code>的第三个参数,用来绑定this</p>
<p><code>Array.from()</code>可以将各种值转为真正的数组,并且还可以提供map功能.这实际上意味着，只要有一个原始的数据结构，你就可以先对它的值进行处理，然后转成规范的数组结构，进而就可以使用数量众多的数组方法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Array</span>.from(&#123; <span class="attr">length</span>: <span class="number">2</span> &#125;, () =&gt; <span class="string">'jack'</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// ['jack', 'jack']</span></span></code><br></pre></td></tr></table></figure>

<p>​             <img src="https://qqadapt.qpic.cn/txdocpic/0/1161aee7a561498dd7738319df182162/0" alt="img">             </p>
<h1 id="Array-of"><a href="#Array-of" class="headerlink" title="Array.of()"></a>Array.of()</h1><p>该方法用于将一组值转换为数组<br><code>Array.of()</code>总是返回参数值组成的数组,如果没有参数,就返回一个空数组</p>
<h1 id="数组实例的-copyWithin"><a href="#数组实例的-copyWithin" class="headerlink" title="数组实例的 copyWithin()"></a>数组实例的 copyWithin()</h1><p>该方法在当前数组内部,将指定位置的成员复制到其他位置(会覆盖原有成员),然后返回当前数组(使用这个方法,会修改当前数组)</p>
<p>接受的三个参数:</p>
<ul>
<li>target(必需):从该位置开始替换数据.  如果为负值,表示倒数</li>
<li>start(可选): 从该位置开始读取数据,默认为0.  如果为负值,表示倒数</li>
<li>end(可选):  到该位置前停止读取数据.    默认等于数组长度,  如果为负值,表示倒数</li>
</ul>
<blockquote>
<p>注意: 三个参数都应该是数值, 如果不是,会自动转为数值</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>].copyWithin(<span class="number">0</span>, <span class="number">3</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// [4, 5, 3, 4, 5]</span></span></code><br></pre></td></tr></table></figure>

<p>上面代码表示将从 3 号位直到数组结束的成员（4 和 5），复制到从 0 号位开始的位置，结果覆盖了原来的 1 和 2。</p>
<h1 id="数组实例的find-和-findIndex"><a href="#数组实例的find-和-findIndex" class="headerlink" title="数组实例的find() 和 findIndex()"></a>数组实例的find() 和 findIndex()</h1><p>数组实例的find方法用于找出一个符合条件的数组成员<br>数组实例的<code>findIndex</code>方法与<code>find</code>方法非常类似,返回第一个符合条件的数组成员的位置,如果所有成员都不符合条件,则返回 -1 </p>
<p>参数:  </p>
<ul>
<li>第一个参数:一个回调函数,所有数组成员一次执行该回调函数,直到找出第一个返回值位true的成员,然后返回该成员.  如果没有符合条件的成员,则返回undefined.</li>
</ul>
<p>回调函数可以接受三个参数:</p>
<ul>
<li><p><code>value</code>: 当前的值</p>
</li>
<li><p><code>index</code>: 当前的位置</p>
</li>
<li><p><code>arr</code>: 原数组</p>
</li>
<li><p>第二个参数: 用来绑定回调函数的<code>this</code>对象</p>
</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">v</span>)</span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> v &gt; <span class="keyword">this</span>.age;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> person = &#123;<span class="attr">name</span>: <span class="string">'John'</span>, <span class="attr">age</span>: <span class="number">20</span>&#125;;</span></code><br><code class="highlight javascript"><span class="line">[<span class="number">10</span>, <span class="number">12</span>, <span class="number">26</span>, <span class="number">15</span>].find(f, person);    <span class="comment">// 26</span></span></code><br></pre></td></tr></table></figure>

<p>注意: 这两个方法都可以发现<code>NaN</code>,弥补了数组的<code>indexOf</code>方法的不足</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">[<span class="literal">NaN</span>].indexOf(<span class="literal">NaN</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// -1</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">[<span class="literal">NaN</span>].findIndex(<span class="function"><span class="params">y</span> =&gt;</span> <span class="built_in">Object</span>.is(<span class="literal">NaN</span>, y))</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 0</span></span></code><br></pre></td></tr></table></figure>

<p>上面代码中,<code>indexOf</code>无法识别数组的<code>NaN</code>成员,但是<code>findIndex</code>方法可以借助<code>O</code>bject.is`方法做到</p>
<h1 id="数组实例的fill"><a href="#数组实例的fill" class="headerlink" title="数组实例的fill()"></a>数组实例的fill()</h1><p>fill方法使用给定值,填充一个数组.</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">[<span class="string">'a'</span>, <span class="string">'b'</span>, <span class="string">'c'</span>].fill(<span class="number">7</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// [7, 7, 7]</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">new</span> <span class="built_in">Array</span>(<span class="number">3</span>).fill(<span class="number">7</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// [7, 7, 7]</span></span></code><br></pre></td></tr></table></figure>

<p>参数:</p>
<ul>
<li>第一个参数:  需要填充的值(如果只有改参数,数组中所有的元素都会被该值覆盖)</li>
<li>第二个参数: 填充的起始位置</li>
<li>第三个参数: 填充的结束位置</li>
</ul>
<blockquote>
<p>注意:如果填充的类型为对象,那么被赋值的是同一个内存地址的对象,而不是深拷贝对象</p>
</blockquote>
<h1 id="数组实例的entries-keys-和-values"><a href="#数组实例的entries-keys-和-values" class="headerlink" title="数组实例的entries() , keys() 和 values()"></a>数组实例的entries() , keys() 和 values()</h1><p>这三个方法都是用来遍历数组的:</p>
<ul>
<li>keys(): 是对键名的遍历</li>
<li>values(): 是对键值的遍历</li>
<li>entries() : 是对键值对的遍历</li>
</ul>
<p>它们都返回一个遍历器对象,可以用for…of循环进行遍历</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> index <span class="keyword">of</span> [<span class="string">'a'</span>, <span class="string">'b'</span>].keys()) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(index);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 0</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 1</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> elem <span class="keyword">of</span> [<span class="string">'a'</span>, <span class="string">'b'</span>].values()) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(elem);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 'a'</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 'b'</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> [index, elem] <span class="keyword">of</span> [<span class="string">'a'</span>, <span class="string">'b'</span>].entries()) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(index, elem);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 0 "a"</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 1 "b"</span></span></code><br></pre></td></tr></table></figure>

<p>如果不适用for…of循环,可以手动调用遍历器对象的next方法,进行遍历</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> letter = [<span class="string">'a'</span>, <span class="string">'b'</span>, <span class="string">'c'</span>];</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> entries = letter.entries();</span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">console</span>.log(entries.next().value); <span class="comment">// [0, 'a']</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">console</span>.log(entries.next().value); <span class="comment">// [1, 'b']</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">console</span>.log(entries.next().value); <span class="comment">// [2, 'c']</span></span></code><br></pre></td></tr></table></figure>

<h1 id="数组实例的includes"><a href="#数组实例的includes" class="headerlink" title="数组实例的includes()"></a>数组实例的includes()</h1><p>该方法检查某个数组是否包含给定的值,返回一个布尔值</p>
<p>参数:</p>
<ul>
<li>第一个参数: 要搜索的值</li>
<li>第二个参数: 搜索的起始位置X,  默认为0, 如果为负数,则表示从右往左数 |X| 个位置(从零开始),搜索顺序还是从左到右,如果这时它大于数组长度,则会重置为从0开始</li>
</ul>
<p>没有该方法之前,我们通常使用数组的<code>indexOf</code>方法,检查是否包含某个值,但是该方法有两个缺点:</p>
<ul>
<li>不够语义化(返回的是参数值第一个出现位置,还要去比较是否不等于-1)</li>
<li>由于内部使用严格相等运算符进行判断,这会导致对<code>NaN</code>的误判</li>
</ul>
<p>但是<code>includes</code>不会对<code>NaN</code>有误判<br>下面代码用来检查当前环境是否支持该方法，如果不支持，部署一个简易的替代版本。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> contains = <span class="function">(<span class="params">(</span>) =&gt;</span></span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">Array</span>.prototype.includes</span></code><br><code class="highlight javascript"><span class="line">    ? <span class="function">(<span class="params">arr, value</span>) =&gt;</span> arr.includes(value)</span></code><br><code class="highlight javascript"><span class="line">    : <span class="function">(<span class="params">arr, value</span>) =&gt;</span> arr.some(<span class="function"><span class="params">el</span> =&gt;</span> el === value)</span></code><br><code class="highlight javascript"><span class="line">)();</span></code><br><code class="highlight javascript"><span class="line">contains([<span class="string">'foo'</span>, <span class="string">'bar'</span>], <span class="string">'baz'</span>); <span class="comment">// =&gt; false</span></span></code><br></pre></td></tr></table></figure>

<h1 id="数组实例的-flat-flatMap"><a href="#数组实例的-flat-flatMap" class="headerlink" title="数组实例的 flat(), flatMap()"></a>数组实例的 flat(), flatMap()</h1><h2 id="flat"><a href="#flat" class="headerlink" title="flat()"></a>flat()</h2><p>该方法用于将嵌套的数组”拉平”,变成一维数组,返回一个新数组,对原数据没有影响</p>
<p><strong>参数:</strong></p>
<ul>
<li>一个整数:  表示想要拉平的层数(多层的嵌套数组)</li>
</ul>
<p><strong>注意:</strong></p>
<ul>
<li>如果不管有多少层嵌套，都要转成一维数组，可以用Infinity关键字作为参数</li>
<li>如果原数组有空位,flat()方法会跳过空位</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">[<span class="number">1</span>, <span class="number">2</span>, [<span class="number">3</span>, [<span class="number">4</span>, <span class="number">5</span>]]].flat()</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// [1, 2, 3, [4, 5]]</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">[<span class="number">1</span>, <span class="number">2</span>, [<span class="number">3</span>, [<span class="number">4</span>, <span class="number">5</span>]]].flat(<span class="number">2</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// [1, 2, 3, 4, 5]</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//跳过空位</span></span></code><br><code class="highlight javascript"><span class="line">[<span class="number">1</span>, <span class="number">2</span>, , <span class="number">4</span>, <span class="number">5</span>].flat()</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// [1, 2, 4, 5]</span></span></code><br></pre></td></tr></table></figure>

<h2 id="flatMap"><a href="#flatMap" class="headerlink" title="flatMap()"></a>flatMap()</h2><p>该方法对原数组的每个成员执行一个函数(相当于执行<code>Array.prototype.map()</code>),然后对返回值组成的数组执行<code>flat()</code>方法,返回一个新数组,不改变原数组</p>
<p><strong>该方法的参数:</strong></p>
<ul>
<li>第一个参数: 遍历函数</li>
<li>第二个参数: 用来绑定遍历函数里面的this对象</li>
</ul>
<p><strong>遍历函数可以接受的三个参数:</strong></p>
<ul>
<li>第一个参数: 当前数组成员</li>
<li>第二个参数: 当前数组成员的位置</li>
<li>第三个参数: 原数组</li>
</ul>
<blockquote>
<p>注意:<code>flatMap()</code>只能展开一层数组</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 相当于 [[2, 4], [3, 6], [4, 8]].flat()</span></span></code><br><code class="highlight javascript"><span class="line">[<span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>].flatMap(<span class="function">(<span class="params">x</span>) =&gt;</span> [x, x * <span class="number">2</span>])</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// [2, 4, 3, 6, 4, 8]</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 相当于 [[[2]], [[4]], [[6]], [[8]]].flat()</span></span></code><br><code class="highlight javascript"><span class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>].flatMap(<span class="function"><span class="params">x</span> =&gt;</span> [[x * <span class="number">2</span>]])</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// [[2], [4], [6], [8]]</span></span></code><br></pre></td></tr></table></figure>

<h1 id="数组的空位"><a href="#数组的空位" class="headerlink" title="数组的空位"></a>数组的空位</h1><p>数组的空位指: 数组的某一个位置没有任何值  (比如Array构造函数返回的数组都是空位)</p>
<blockquote>
<p>注意:  空位不是undefined , 一个位置的值等于undefined,依然是有值的 . 空位没有任何值,in运算符可以说明这一点.</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="number">0</span> <span class="keyword">in</span> [<span class="literal">undefined</span>, <span class="literal">undefined</span>, <span class="literal">undefined</span>] <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line"><span class="number">0</span> <span class="keyword">in</span> [, , ,] <span class="comment">// false</span></span></code><br></pre></td></tr></table></figure>

<p>ES5对空位的处理很不一致,大多数情况下会忽略空位:</p>
<ul>
<li><code>forEach()</code>,<code>filter()</code>, <code>reduce()</code>, <code>every()</code> 和<code>some()</code>都会跳过空位。</li>
<li><code>map()</code>会跳过空位，但会保留这个值</li>
<li><code>join()</code>和<code>toString()</code>会将空位视为<code>undefined</code>，而<code>undefined</code>和<code>null</code>会被处理成空字符串。</li>
</ul>
<p>ES6则是明确规定将空位转为<code>undefined</code></p>
<ul>
<li><code>Array.from</code>方法会将数组的空位转为<code>undefined</code></li>
<li>扩展运算符( … )会将空位转为<code>undefined</code></li>
<li><code>copyWithin()</code>会连空位一起拷贝</li>
<li><code>fill()</code>会将空位视为正常的数组位置</li>
<li><code>for...of</code> 循环也会遍历空位</li>
<li><code>entries()  keys() values()  find()和findIndex会将空位处理成undefined</code></li>
</ul>
]]></content>
      <categories>
        <category>前端开发</category>
        <category>ES6学习</category>
      </categories>
      <tags>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript6--函数的扩展</title>
    <url>/2019/10/18/ECMAScript6/6%E3%80%81%E5%87%BD%E6%95%B0%E7%9A%84%E6%89%A9%E5%B1%95/</url>
    <content><![CDATA[<h1 id="函数参数的默认值"><a href="#函数参数的默认值" class="headerlink" title="函数参数的默认值"></a>函数参数的默认值</h1><h2 id="基本用法"><a href="#基本用法" class="headerlink" title="基本用法"></a>基本用法</h2><p>ES6允许为函数的参数设置默认值,即直接写在参数定义的后面</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">log</span>(<span class="params">x, y = <span class="string">'World'</span></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(x, y);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">log(<span class="string">'Hello'</span>) <span class="comment">// Hello World</span></span></code><br><code class="highlight javascript"><span class="line">log(<span class="string">'Hello'</span>, <span class="string">'China'</span>) <span class="comment">// Hello China</span></span></code><br><code class="highlight javascript"><span class="line">log(<span class="string">'Hello'</span>, <span class="string">''</span>) <span class="comment">// Hello</span></span></code><br></pre></td></tr></table></figure>

<a id="more"></a>

<p>注意:</p>
<ul>
<li>参数变量是默认声明的,不能用<code>let</code>或<code>const</code>再次声明,否则会报错</li>
<li>使用参数默认值时,函数不能有同名参数</li>
</ul>
<h2 id="与解构赋值默认值结合使用"><a href="#与解构赋值默认值结合使用" class="headerlink" title="与解构赋值默认值结合使用"></a>与解构赋值默认值结合使用</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">&#123;x, y = <span class="number">5</span>&#125;</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(x, y);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">foo(&#123;&#125;) <span class="comment">// undefined 5</span></span></code><br><code class="highlight javascript"><span class="line">foo(&#123;<span class="attr">x</span>: <span class="number">1</span>&#125;) <span class="comment">// 1 5</span></span></code><br><code class="highlight javascript"><span class="line">foo(&#123;<span class="attr">x</span>: <span class="number">1</span>, <span class="attr">y</span>: <span class="number">2</span>&#125;) <span class="comment">// 1 2</span></span></code><br><code class="highlight javascript"><span class="line">foo() <span class="comment">// TypeError: Cannot read property 'x' of undefined</span></span></code><br></pre></td></tr></table></figure>

<p>上面代码只使用了解构赋值默认值,并没有使用函数参数的默认值,所以函数不传参的情况下会报错</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">&#123;x, y = <span class="number">5</span>&#125; = &#123;&#125;</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(x, y);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">foo() <span class="comment">// undefined 5</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>函数的参数默认为一个空对象</p>
</blockquote>
<h2 id="函数的length属性"><a href="#函数的length属性" class="headerlink" title="函数的length属性"></a>函数的length属性</h2><p>制定了默认值以后,函数的length属性,将返回没有指定默认值的参数的个数,  即指定了默认值后,length属性将失真</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params">a</span>) </span>&#123;&#125;).length <span class="comment">// 1</span></span></code><br><code class="highlight javascript"><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params">a = <span class="number">5</span></span>) </span>&#123;&#125;).length <span class="comment">// 0</span></span></code><br><code class="highlight javascript"><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params">a, b, c = <span class="number">5</span></span>) </span>&#123;&#125;).length <span class="comment">// 2</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意:</p>
<ul>
<li><p>rest参数也不会计入length属性<br><code>(function(...args) {}).length // 0</code></p>
</li>
<li><p>如果设置了默认值的参数不是尾参数,那么length属性也不再计入后面的参数了</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params">a = <span class="number">0</span>, b, c</span>) </span>&#123;&#125;).length <span class="comment">// 0</span></span></code><br><code class="highlight javascript"><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params">a, b = <span class="number">1</span>, c</span>) </span>&#123;&#125;).length <span class="comment">// 1</span></span></code><br></pre></td></tr></table></figure>
</li>
</ul>
</blockquote>
<h2 id="作用域"><a href="#作用域" class="headerlink" title="作用域"></a>作用域</h2><p>一旦设置了参数的默认值，函数进行声明初始化时，参数会形成一个单独的作用域（context）。等到初始化结束，这个作用域就会消失。这种语法行为，在不设置参数默认值时，是不会出现的。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> x = <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">x, y = x</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(y);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">f(<span class="number">2</span>) <span class="comment">// 2</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//调用函数f时,默认值指向的是第一个参数x,而不是全局变量x</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//即圆括号内形成一个单独的作用域</span></span></code><br></pre></td></tr></table></figure>

<h2 id="应用"><a href="#应用" class="headerlink" title="应用"></a>应用</h2><p>利用参数默认值，可以指定某一个参数不得省略，如果省略就抛出一个错误。</p>
<p>即执行一个函数</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">throwIfMissing</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'Missing parameter'</span>);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">mustBeProvided = throwIfMissing(</span>)) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> mustBeProvided;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">foo()</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// Error: Missing parameter</span></span></code><br></pre></td></tr></table></figure>

<p>另外,可以将参数默认值设为undefined,表明这个参数是可以省略的</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">optional = undefined</span>) </span>&#123; ··· &#125;</span></code><br></pre></td></tr></table></figure>

<h1 id="rest参数"><a href="#rest参数" class="headerlink" title="rest参数"></a>rest参数</h1><p>ES6引入rest参数(形式为 …变量名),用来获取函数的多余参数,这样就不需要使用arguments对象了</p>
<p>rest参数搭配的变量是一个数组,该变量将多余的参数放入数组中</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">add</span>(<span class="params">...values</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">let</span> sum = <span class="number">0</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">for</span> (<span class="keyword">var</span> val <span class="keyword">of</span> values) &#123;</span></code><br><code class="highlight javascript"><span class="line">    sum += val;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> sum;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">add(<span class="number">2</span>, <span class="number">5</span>, <span class="number">3</span>) <span class="comment">// 10</span></span></code><br></pre></td></tr></table></figure>

<p><strong>注意:</strong></p>
<ul>
<li>rest参数之后不能再有其他参数(即只能是最后一个参数),否则会报错</li>
<li>函数的length属性,不包括rest参数</li>
</ul>
<h1 id="严格模式"><a href="#严格模式" class="headerlink" title="严格模式"></a>严格模式</h1><p>ES2016中规定,只要函数参数使用了默认值,解构赋值或者扩展运算符,那么函数内部就不能显示设定为严格模式,否则会报错</p>
<h1 id="name属性"><a href="#name属性" class="headerlink" title="name属性"></a>name属性</h1><p>函数的name属性,返回该函数的函数名.</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;&#125;</span></code><br><code class="highlight javascript"><span class="line">foo.name <span class="comment">// "foo"</span></span></code><br></pre></td></tr></table></figure>

<p><strong>注意:</strong></p>
<ul>
<li>如果将一个匿名函数赋值给一个变量,会返回实际的函数名(变量名)</li>
<li>如果将一个具名函数赋值给一个变量,会返回这个具名函数原本的名字</li>
<li>Function构造函数返回的函数实例,name属性的值为anonymous</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">(<span class="keyword">new</span> <span class="built_in">Function</span>).name <span class="comment">// "anonymous"</span></span></code><br></pre></td></tr></table></figure>

<ul>
<li>bind返回的函数,name属性值上会加上bound前缀</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line">foo.bind(&#123;&#125;).name <span class="comment">// "bound foo"</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;&#125;).bind(&#123;&#125;).name <span class="comment">// "bound "</span></span></code><br></pre></td></tr></table></figure>

<h1 id="箭头函数"><a href="#箭头函数" class="headerlink" title="箭头函数"></a>箭头函数</h1><h2 id="基本用法-1"><a href="#基本用法-1" class="headerlink" title="基本用法"></a>基本用法</h2><p>ES6允许使用”箭头”(<code>=&gt;</code>)自定义函数</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> f = <span class="function"><span class="params">v</span> =&gt;</span> v;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> f = <span class="function"><span class="keyword">function</span> (<span class="params">v</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> v;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<p>如果箭头函数不需要参数或需要多个参数,就是用一个圆括号代表参数部分</p>
<p>如果箭头函数的代码块部分只有一条语句且有返回值,则可以直接省略大括号,直接写返回值</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> f = <span class="function"><span class="params">()</span> =&gt;</span> <span class="number">5</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> f = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123; <span class="keyword">return</span> <span class="number">5</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> sum = <span class="function">(<span class="params">num1, num2</span>) =&gt;</span> num1 + num2;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> sum = <span class="function"><span class="keyword">function</span>(<span class="params">num1, num2</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> num1 + num2;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<p>如果箭头函数直接返回一个对象,必须在对象外面加上括号,否则会报错</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 报错</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> getTempItem = <span class="function"><span class="params">id</span> =&gt;</span> &#123; <span class="attr">id</span>: id, <span class="attr">name</span>: <span class="string">"Temp"</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 不报错</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> getTempItem = <span class="function"><span class="params">id</span> =&gt;</span> (&#123; <span class="attr">id</span>: id, <span class="attr">name</span>: <span class="string">"Temp"</span> &#125;);</span></code><br></pre></td></tr></table></figure>

<p>如果箭头函数只有一行语句,且不需要返回值,可以采用下面的写法:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> fn = <span class="function"><span class="params">()</span> =&gt;</span> <span class="keyword">void</span> doesNotReturn();</span></code><br></pre></td></tr></table></figure>

<p>如果箭头函数的代码块部分多于一条语句,就要使用大括号括起来,并且使用return语句返回</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> sum = <span class="function">(<span class="params">num1, num2</span>) =&gt;</span> &#123; <span class="keyword">return</span> num1 + num2; &#125;</span></code><br></pre></td></tr></table></figure>

<h2 id="使用注意点"><a href="#使用注意点" class="headerlink" title="使用注意点"></a>使用注意点</h2><p>箭头函数有几个使用注意点:</p>
<ul>
<li>函数体内的this对象,就是定义时所在的对象,而不是使用时所在的对象</li>
<li>不可以当作构造函数</li>
<li>不可以使用arguments对象,该对象在函数体内不存在,  可以使用rest参数代替</li>
<li>不可以使用yield命令,因此箭头函数不能用作Generator</li>
</ul>
<p>因为箭头函数内部没有自己的this,导致内部的this就是外层代码块的this</p>
<p>下面三个变量在箭头函数之中也是不存在的,指向外层函数的对应变量:</p>
<ul>
<li><code>arguments</code></li>
<li><code>super</code></li>
<li><code>new.target</code></li>
</ul>
<h2 id="不适用场合"><a href="#不适用场合" class="headerlink" title="不适用场合"></a>不适用场合</h2><p>一.定义对象的方法,且该方法内部包括this<br>    因为对象不构成单独的作用域,所以对象内部的箭头函数的this会指向全局作用域</p>
<p>二.需要动态this的时候,也不应使用箭头函数</p>
<p>三.如果函数体很复杂，有许多行，或者函数内部有大量的读写操作，不单纯是为了计算值，这时也不应该使用箭头函数，    而是要使用普通函数，这样可以提高代码可读性。</p>
<h2 id="嵌套的箭头函数"><a href="#嵌套的箭头函数" class="headerlink" title="嵌套的箭头函数"></a>嵌套的箭头函数</h2><p>箭头函数的内部,还可以再使用箭头函数</p>
<h1 id="尾调用优化"><a href="#尾调用优化" class="headerlink" title="尾调用优化"></a>尾调用优化</h1><p>尾调用就是值某个函数的最后一步是调用另一个函数</p>
<p>以下三种情况,都不属于尾调用:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 情况一</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">x</span>)</span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">let</span> y = g(x);</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> y;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 情况二</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">x</span>)</span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> g(x) + <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 情况三</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">x</span>)</span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  g(x);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//情况三等同于下面的代码:</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">x</span>)</span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  g(x);</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="literal">undefined</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<h2 id="尾调用优化-1"><a href="#尾调用优化-1" class="headerlink" title="尾调用优化"></a>尾调用优化</h2><p>[严格模式下生效]<br>函数调用会在内存形成一个”调用记录”,又称为”调用帧”,保存调用位置和内部变量等信息.</p>
<p>如果在函数A的内部调用函数B,那么在A的调用帧上方,还会形成一个B的调用帧.等到B运行结束,将结果返回到A,B的调用帧才会消失,如果函数B内部还调用函数C,那就还有一个C的调用帧,以此类推.所有的调用帧就形成一个”调用栈”</p>
<p>尾调用由于是函数的最后一步操作,所以不需要保留外层函数的调用帧,因为调用位置,内部变量等信息都不会在用到了,只要直接用内层函数的调用帧,取代外层函数的调用帧就可以了.  </p>
<p>“尾调用优化”,即只保留内层函数的调用帧,如果所有函数都是尾调用,那么完全可以做到每次执行时,调用帧只有一项,这将大大节省内存.</p>
<blockquote>
<p>注意:只有不再用到外层函数的内部变量，内层函数的调用帧才会取代外层函数的调用帧，否则就无法进行“尾调用优化”。</p>
</blockquote>
<h2 id="尾递归"><a href="#尾递归" class="headerlink" title="尾递归"></a>尾递归</h2><p>[严格模式下生效]</p>
<p>函数调用自身，称为递归。如果尾调用自身，就称为尾递归。</p>
<p>递归非常消耗内存,因为需要同时保存成千上百个调用帧,很容易发生”栈溢出”错误(stack overflow)</p>
<p>但对于尾递归来说,由于只存在一个调用帧,所以永远不会发生”栈溢出”错误</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">factorial</span>(<span class="params">n, total</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">if</span> (n === <span class="number">1</span>) <span class="keyword">return</span> total;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> factorial(n - <span class="number">1</span>, n * total);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">factorial(<span class="number">5</span>, <span class="number">1</span>) <span class="comment">// 120</span></span></code><br></pre></td></tr></table></figure>

<h2 id="递归函数的改写"><a href="#递归函数的改写" class="headerlink" title="递归函数的改写"></a>递归函数的改写</h2><p>尾递归的实现，往往需要改写递归函数，确保最后一步只调用自身。做到这一点的方法，就是把所有用到的内部变量改写成函数的参数。但是这样做的缺点就是不太直观,第一眼很难看出来</p>
<p><strong>解决方法一:</strong></p>
<p>在尾递归函数之外，再提供一个正常形式的函数。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">tailFactorial</span>(<span class="params">n, total</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">if</span> (n === <span class="number">1</span>) <span class="keyword">return</span> total;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> tailFactorial(n - <span class="number">1</span>, n * total);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">factorial</span>(<span class="params">n</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> tailFactorial(n, <span class="number">1</span>);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">factorial(<span class="number">5</span>) <span class="comment">// 120</span></span></code><br></pre></td></tr></table></figure>

<p>函数式编程中 柯里化(currying) 的概念:</p>
<p>意思是将多参数的函数转换成单参数的形式</p>
<p><strong>解决方法二:</strong></p>
<p>采用ES6的函数默认值</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">factorial</span>(<span class="params">n, total = <span class="number">1</span></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">if</span> (n === <span class="number">1</span>) <span class="keyword">return</span> total;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> factorial(n - <span class="number">1</span>, n * total);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">factorial(<span class="number">5</span>) <span class="comment">// 120</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>总结: 递归本质上是一种循环操作。纯粹的函数式编程语言没有循环操作命令，所有的循环都用递归实现</p>
</blockquote>
<h2 id="严格模式-1"><a href="#严格模式-1" class="headerlink" title="严格模式"></a>严格模式</h2><p>ES6 的尾调用优化只在严格模式下开启，正常模式是无效的。<br>正常模式下,函数内部有两个变量,可以跟踪函数的调用栈</p>
<ul>
<li><code>func.arguments</code>:返回调用时函数的参数</li>
<li><code>func.caller</code>: 返回调用时当前函数的那个函数</li>
</ul>
<p>尾调用优化发生时，函数的调用栈会改写，因此上面两个变量就会失真。严格模式禁用这两个变量，所以尾调用模式仅在严格模式下生效。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">restricted</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line"><span class="meta">  'use strict'</span>;</span></code><br><code class="highlight javascript"><span class="line">  restricted.caller;    <span class="comment">// 报错</span></span></code><br><code class="highlight javascript"><span class="line">  restricted.arguments; <span class="comment">// 报错</span></span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line">restricted();</span></code><br></pre></td></tr></table></figure>

<h2 id="尾递归优化的实现"><a href="#尾递归优化的实现" class="headerlink" title="尾递归优化的实现"></a>尾递归优化的实现</h2><p>在正常模式下实现尾递归优化</p>
<p>原理: 用“循环”换掉“递归”,减少调用栈</p>
<p><strong>方法一:</strong></p>
<p>蹦床函数(将递归执行转为循环执行) </p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">trampoline</span>(<span class="params">f</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">while</span> (f &amp;&amp; f <span class="keyword">instanceof</span> <span class="built_in">Function</span>) &#123;</span></code><br><code class="highlight javascript"><span class="line">    f = f();</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> f;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//接受一个函数f作为参数,只要f执行后返回一个函数,就继续执行,否则返回值</span></span></code><br></pre></td></tr></table></figure>

<p><strong>方法二:</strong></p>
<p>方法一的蹦床函数并非真正的尾递归优化</p>
<p>下面的才是:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">tco</span>(<span class="params">f</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">var</span> value;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">var</span> active = <span class="literal">false</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">var</span> accumulated = [];</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span> <span class="title">accumulator</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">    accumulated.push(<span class="built_in">arguments</span>);</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">if</span> (!active) &#123;</span></code><br><code class="highlight javascript"><span class="line">      active = <span class="literal">true</span>;</span></code><br><code class="highlight javascript"><span class="line">      <span class="keyword">while</span> (accumulated.length) &#123;</span></code><br><code class="highlight javascript"><span class="line">        value = f.apply(<span class="keyword">this</span>, accumulated.shift());</span></code><br><code class="highlight javascript"><span class="line">      &#125;</span></code><br><code class="highlight javascript"><span class="line">      active = <span class="literal">false</span>;</span></code><br><code class="highlight javascript"><span class="line">      <span class="keyword">return</span> value;</span></code><br><code class="highlight javascript"><span class="line">    &#125;</span></code><br><code class="highlight javascript"><span class="line">  &#125;;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> sum = tco(<span class="function"><span class="keyword">function</span>(<span class="params">x, y</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">if</span> (y &gt; <span class="number">0</span>) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> sum(x + <span class="number">1</span>, y - <span class="number">1</span>)</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">else</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> x</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">sum(<span class="number">1</span>, <span class="number">100000</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 100001</span></span></code><br></pre></td></tr></table></figure>

<p>上面的代码中,<code>tco</code>函数时尾递归优化的实现,它的奥妙就在于状态变量<code>active</code></p>
<h1 id="函数参数的尾逗号"><a href="#函数参数的尾逗号" class="headerlink" title="函数参数的尾逗号"></a>函数参数的尾逗号</h1><p>以前不允许最后一个参数后面出现逗号,这样对以后的修改很造成麻烦<br>ES2017 允许函数的最后一个参数有尾逗号</p>
]]></content>
      <categories>
        <category>前端开发</category>
        <category>ES6学习</category>
      </categories>
      <tags>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript6--函数的扩展</title>
    <url>/2019/10/18/ECMAScript6/8%E3%80%81%E5%87%BD%E6%95%B0%E7%9A%84%E6%89%A9%E5%B1%95/</url>
    <content><![CDATA[<h1 id="函数参数的默认值"><a href="#函数参数的默认值" class="headerlink" title="函数参数的默认值"></a>函数参数的默认值</h1><h2 id="基本用法"><a href="#基本用法" class="headerlink" title="基本用法"></a>基本用法</h2><p>ES6允许为函数的参数设置默认值,即直接写在参数定义的后面</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">log</span>(<span class="params">x, y = <span class="string">'World'</span></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(x, y);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">log(<span class="string">'Hello'</span>) <span class="comment">// Hello World</span></span></code><br><code class="highlight javascript"><span class="line">log(<span class="string">'Hello'</span>, <span class="string">'China'</span>) <span class="comment">// Hello China</span></span></code><br><code class="highlight javascript"><span class="line">log(<span class="string">'Hello'</span>, <span class="string">''</span>) <span class="comment">// Hello</span></span></code><br></pre></td></tr></table></figure>

<a id="more"></a>

<p>注意:</p>
<ul>
<li>参数变量是默认声明的,不能用<code>let</code>或<code>const</code>再次声明,否则会报错</li>
<li>使用参数默认值时,函数不能有同名参数</li>
</ul>
<h2 id="与解构赋值默认值结合使用"><a href="#与解构赋值默认值结合使用" class="headerlink" title="与解构赋值默认值结合使用"></a>与解构赋值默认值结合使用</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">&#123;x, y = <span class="number">5</span>&#125;</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(x, y);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">foo(&#123;&#125;) <span class="comment">// undefined 5</span></span></code><br><code class="highlight javascript"><span class="line">foo(&#123;<span class="attr">x</span>: <span class="number">1</span>&#125;) <span class="comment">// 1 5</span></span></code><br><code class="highlight javascript"><span class="line">foo(&#123;<span class="attr">x</span>: <span class="number">1</span>, <span class="attr">y</span>: <span class="number">2</span>&#125;) <span class="comment">// 1 2</span></span></code><br><code class="highlight javascript"><span class="line">foo() <span class="comment">// TypeError: Cannot read property 'x' of undefined</span></span></code><br></pre></td></tr></table></figure>

<p>上面代码只使用了解构赋值默认值,并没有使用函数参数的默认值,所以函数不传参的情况下会报错</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">&#123;x, y = <span class="number">5</span>&#125; = &#123;&#125;</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(x, y);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">foo() <span class="comment">// undefined 5</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>函数的参数默认为一个空对象</p>
</blockquote>
<h2 id="函数的length属性"><a href="#函数的length属性" class="headerlink" title="函数的length属性"></a>函数的length属性</h2><p>制定了默认值以后,函数的length属性,将返回没有指定默认值的参数的个数,  即指定了默认值后,length属性将失真</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params">a</span>) </span>&#123;&#125;).length <span class="comment">// 1</span></span></code><br><code class="highlight javascript"><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params">a = <span class="number">5</span></span>) </span>&#123;&#125;).length <span class="comment">// 0</span></span></code><br><code class="highlight javascript"><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params">a, b, c = <span class="number">5</span></span>) </span>&#123;&#125;).length <span class="comment">// 2</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意:</p>
<ul>
<li><p>rest参数也不会计入length属性<br><code>(function(...args) {}).length // 0</code></p>
</li>
<li><p>如果设置了默认值的参数不是尾参数,那么length属性也不再计入后面的参数了</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params">a = <span class="number">0</span>, b, c</span>) </span>&#123;&#125;).length <span class="comment">// 0</span></span></code><br><code class="highlight javascript"><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params">a, b = <span class="number">1</span>, c</span>) </span>&#123;&#125;).length <span class="comment">// 1</span></span></code><br></pre></td></tr></table></figure>
</li>
</ul>
</blockquote>
<h2 id="作用域"><a href="#作用域" class="headerlink" title="作用域"></a>作用域</h2><p>一旦设置了参数的默认值，函数进行声明初始化时，参数会形成一个单独的作用域（context）。等到初始化结束，这个作用域就会消失。这种语法行为，在不设置参数默认值时，是不会出现的。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> x = <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">x, y = x</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(y);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">f(<span class="number">2</span>) <span class="comment">// 2</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//调用函数f时,默认值指向的是第一个参数x,而不是全局变量x</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//即圆括号内形成一个单独的作用域</span></span></code><br></pre></td></tr></table></figure>

<h2 id="应用"><a href="#应用" class="headerlink" title="应用"></a>应用</h2><p>利用参数默认值，可以指定某一个参数不得省略，如果省略就抛出一个错误。</p>
<p>即执行一个函数</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">throwIfMissing</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'Missing parameter'</span>);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">mustBeProvided = throwIfMissing(</span>)) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> mustBeProvided;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">foo()</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// Error: Missing parameter</span></span></code><br></pre></td></tr></table></figure>

<p>另外,可以将参数默认值设为undefined,表明这个参数是可以省略的</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">optional = undefined</span>) </span>&#123; ··· &#125;</span></code><br></pre></td></tr></table></figure>

<h1 id="rest参数"><a href="#rest参数" class="headerlink" title="rest参数"></a>rest参数</h1><p>ES6引入rest参数(形式为 …变量名),用来获取函数的多余参数,这样就不需要使用arguments对象了</p>
<p>rest参数搭配的变量是一个数组,该变量将多余的参数放入数组中</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">add</span>(<span class="params">...values</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">let</span> sum = <span class="number">0</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">for</span> (<span class="keyword">var</span> val <span class="keyword">of</span> values) &#123;</span></code><br><code class="highlight javascript"><span class="line">    sum += val;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> sum;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">add(<span class="number">2</span>, <span class="number">5</span>, <span class="number">3</span>) <span class="comment">// 10</span></span></code><br></pre></td></tr></table></figure>

<p><strong>注意:</strong></p>
<ul>
<li>rest参数之后不能再有其他参数(即只能是最后一个参数),否则会报错</li>
<li>函数的length属性,不包括rest参数</li>
</ul>
<h1 id="严格模式"><a href="#严格模式" class="headerlink" title="严格模式"></a>严格模式</h1><p>ES2016中规定,只要函数参数使用了默认值,解构赋值或者扩展运算符,那么函数内部就不能显示设定为严格模式,否则会报错</p>
<h1 id="name属性"><a href="#name属性" class="headerlink" title="name属性"></a>name属性</h1><p>函数的name属性,返回该函数的函数名.</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;&#125;</span></code><br><code class="highlight javascript"><span class="line">foo.name <span class="comment">// "foo"</span></span></code><br></pre></td></tr></table></figure>

<p><strong>注意:</strong></p>
<ul>
<li>如果将一个匿名函数赋值给一个变量,会返回实际的函数名(变量名)</li>
<li>如果将一个具名函数赋值给一个变量,会返回这个具名函数原本的名字</li>
<li>Function构造函数返回的函数实例,name属性的值为anonymous</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">(<span class="keyword">new</span> <span class="built_in">Function</span>).name <span class="comment">// "anonymous"</span></span></code><br></pre></td></tr></table></figure>

<ul>
<li>bind返回的函数,name属性值上会加上bound前缀</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line">foo.bind(&#123;&#125;).name <span class="comment">// "bound foo"</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;&#125;).bind(&#123;&#125;).name <span class="comment">// "bound "</span></span></code><br></pre></td></tr></table></figure>

<h1 id="箭头函数"><a href="#箭头函数" class="headerlink" title="箭头函数"></a>箭头函数</h1><h2 id="基本用法-1"><a href="#基本用法-1" class="headerlink" title="基本用法"></a>基本用法</h2><p>ES6允许使用”箭头”(<code>=&gt;</code>)自定义函数</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> f = <span class="function"><span class="params">v</span> =&gt;</span> v;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> f = <span class="function"><span class="keyword">function</span> (<span class="params">v</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> v;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<p>如果箭头函数不需要参数或需要多个参数,就是用一个圆括号代表参数部分</p>
<p>如果箭头函数的代码块部分只有一条语句且有返回值,则可以直接省略大括号,直接写返回值</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> f = <span class="function"><span class="params">()</span> =&gt;</span> <span class="number">5</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> f = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123; <span class="keyword">return</span> <span class="number">5</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> sum = <span class="function">(<span class="params">num1, num2</span>) =&gt;</span> num1 + num2;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> sum = <span class="function"><span class="keyword">function</span>(<span class="params">num1, num2</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> num1 + num2;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<p>如果箭头函数直接返回一个对象,必须在对象外面加上括号,否则会报错</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 报错</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> getTempItem = <span class="function"><span class="params">id</span> =&gt;</span> &#123; <span class="attr">id</span>: id, <span class="attr">name</span>: <span class="string">"Temp"</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 不报错</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> getTempItem = <span class="function"><span class="params">id</span> =&gt;</span> (&#123; <span class="attr">id</span>: id, <span class="attr">name</span>: <span class="string">"Temp"</span> &#125;);</span></code><br></pre></td></tr></table></figure>

<p>如果箭头函数只有一行语句,且不需要返回值,可以采用下面的写法:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> fn = <span class="function"><span class="params">()</span> =&gt;</span> <span class="keyword">void</span> doesNotReturn();</span></code><br></pre></td></tr></table></figure>

<p>如果箭头函数的代码块部分多于一条语句,就要使用大括号括起来,并且使用return语句返回</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> sum = <span class="function">(<span class="params">num1, num2</span>) =&gt;</span> &#123; <span class="keyword">return</span> num1 + num2; &#125;</span></code><br></pre></td></tr></table></figure>

<h2 id="使用注意点"><a href="#使用注意点" class="headerlink" title="使用注意点"></a>使用注意点</h2><p>箭头函数有几个使用注意点:</p>
<ul>
<li>函数体内的this对象,就是定义时所在的对象,而不是使用时所在的对象</li>
<li>不可以当作构造函数</li>
<li>不可以使用arguments对象,该对象在函数体内不存在,  可以使用rest参数代替</li>
<li>不可以使用yield命令,因此箭头函数不能用作Generator</li>
</ul>
<p>因为箭头函数内部没有自己的this,导致内部的this就是外层代码块的this</p>
<p>下面三个变量在箭头函数之中也是不存在的,指向外层函数的对应变量:</p>
<ul>
<li><code>arguments</code></li>
<li><code>super</code></li>
<li><code>new.target</code></li>
</ul>
<h2 id="不适用场合"><a href="#不适用场合" class="headerlink" title="不适用场合"></a>不适用场合</h2><p>一.定义对象的方法,且该方法内部包括this<br>    因为对象不构成单独的作用域,所以对象内部的箭头函数的this会指向全局作用域</p>
<p>二.需要动态this的时候,也不应使用箭头函数</p>
<p>三.如果函数体很复杂，有许多行，或者函数内部有大量的读写操作，不单纯是为了计算值，这时也不应该使用箭头函数，    而是要使用普通函数，这样可以提高代码可读性。</p>
<h2 id="嵌套的箭头函数"><a href="#嵌套的箭头函数" class="headerlink" title="嵌套的箭头函数"></a>嵌套的箭头函数</h2><p>箭头函数的内部,还可以再使用箭头函数</p>
<h1 id="尾调用优化"><a href="#尾调用优化" class="headerlink" title="尾调用优化"></a>尾调用优化</h1><p>尾调用就是值某个函数的最后一步是调用另一个函数</p>
<p>以下三种情况,都不属于尾调用:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 情况一</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">x</span>)</span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">let</span> y = g(x);</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> y;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 情况二</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">x</span>)</span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> g(x) + <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 情况三</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">x</span>)</span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  g(x);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//情况三等同于下面的代码:</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">x</span>)</span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  g(x);</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="literal">undefined</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<h2 id="尾调用优化-1"><a href="#尾调用优化-1" class="headerlink" title="尾调用优化"></a>尾调用优化</h2><p>[严格模式下生效]<br>函数调用会在内存形成一个”调用记录”,又称为”调用帧”,保存调用位置和内部变量等信息.</p>
<p>如果在函数A的内部调用函数B,那么在A的调用帧上方,还会形成一个B的调用帧.等到B运行结束,将结果返回到A,B的调用帧才会消失,如果函数B内部还调用函数C,那就还有一个C的调用帧,以此类推.所有的调用帧就形成一个”调用栈”</p>
<p>尾调用由于是函数的最后一步操作,所以不需要保留外层函数的调用帧,因为调用位置,内部变量等信息都不会在用到了,只要直接用内层函数的调用帧,取代外层函数的调用帧就可以了.  </p>
<p>“尾调用优化”,即只保留内层函数的调用帧,如果所有函数都是尾调用,那么完全可以做到每次执行时,调用帧只有一项,这将大大节省内存.</p>
<blockquote>
<p>注意:只有不再用到外层函数的内部变量，内层函数的调用帧才会取代外层函数的调用帧，否则就无法进行“尾调用优化”。</p>
</blockquote>
<h2 id="尾递归"><a href="#尾递归" class="headerlink" title="尾递归"></a>尾递归</h2><p>[严格模式下生效]</p>
<p>函数调用自身，称为递归。如果尾调用自身，就称为尾递归。</p>
<p>递归非常消耗内存,因为需要同时保存成千上百个调用帧,很容易发生”栈溢出”错误(stack overflow)</p>
<p>但对于尾递归来说,由于只存在一个调用帧,所以永远不会发生”栈溢出”错误</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">factorial</span>(<span class="params">n, total</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">if</span> (n === <span class="number">1</span>) <span class="keyword">return</span> total;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> factorial(n - <span class="number">1</span>, n * total);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">factorial(<span class="number">5</span>, <span class="number">1</span>) <span class="comment">// 120</span></span></code><br></pre></td></tr></table></figure>

<h2 id="递归函数的改写"><a href="#递归函数的改写" class="headerlink" title="递归函数的改写"></a>递归函数的改写</h2><p>尾递归的实现，往往需要改写递归函数，确保最后一步只调用自身。做到这一点的方法，就是把所有用到的内部变量改写成函数的参数。但是这样做的缺点就是不太直观,第一眼很难看出来</p>
<p><strong>解决方法一:</strong></p>
<p>在尾递归函数之外，再提供一个正常形式的函数。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">tailFactorial</span>(<span class="params">n, total</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">if</span> (n === <span class="number">1</span>) <span class="keyword">return</span> total;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> tailFactorial(n - <span class="number">1</span>, n * total);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">factorial</span>(<span class="params">n</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> tailFactorial(n, <span class="number">1</span>);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">factorial(<span class="number">5</span>) <span class="comment">// 120</span></span></code><br></pre></td></tr></table></figure>

<p>函数式编程中 柯里化(currying) 的概念:</p>
<p>意思是将多参数的函数转换成单参数的形式</p>
<p><strong>解决方法二:</strong></p>
<p>采用ES6的函数默认值</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">factorial</span>(<span class="params">n, total = <span class="number">1</span></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">if</span> (n === <span class="number">1</span>) <span class="keyword">return</span> total;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> factorial(n - <span class="number">1</span>, n * total);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">factorial(<span class="number">5</span>) <span class="comment">// 120</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>总结: 递归本质上是一种循环操作。纯粹的函数式编程语言没有循环操作命令，所有的循环都用递归实现</p>
</blockquote>
<h2 id="严格模式-1"><a href="#严格模式-1" class="headerlink" title="严格模式"></a>严格模式</h2><p>ES6 的尾调用优化只在严格模式下开启，正常模式是无效的。<br>正常模式下,函数内部有两个变量,可以跟踪函数的调用栈</p>
<ul>
<li><code>func.arguments</code>:返回调用时函数的参数</li>
<li><code>func.caller</code>: 返回调用时当前函数的那个函数</li>
</ul>
<p>尾调用优化发生时，函数的调用栈会改写，因此上面两个变量就会失真。严格模式禁用这两个变量，所以尾调用模式仅在严格模式下生效。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">restricted</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line"><span class="meta">  'use strict'</span>;</span></code><br><code class="highlight javascript"><span class="line">  restricted.caller;    <span class="comment">// 报错</span></span></code><br><code class="highlight javascript"><span class="line">  restricted.arguments; <span class="comment">// 报错</span></span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line">restricted();</span></code><br></pre></td></tr></table></figure>

<h2 id="尾递归优化的实现"><a href="#尾递归优化的实现" class="headerlink" title="尾递归优化的实现"></a>尾递归优化的实现</h2><p>在正常模式下实现尾递归优化</p>
<p>原理: 用“循环”换掉“递归”,减少调用栈</p>
<p><strong>方法一:</strong></p>
<p>蹦床函数(将递归执行转为循环执行) </p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">trampoline</span>(<span class="params">f</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">while</span> (f &amp;&amp; f <span class="keyword">instanceof</span> <span class="built_in">Function</span>) &#123;</span></code><br><code class="highlight javascript"><span class="line">    f = f();</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> f;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//接受一个函数f作为参数,只要f执行后返回一个函数,就继续执行,否则返回值</span></span></code><br></pre></td></tr></table></figure>

<p><strong>方法二:</strong></p>
<p>方法一的蹦床函数并非真正的尾递归优化</p>
<p>下面的才是:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">tco</span>(<span class="params">f</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">var</span> value;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">var</span> active = <span class="literal">false</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">var</span> accumulated = [];</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span> <span class="title">accumulator</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">    accumulated.push(<span class="built_in">arguments</span>);</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">if</span> (!active) &#123;</span></code><br><code class="highlight javascript"><span class="line">      active = <span class="literal">true</span>;</span></code><br><code class="highlight javascript"><span class="line">      <span class="keyword">while</span> (accumulated.length) &#123;</span></code><br><code class="highlight javascript"><span class="line">        value = f.apply(<span class="keyword">this</span>, accumulated.shift());</span></code><br><code class="highlight javascript"><span class="line">      &#125;</span></code><br><code class="highlight javascript"><span class="line">      active = <span class="literal">false</span>;</span></code><br><code class="highlight javascript"><span class="line">      <span class="keyword">return</span> value;</span></code><br><code class="highlight javascript"><span class="line">    &#125;</span></code><br><code class="highlight javascript"><span class="line">  &#125;;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> sum = tco(<span class="function"><span class="keyword">function</span>(<span class="params">x, y</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">if</span> (y &gt; <span class="number">0</span>) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> sum(x + <span class="number">1</span>, y - <span class="number">1</span>)</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">else</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> x</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">sum(<span class="number">1</span>, <span class="number">100000</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 100001</span></span></code><br></pre></td></tr></table></figure>

<p>上面的代码中,<code>tco</code>函数时尾递归优化的实现,它的奥妙就在于状态变量<code>active</code></p>
<h1 id="函数参数的尾逗号"><a href="#函数参数的尾逗号" class="headerlink" title="函数参数的尾逗号"></a>函数参数的尾逗号</h1><p>以前不允许最后一个参数后面出现逗号,这样对以后的修改很造成麻烦<br>ES2017 允许函数的最后一个参数有尾逗号</p>
]]></content>
      <categories>
        <category>前端开发</category>
        <category>ES6学习</category>
      </categories>
      <tags>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript6--数值的扩展</title>
    <url>/2019/10/17/ECMAScript6/5%E3%80%81%E6%95%B0%E5%80%BC%E7%9A%84%E6%89%A9%E5%B1%95/</url>
    <content><![CDATA[<h1 id="二进制和八进制表示法"><a href="#二进制和八进制表示法" class="headerlink" title="二进制和八进制表示法"></a>二进制和八进制表示法</h1><p>ES6提供了新的写法<br>二进制:  前缀使用0b(或0B)表示<br>八进制: 前缀使用0o(或0O)表示</p>
<a id="more"></a>

<h1 id="Number-isFinite-Number-isNaN"><a href="#Number-isFinite-Number-isNaN" class="headerlink" title="Number.isFinite() , Number.isNaN()"></a>Number.isFinite() , Number.isNaN()</h1><h2 id="Number-isFinite"><a href="#Number-isFinite" class="headerlink" title="Number.isFinite()"></a>Number.isFinite()</h2><p>检查一个数值是否有限的(即不是Infinity),如果参数类型不是数值,返回false</p>
<h2 id="Number-isNaN"><a href="#Number-isNaN" class="headerlink" title="Number.isNaN()"></a>Number.isNaN()</h2><p>检查一个值是否为<code>NaN</code>,如果参数类型不是<code>NaN</code>,<code>Number.isNaN</code>返回<code>false</code><br>与传统的全局方法<code>isFinite()</code>和<code>isNaN()</code>的区别:<br>传统方法先调用Number()将非数值的值转为数值,再进行判断<br><em>而这两个新方法只对数值有效</em></p>
<h1 id="Number-parseInt-Number-parseFloat"><a href="#Number-parseInt-Number-parseFloat" class="headerlink" title="Number.parseInt() , Number.parseFloat()"></a>Number.parseInt() , Number.parseFloat()</h1><p>ES6将全局方法<code>parseInt()</code>和<code>parseFloat()</code>,移植到Number对象上,行为完全保持不变</p>
<h1 id="Number-isInteger"><a href="#Number-isInteger" class="headerlink" title="Number.isInteger()"></a>Number.isInteger()</h1><p>判断一个数值是否为整数,如果参数不是数值,该方法返回false</p>
<p>注意:<br>由于 JavaScript 采用 IEEE 754 标准，数值存储为64位双精度格式，数值精度最多可以达到 53 个二进制位（1 个隐藏位与 52 个有效位）。如果数值的精度超过这个限度，第54位及后面的位就会被丢弃,这种情况下,<code>Number.isInteger</code>可能会误判</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Number</span>.isInteger(<span class="number">3.0000000000000002</span>) <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//这个小数的精度达到了小数点后16个十进制位，转成二进制位超过了53个二进制位，</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//导致最后的那个2被丢弃了。所以误判为整数</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>如果对数据精度的要求比较高,不建议使用该方法判断一个数值是否为整数</p>
</blockquote>
<h1 id="Number-EPSILON"><a href="#Number-EPSILON" class="headerlink" title="Number.EPSILON"></a>Number.EPSILON</h1><p>ES6新增的一个极小的常量,实际上是JavaScript能够表示的最小精度<br>根据规格,它表示1 与大于 1 的最小浮点数之间的差。<br>对于64 位浮点数来说，大于 1 的最小浮点数相当于二进制的1.00..001,小数点后面有连续 51 个零。这个值减去 1 之后，就等于 2 的 -52 次方。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Number</span>.EPSILON === <span class="built_in">Math</span>.pow(<span class="number">2</span>, <span class="number">-52</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Number</span>.EPSILON</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 2.220446049250313e-16</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Number</span>.EPSILON.toFixed(<span class="number">20</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// "0.00000000000000022204"</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p> 能够用来设置”能够接受的误差范围”(暂时还没有搞懂)</p>
</blockquote>
<h1 id="安全整数和Number-isSafeInteger"><a href="#安全整数和Number-isSafeInteger" class="headerlink" title="安全整数和Number.isSafeInteger()"></a>安全整数和Number.isSafeInteger()</h1><p>JavaScript能够准确表示的整数范围在-2^53到2^53之间(不含两个端点),超过这个范围,无法精确表示这个值<br>ES6引入了<code>Number.MAX_SAFE_INTEGER</code>和<code>Number.MIN_SAFE_INTEGER</code>这两个常量,用来表示这个范围的上下限.<br><code>Number.isSafeInteger()</code>用来判断一个整数是否在这个范围内</p>
<h1 id="Math对象的扩展"><a href="#Math对象的扩展" class="headerlink" title="Math对象的扩展"></a>Math对象的扩展</h1><p>ES6 在 Math 对象上新增了 17 个与数学相关的方法。所有这些方法都是静态方法，只能在 Math 对象上调用。</p>
<h2 id="Math-trunc"><a href="#Math-trunc" class="headerlink" title="Math.trunc()"></a>Math.trunc()</h2><p>该方法用于去除一个数的小数部分,返回整数部分.<br>规则:<br>对于非数值,<code>Math.trunc</code>内部使用<code>Number</code>方法将其先转为数值<br>对于空值和无法截取整数的值,返回<code>NaN</code></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.trunc(<span class="number">4.9</span>) <span class="comment">// 4</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.trunc(<span class="number">-4.1</span>) <span class="comment">// -4</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.trunc(<span class="number">-0.1234</span>) <span class="comment">// -0</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.trunc(<span class="string">'123.456'</span>) <span class="comment">// 123</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.trunc(<span class="literal">true</span>) <span class="comment">//1</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.trunc(<span class="literal">false</span>) <span class="comment">// 0</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.trunc(<span class="literal">null</span>) <span class="comment">// 0</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.trunc(<span class="literal">NaN</span>);      <span class="comment">// NaN</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.trunc(<span class="string">'foo'</span>);    <span class="comment">// NaN</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.trunc();         <span class="comment">// NaN</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.trunc(<span class="literal">undefined</span>) <span class="comment">// NaN</span></span></code><br></pre></td></tr></table></figure>

<p>对于没有部署这个方法的环境:可以使用下面的代码模拟:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.trunc = <span class="built_in">Math</span>.trunc  <span class="function"><span class="keyword">function</span>(<span class="params">x</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> x &lt; <span class="number">0</span> ? <span class="built_in">Math</span>.ceil(x) : <span class="built_in">Math</span>.floor(x);</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<h2 id="Math-sign"><a href="#Math-sign" class="headerlink" title="Math.sign()"></a>Math.sign()</h2><p>该方法用来判断一个数到底是正数,负数,还是零.  对于非数值,会先将其转换为数值<br>返回值:</p>
<ul>
<li>参数为正数,返回+1</li>
<li>参数为负数,返回-1</li>
<li>参数为0,返回0</li>
<li>参数为-0,返回-0</li>
<li>其他值,返回<code>NaN</code></li>
</ul>
<p>对于没有部署这个方法的环境:可以使用下面的代码模拟:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.sign = <span class="built_in">Math</span>.sign  <span class="function"><span class="keyword">function</span>(<span class="params">x</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  x = +x; <span class="comment">// convert to a number</span></span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">if</span> (x === <span class="number">0</span>  <span class="built_in">isNaN</span>(x)) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">return</span> x;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> x &gt; <span class="number">0</span> ? <span class="number">1</span> : <span class="number">-1</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<h2 id="Math-cbrt"><a href="#Math-cbrt" class="headerlink" title="Math.cbrt()"></a>Math.cbrt()</h2><p>该方法用于计算一个数的立方根,  对于非数值,会先使用Number方法将其转换为数值<br>对于没有部署这个方法的环境:可以使用下面的代码模拟:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.cbrt = <span class="built_in">Math</span>.cbrt  <span class="function"><span class="keyword">function</span>(<span class="params">x</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">var</span> y = <span class="built_in">Math</span>.pow(<span class="built_in">Math</span>.abs(x), <span class="number">1</span>/<span class="number">3</span>);</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> x &lt; <span class="number">0</span> ? -y : y;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<h2 id="Math-clz32"><a href="#Math-clz32" class="headerlink" title="Math.clz32()"></a>Math.clz32()</h2><p>该方法将参数转为32位无符号整数的形式,然后返回这个32位值里面有多少个前导0</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.clz32(<span class="number">0</span>) <span class="comment">// 32</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.clz32(<span class="number">1</span>) <span class="comment">// 31</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.clz32(<span class="number">1000</span>) <span class="comment">// 22</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.clz32(<span class="number">0b01000000000000000000000000000000</span>) <span class="comment">// 1</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.clz32(<span class="number">0b00100000000000000000000000000000</span>) <span class="comment">// 2</span></span></code><br></pre></td></tr></table></figure>

<p>注意:</p>
<ul>
<li>对于小数,Math.clz32方法只考虑整数部分</li>
<li>对于空值或其他类型的值,<code>Math.clz32</code>方法会将它们先转为数值,然后再计算</li>
</ul>
<h2 id="Math-imul"><a href="#Math-imul" class="headerlink" title="Math.imul()"></a>Math.imul()</h2><p>该方法返回两个数以32位带符号整数形式相乘的结果,返回的也是一个32位的带符号整数</p>
<h2 id="Math-fround"><a href="#Math-fround" class="headerlink" title="Math.fround()"></a>Math.fround()</h2><p>该方法返回一个数的32位单精度浮点数形式<br>对于32位单精度格式来说,数值精度是24个二进制位(1 位隐藏位与 23 位有效位),所以对于 -224 至 224 之间的整数（不含两个端点），返回结果与参数本身一致。超出这个范围的结果便开始丢失精度</p>
<p>注意:</p>
<ul>
<li>对于<code>NaN</code>和<code>Infinity</code>,该方法返回原值</li>
<li>对于其它类型的非数值,该方法会先将其转为数值,再返回单精度浮点数</li>
</ul>
<p>对于没有部署这个方法的环境:可以使用下面的代码模拟:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.fround = <span class="built_in">Math</span>.fround  <span class="function"><span class="keyword">function</span> (<span class="params">x</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Float32Array</span>([x])[<span class="number">0</span>];</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<h2 id="Math-hypot"><a href="#Math-hypot" class="headerlink" title="Math.hypot()"></a>Math.hypot()</h2><p>该方法返回所有参数的平方和的平方根</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.hypot(<span class="number">3</span>, <span class="number">4</span>);        <span class="comment">// 5</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.hypot(<span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>);     <span class="comment">// 7.0710678118654755</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.hypot();            <span class="comment">// 0</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.hypot(<span class="literal">NaN</span>);         <span class="comment">// NaN</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.hypot(<span class="number">3</span>, <span class="number">4</span>, <span class="string">'foo'</span>); <span class="comment">// NaN</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.hypot(<span class="number">3</span>, <span class="number">4</span>, <span class="string">'5'</span>);   <span class="comment">// 7.0710678118654755</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.hypot(<span class="number">-3</span>);          <span class="comment">// 3</span></span></code><br></pre></td></tr></table></figure>

<p>注意:</p>
<ul>
<li>如果参数不是数值,<code>Math.hypot</code>方法会将其转为数值</li>
<li>只要有一个参数无法转为数值,就会返回<code>NaN</code></li>
</ul>
<h2 id="对数的方法"><a href="#对数的方法" class="headerlink" title="对数的方法"></a><strong>对数的方法</strong></h2><h3 id="Math-expm1"><a href="#Math-expm1" class="headerlink" title="Math.expm1()"></a>Math.expm1()</h3><p>该方法返回 ex - 1,即<code>Math.exp(x) - 1</code><br>对于没有部署这个方法的环境:可以使用下面的代码模拟:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.expm1 = <span class="built_in">Math</span>.expm1  <span class="function"><span class="keyword">function</span>(<span class="params">x</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="built_in">Math</span>.exp(x) - <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<h3 id="Math-log1p"><a href="#Math-log1p" class="headerlink" title="Math.log1p()"></a>Math.log1p()</h3><p>该方法返回 1 + x 的自然对数,即Math.log(1+x).  如果x小于 -1 ,返回<code>NaN</code></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.log1p(<span class="number">1</span>)  <span class="comment">// 0.6931471805599453</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.log1p(<span class="number">0</span>)  <span class="comment">// 0</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.log1p(<span class="number">-1</span>) <span class="comment">// -Infinity</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.log1p(<span class="number">-2</span>) <span class="comment">// NaN</span></span></code><br></pre></td></tr></table></figure>

<p>对于没有部署这个方法的环境:可以使用下面的代码模拟:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.log1p = <span class="built_in">Math</span>.log1p  <span class="function"><span class="keyword">function</span>(<span class="params">x</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="built_in">Math</span>.log(<span class="number">1</span> + x);</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<h3 id="Math-log10"><a href="#Math-log10" class="headerlink" title="Math.log10()"></a>Math.log10()</h3><p>该方法返回10为底的x的对数,   如果x小于0,则返回<code>NaN</code></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.log10(<span class="number">2</span>)      <span class="comment">// 0.3010299956639812</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.log10(<span class="number">1</span>)      <span class="comment">// 0</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.log10(<span class="number">0</span>)      <span class="comment">// -Infinity</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.log10(<span class="number">-2</span>)     <span class="comment">// NaN</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.log10(<span class="number">100000</span>) <span class="comment">// 5</span></span></code><br></pre></td></tr></table></figure>

<p>对于没有部署这个方法的环境:可以使用下面的代码模拟:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.log10 = <span class="built_in">Math</span>.log10  <span class="function"><span class="keyword">function</span>(<span class="params">x</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="built_in">Math</span>.log(x) / <span class="built_in">Math</span>.LN10;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<h3 id="Math-log2"><a href="#Math-log2" class="headerlink" title="Math.log2()"></a>Math.log2()</h3><p>该方法返回以2为底的x的对数,   如果x小于0,则返回<code>NaN</code></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.log2(<span class="number">3</span>)       <span class="comment">// 1.584962500721156</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.log2(<span class="number">2</span>)       <span class="comment">// 1</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.log2(<span class="number">1</span>)       <span class="comment">// 0</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.log2(<span class="number">0</span>)       <span class="comment">// -Infinity</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.log2(<span class="number">-2</span>)      <span class="comment">// NaN</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.log2(<span class="number">1024</span>)    <span class="comment">// 10</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.log2(<span class="number">1</span> &lt;&lt; <span class="number">29</span>) <span class="comment">// 29</span></span></code><br></pre></td></tr></table></figure>

<p>对于没有部署这个方法的环境:可以使用下面的代码模拟:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.log2 = <span class="built_in">Math</span>.log2  <span class="function"><span class="keyword">function</span>(<span class="params">x</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="built_in">Math</span>.log(x) / <span class="built_in">Math</span>.LN2;</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br></pre></td></tr></table></figure>

<h2 id="双曲函数方法"><a href="#双曲函数方法" class="headerlink" title="双曲函数方法"></a>双曲函数方法</h2><ul>
<li><code>Math.sinh(x)</code> 返回x的双曲正弦（hyperbolic sine）</li>
<li><code>Math.cosh(x)</code> 返回x的双曲余弦（hyperbolic cosine）</li>
<li><code>Math.tanh(x)</code> 返回x的双曲正切（hyperbolic tangent）</li>
<li><code>Math.asinh(x)</code> 返回x的反双曲正弦（inverse hyperbolic sine）</li>
<li><code>Math.acosh(x)</code> 返回x的反双曲余弦（inverse hyperbolic cosine）</li>
<li><code>Math.atanh(x)</code> 返回x的反双曲正切（inverse hyperbolic tangent）</li>
</ul>
<h1 id="指数运算符"><a href="#指数运算符" class="headerlink" title="指数运算符"></a>指数运算符</h1><p>指数运算符( ** )</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="number">2</span> ** <span class="number">2</span> <span class="comment">// 4</span></span></code><br><code class="highlight javascript"><span class="line"><span class="number">2</span> ** <span class="number">3</span> <span class="comment">// 8</span></span></code><br></pre></td></tr></table></figure>

<p>该运算符的特点是左结合,即多个指数运算符连用是,是从最左边开始计算的</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 相当于 2 ** (3 ** 2)</span></span></code><br><code class="highlight javascript"><span class="line"><span class="number">2</span> ** <span class="number">3</span> ** <span class="number">2</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 512</span></span></code><br></pre></td></tr></table></figure>

<p>与等号连用( **= )</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> a = <span class="number">1.5</span>;</span></code><br><code class="highlight javascript"><span class="line">a **= <span class="number">2</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于 a = a * a;</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> b = <span class="number">4</span>;</span></code><br><code class="highlight javascript"><span class="line">b **= <span class="number">3</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于 b = b * b * b;</span></span></code><br></pre></td></tr></table></figure>

<p>注意:</p>
<p>V8 引擎的指数运算符与<code>Math.pow</code>的实现不相同,对于特别大的运算结果,两者会有细微的差异</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">Math</span>.pow(<span class="number">99</span>, <span class="number">99</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 3.697296376497263e+197</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="number">99</span> ** <span class="number">99</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 3.697296376497268e+197</span></span></code><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>前端开发</category>
        <category>ES6学习</category>
      </categories>
      <tags>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript6--正则的扩展</title>
    <url>/2019/10/16/ECMAScript6/4%E3%80%81%E6%AD%A3%E5%88%99%E7%9A%84%E6%89%A9%E5%B1%95/</url>
    <content><![CDATA[<h1 id="RegExp构造函数"><a href="#RegExp构造函数" class="headerlink" title="RegExp构造函数"></a>RegExp构造函数</h1><p>在ES5中,RegExp构造函数的参数有两种情况</p>
<p>第一种: 参数是字符串，这时第二个参数表示正则表达式的修饰符（flag）。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> regex = <span class="keyword">new</span> <span class="built_in">RegExp</span>(<span class="string">'xyz'</span>, <span class="string">'i'</span>);</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等价于</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> regex = <span class="regexp">/xyz/i</span>;</span></code><br></pre></td></tr></table></figure>

<p>第二种: 参数是一个正则表示式，这时会返回一个原有正则表达式的拷贝。</p>
<a id="more"></a>

<p>但是不允许使用第二个参数添加修饰符,否则会报错</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> regex = <span class="keyword">new</span> <span class="built_in">RegExp</span>(<span class="regexp">/xyz/i</span>);</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等价于</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> regex = <span class="regexp">/xyz/i</span>;</span></code><br></pre></td></tr></table></figure>

<p>ES6中改变了这种行为,如果RegExp构造函数第一个参数是一个正则对象,那么可以使用第二个参数指定修饰符,而且返回的正则表达式会忽略原有的正则表达式的修饰符,只使用新指定的修饰符.</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">new</span> <span class="built_in">RegExp</span>(<span class="regexp">/abc/ig</span>, <span class="string">'i'</span>).flags</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// "i"</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//String.prototype.match 调用 RegExp.prototype[Symbol.match]</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">///String.prototype.replace 调用 RegExp.prototype[Symbol.replace]</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//String.prototype.search 调用 RegExp.prototype[Symbol.search]</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//String.prototype.split 调用 RegExp.prototype[Symbol.split]</span></span></code><br></pre></td></tr></table></figure>

<h1 id="字符串的正则方法"><a href="#字符串的正则方法" class="headerlink" title="字符串的正则方法"></a>字符串的正则方法</h1><p>字符串对象共有4个方法,可以使用正则表达式:</p>
<ul>
<li>match()</li>
<li>replace()</li>
<li>search()</li>
<li>split()</li>
</ul>
<p>ES6将这4个方法,在语言内部全部调用RegExp的实例方法,从而做到所有与正则相关的方法,全都定义在RegExp对象上.</p>
<ul>
<li>String.prototype.match 调用 RegExp.prototype[Symbol.match]</li>
<li>String.prototype.replace 调用 RegExp.prototype[Symbol.replace]</li>
<li>String.prototype.search 调用 RegExp.prototype[Symbol.search]</li>
<li>String.prototype.split 调用 RegExp.prototype[Symbol.split]</li>
</ul>
<h1 id="u修饰符"><a href="#u修饰符" class="headerlink" title="u修饰符"></a>u修饰符</h1><p>ES6对正则表达式添加了u修饰符,含义为”Unicode 模式”,用来正确处理大于\uFFFF的Unicode 字符(即会正确处理四个字节的 UTF-16 编码)</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">/^\uD83D/u.test(<span class="string">'\uD83D\uDC2A'</span>) <span class="comment">// false</span></span></code><br><code class="highlight javascript"><span class="line">/^\uD83D/.test(<span class="string">'\uD83D\uDC2A'</span>) <span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>

<p>加了u修饰符后,ES6会将 \uD83D\uDC2A 识别为一个字符,所以第一行代码为false</p>
<blockquote>
<p>总结:要想识别码点大于oxFFFF的Unicode字符,就需要加上u修饰符</p>
</blockquote>
<p><strong>一旦加上u修饰符号,就会修改下面这些正则表达式的行为:</strong></p>
<ol>
<li>点字符</li>
</ol>
<p>点( . )字符在正则表达式中,含义是除了换行符以外的任意单个字符,对于码点大于oxFFFF的Unicode字符,点字符不能识别,必须加上u修饰符</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> s = <span class="string">'𠮷'</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">/^.$/.test(s) <span class="comment">// false</span></span></code><br><code class="highlight javascript"><span class="line">/^.$/u.test(s) <span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>



<p>2.Unicode字符表示法</p>
<p>ES6 新增了使用大括号表示 Unicode 字符，这种表示法在正则表达式中必须加上 u 修饰符，才能识别当中的大括号，否则会被解读为量词。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">//不加u修饰符,正则表达式会认为匹配61个连续的u</span></span></code><br><code class="highlight javascript"><span class="line">/\u&#123;<span class="number">61</span>&#125;/.test(<span class="string">'a'</span>) <span class="comment">// false</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">/\u&#123;<span class="number">61</span>&#125;/u.test(<span class="string">'a'</span>) <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line">/\u&#123;<span class="number">20</span>BB7&#125;/u.test(<span class="string">'𠮷'</span>) <span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>

<p>3.量词</p>
<p>使用u修饰符后,所有量词都会正确识别码点大于0xFFFF的Unicode字符.</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">/a&#123;<span class="number">2</span>&#125;/.test(<span class="string">'aa'</span>) <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line">/a&#123;<span class="number">2</span>&#125;/u.test(<span class="string">'aa'</span>) <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line">/𠮷&#123;<span class="number">2</span>&#125;/.test(<span class="string">'𠮷𠮷'</span>) <span class="comment">// false</span></span></code><br><code class="highlight javascript"><span class="line">/𠮷&#123;<span class="number">2</span>&#125;/u.test(<span class="string">'𠮷𠮷'</span>) <span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>

<p>4.预定义模式</p>
<p>u修饰符也影响到预定义模式(空白字符类), 加了u修饰符,才能正确识别码点大于oxFFFF的Unicode字符</p>
<p>5.i修饰符</p>
<p>​             <img src="https://qqadapt.qpic.cn/txdocpic/0/704242ba738622b265a23b39a6724a07/0" alt="img">             </p>
<h1 id="RegExp-prototype-unicode-属性"><a href="#RegExp-prototype-unicode-属性" class="headerlink" title="RegExp.prototype.unicode 属性"></a>RegExp.prototype.unicode 属性</h1><p>检查一个正则表达式是否设置了u修饰符</p>
<p>设置了返回true,否则返回false</p>
<h1 id="y修饰符"><a href="#y修饰符" class="headerlink" title="y修饰符"></a>y修饰符</h1><p>ES6添加了y修饰符,叫做”粘连(sticky)”修饰符</p>
<p>y修饰符的作用与g修饰符类似,也是全局匹配,后一次匹配都从上一次匹配成功的下一个位置开始。但是y修饰符确保匹配必须从剩余的第一个位置开始(这也就是”粘连”的涵义)</p>
<h1 id="RegExp-prototype-sticky属性"><a href="#RegExp-prototype-sticky属性" class="headerlink" title="RegExp.prototype.sticky属性"></a>RegExp.prototype.sticky属性</h1><p>检测正则表达式是否设置了y修饰符</p>
<p>设置了返回true,否则返回false</p>
<h1 id="RegExp-prototype-flags属性"><a href="#RegExp-prototype-flags属性" class="headerlink" title="RegExp.prototype.flags属性"></a>RegExp.prototype.flags属性</h1><p>返回正则表达式的修饰符</p>
<h1 id="s修饰符-dotAll模式"><a href="#s修饰符-dotAll模式" class="headerlink" title="s修饰符:dotAll模式"></a>s修饰符:dotAll模式</h1><p>点<code>( . )</code>字符不能匹配的两种情况:</p>
<ol>
<li>四个字节的UTF-16字符</li>
<li>行终止符(表示一行的终结)</li>
</ol>
<ul>
<li><p>U+000A 换行符(\n)</p>
</li>
<li><p>U+000D 回车符(\r)</p>
</li>
<li><p>U+2028 行分隔符（line separator）</p>
</li>
<li><p>U+2029 段分隔符（paragraph separator）</p>
</li>
</ul>
<p>加了s修饰符后,点( . )可以代表任意单个字符</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">/foo.bar/s.test(<span class="string">'foo\nbar'</span>) <span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>

<p>使用<code>dotAll</code>属性检查一个正则表达式是否设置了s修饰符</p>
<p>设置了返回true,否则返回false</p>
<h1 id="Unicode属性类"><a href="#Unicode属性类" class="headerlink" title="Unicode属性类"></a>Unicode属性类</h1><p>新的类的写法:</p>
<ul>
<li>大写的P : <code>\P{...}</code></li>
<li>小写的p : <code>\p{...}</code></li>
</ul>
<p>以上两种写法允许正则表达式匹配符合<code>Unicode</code>某种属性的所有字符</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">//Unicode 属性类要指定属性名和属性值。</span></span></code><br><code class="highlight javascript"><span class="line">\p&#123;UnicodePropertyName=UnicodePropertyValue&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//对于某些属性，可以只写属性名，或者只写属性值。</span></span></code><br><code class="highlight javascript"><span class="line">\p&#123;UnicodePropertyName&#125;</span></code><br><code class="highlight javascript"><span class="line">\p&#123;UnicodePropertyValue&#125;</span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意:</p>
<ul>
<li>大写P的写法是小写p的反向匹配,即匹配不满足条件的字符</li>
<li>这两种类(写法)只对Unicode有效,所以使用的时候一定要加上u修饰符,不然会报错</li>
</ul>
</blockquote>
<h1 id="具名组匹配"><a href="#具名组匹配" class="headerlink" title="具名组匹配"></a>具名组匹配</h1><p>原先获取分组内容:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> RE_DATE = <span class="regexp">/(\d&#123;4&#125;)-(\d&#123;2&#125;)-(\d&#123;2&#125;)/</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> matchObj = RE_DATE.exec(<span class="string">'1999-12-31'</span>);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> year = matchObj[<span class="number">1</span>]; <span class="comment">// 1999</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> month = matchObj[<span class="number">2</span>]; <span class="comment">// 12</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> day = matchObj[<span class="number">3</span>]; <span class="comment">// 31</span></span></code><br></pre></td></tr></table></figure>

<p>分组只能通过数字序号引用来获取,一旦组的顺序变了,引用的时候就必须修改序号<br>具名组匹配: 为每个分组指定一个名字<br>用法: 模式的头部添加<code>“问号 + 尖括号 + 组名” (?&lt;year&gt;)</code>;<br>获取: 匹配结果的groups属性上引用组名</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> RE_DATE = <span class="regexp">/(?&lt;year&gt;\d&#123;4&#125;)-(?&lt;month&gt;\d&#123;2&#125;)-(?&lt;day&gt;\d&#123;2&#125;)/</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> matchObj = RE_DATE.exec(<span class="string">'1999-12-31'</span>);</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> year = matchObj.groups.year; <span class="comment">// 1999</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> month = matchObj.groups.month; <span class="comment">// 12</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> day = matchObj.groups.day; <span class="comment">// 31</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//以前的写法在这里也依然有效</span></span></code><br></pre></td></tr></table></figure>

<p>如果具名组没有匹配,那么对应的groups对象的属性会是<code>undefined</code></p>
<h2 id="使用解构赋值和替换"><a href="#使用解构赋值和替换" class="headerlink" title="使用解构赋值和替换"></a>使用解构赋值和替换</h2><p>使用解构赋值直接从匹配结果上为变量赋值。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> &#123;<span class="attr">groups</span>: &#123;one, two&#125;&#125; = <span class="regexp">/^(?&lt;one&gt;.*):(?&lt;two&gt;.*)$/u</span>.exec(<span class="string">'foo:bar'</span>);</span></code><br><code class="highlight javascript"><span class="line">one  <span class="comment">// foo</span></span></code><br><code class="highlight javascript"><span class="line">two  <span class="comment">// bar</span></span></code><br></pre></td></tr></table></figure>

<p>字符串替换时，使用%&lt;组名&gt;引用具名组</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> re = <span class="regexp">/(?&lt;year&gt;\d&#123;4&#125;)-(?&lt;month&gt;\d&#123;2&#125;)-(?&lt;day&gt;\d&#123;2&#125;)/u</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="string">'2015-01-02'</span>.replace(re, <span class="string">'$&lt;day&gt;/$&lt;month&gt;/$&lt;year&gt;'</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// '02/01/2015'</span></span></code><br></pre></td></tr></table></figure>

<p>replace的第二个参数也可以是个函数,该函数的参数序列如下:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="string">'2015-01-02'</span>.replace(re, (</span></code><br><code class="highlight javascript"><span class="line">   matched, <span class="comment">// 整个匹配结果 2015-01-02</span></span></code><br><code class="highlight javascript"><span class="line">   capture1, <span class="comment">// 第一个组匹配 2015</span></span></code><br><code class="highlight javascript"><span class="line">   capture2, <span class="comment">// 第二个组匹配 01</span></span></code><br><code class="highlight javascript"><span class="line">   capture3, <span class="comment">// 第三个组匹配 02</span></span></code><br><code class="highlight javascript"><span class="line">   position, <span class="comment">// 匹配开始的位置 0</span></span></code><br><code class="highlight javascript"><span class="line">   S, <span class="comment">// 原字符串 2015-01-02</span></span></code><br><code class="highlight javascript"><span class="line">   groups <span class="comment">// 具名组构成的一个对象 &#123;year, month, day&#125;</span></span></code><br><code class="highlight javascript"><span class="line"> ) =&gt; &#123;</span></code><br><code class="highlight javascript"><span class="line"> <span class="keyword">let</span> &#123;day, month, year&#125; = groups;</span></code><br><code class="highlight javascript"><span class="line"> <span class="keyword">return</span> <span class="string">`<span class="subst">$&#123;day&#125;</span>/<span class="subst">$&#123;month&#125;</span>/<span class="subst">$&#123;year&#125;</span>`</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;);</span></code><br></pre></td></tr></table></figure>

<p>具名组匹配在原来的基础上，新增了最后一个函数参数：具名组构成的一个对象。函数内部可以直接对这个对象进行解构赋值。</p>
<h2 id="引用"><a href="#引用" class="headerlink" title="引用"></a><strong>引用</strong></h2><p>如果要在正则表达式内部引用某个“具名组匹配”，可以使用\k&lt;组名&gt;的写法</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> RE_TWICE = <span class="regexp">/^(?&lt;word&gt;[a-z]+)!\k&lt;word&gt;$/</span>;</span></code><br><code class="highlight javascript"><span class="line">RE_TWICE.test(<span class="string">'abc!abc'</span>) <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line">RE_TWICE.test(<span class="string">'abc!ab'</span>) <span class="comment">// false</span></span></code><br></pre></td></tr></table></figure>

<p>数字引用依然有效(可以同时使用)</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">//数字引用</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> RE_TWICE = <span class="regexp">/^(?&lt;word&gt;[a-z]+)!\1$/</span>;</span></code><br><code class="highlight javascript"><span class="line">RE_TWICE.test(<span class="string">'abc!abc'</span>) <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line">RE_TWICE.test(<span class="string">'abc!ab'</span>) <span class="comment">// false</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//混合使用</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> RE_TWICE = <span class="regexp">/^(?&lt;word&gt;[a-z]+)!\k&lt;word&gt;!\1$/</span>;</span></code><br><code class="highlight javascript"><span class="line">RE_TWICE.test(<span class="string">'abc!abc!abc'</span>) <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line">RE_TWICE.test(<span class="string">'abc!abc!ab'</span>) <span class="comment">// false</span></span></code><br></pre></td></tr></table></figure>

<h1 id="String-prototype-matchAll"><a href="#String-prototype-matchAll" class="headerlink" title="String.prototype.matchAll"></a>String.prototype.matchAll</h1><p>用法: 字符串.matchAll(正则表达式)<br>用于一次性取出所有匹配,返回一个遍历器</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> string = <span class="string">'test1test2test3'</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// g 修饰符加不加都可以</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> regex = <span class="regexp">/t(e)(st(\d?))/g</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">const</span> match <span class="keyword">of</span> string.matchAll(regex)) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(match);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//["test1", "e", "st1", "1", index: 0, input: "test1test2test3"]</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//["test2", "e", "st2", "2", index: 5, input: "test1test2test3"]</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//["test3", "e", "st3", "3", index: 10, input: "test1test2test3"]</span></span></code><br></pre></td></tr></table></figure>

<p>遍历器转换为数组:<br>使用…运算符和Array.from()</p>
]]></content>
      <categories>
        <category>前端开发</category>
        <category>ES6学习</category>
      </categories>
      <tags>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript6--字符串的扩展</title>
    <url>/2019/10/15/ECMAScript6/3%E3%80%81%E5%AD%97%E7%AC%A6%E4%B8%B2%E7%9A%84%E6%89%A9%E5%B1%95/</url>
    <content><![CDATA[<h1 id="字符的Unicode表示法"><a href="#字符的Unicode表示法" class="headerlink" title="字符的Unicode表示法"></a>字符的Unicode表示法</h1><p>ES6 加强了对 Unicode 的支持，允许采用\uxxxx形式表示一个字符,其中xxxx表示字符的Unicode码点<br>表示范围:   \u0000 ~ \uFFFF<br>超出范围的字符,必须用两个双字节的形式表示</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="string">"\uD842\uDFB7"</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// "𠮷"</span></span></code><br><code class="highlight javascript"><span class="line"><span class="string">"\u20BB7"</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// " 7"</span></span></code><br></pre></td></tr></table></figure>

<p>上面代码中,如果直接在\u后面跟上超过0xFFFF的数值,JavaScript会理解成 \u20BB+7. 由于\u20BB是一个不可打印字符,所以只会显示一个空格,后面跟着一个7</p>
<a id="more"></a>

<p>ES6 对这一点做出了改进，只要将码点放入大括号，就能正确解读该字符。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="string">"\u&#123;20BB7&#125;"</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// "𠮷"</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="string">"\u&#123;41&#125;\u&#123;42&#125;\u&#123;43&#125;"</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// "ABC"</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> hello = <span class="number">123</span>;</span></code><br><code class="highlight javascript"><span class="line">hell\u&#123;<span class="number">6</span>F&#125; <span class="comment">// 123</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="string">'\u&#123;1F680&#125;'</span> === <span class="string">'\uD83D\uDE80'</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>

<p>上面代码中，最后一个例子表明，大括号表示法与四字节的 UTF-16 编码是等价的。</p>
<p>有了这种表示法之后，JavaScript 共有 6 种方法可以表示一个字符。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="string">'\z'</span> === <span class="string">'z'</span>  <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line"><span class="string">'\172'</span> === <span class="string">'z'</span> <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line"><span class="string">'\x7A'</span> === <span class="string">'z'</span> <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line"><span class="string">'\u007A'</span> === <span class="string">'z'</span> <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line"><span class="string">'\u&#123;7A&#125;'</span> === <span class="string">'z'</span> <span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>

<h1 id="字符串的遍历器接口"><a href="#字符串的遍历器接口" class="headerlink" title="字符串的遍历器接口"></a>字符串的遍历器接口</h1><p>ES6位字符串添加了遍历器接口,使得字符串可以被for…of循环遍历</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> codePoint <span class="keyword">of</span> <span class="string">'foo'</span>) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(codePoint)</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// "f"</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// "o"</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// "o"</span></span></code><br></pre></td></tr></table></figure>

<p>除了遍历字符串,这个遍历器最大的有点是可以识别大于0xFFFF的码点.传统的for循环无法识别</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> text = <span class="built_in">String</span>.fromCodePoint(<span class="number">0x20BB7</span>);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; text.length; i++) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(text[i]);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// " "</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// " "</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> i <span class="keyword">of</span> text) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(i);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// "𠮷"</span></span></code><br></pre></td></tr></table></figure>

<h1 id="直接输入U-2028-和-U-2029"><a href="#直接输入U-2028-和-U-2029" class="headerlink" title="直接输入U+2028 和 U+2029"></a>直接输入U+2028 和 U+2029</h1><p>JavaScript字符串允许直接输入字符,以及输入字符的转义形式<br>例如: <code>&#39;中&#39; === &#39;\u4e2d&#39;         //true</code><br>但是,JavaScript规定有五个字符,不能直接在字符串里面直接使用,只能使用转义形式.</p>
<ul>
<li>U+005C：反斜杠（reverse solidus)</li>
<li>U+000D：回车（carriage return）</li>
<li>U+2028：行分隔符（line separator）</li>
<li>U+2029：段分隔符（paragraph separator）</li>
<li>U+000A：换行符（line feed）</li>
</ul>
<p>对于json格式允许字符串里面直接使用 U+2028（行分隔符）和 U+2029（段分隔符）</p>
<h1 id="JSON-stringify-的改造"><a href="#JSON-stringify-的改造" class="headerlink" title="JSON.stringify()的改造"></a>JSON.stringify()的改造</h1><p>UTF-8标准规定,0xD800到0xDFFF之间的码点,不能单独使用,必须配对使用<br>现在JSON.stringify方法可能返回不符合UTF-8标准的字符串(可能返回0xD800到0xDFFF之间的单个码点)</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">JSON</span>.stringify(<span class="string">'\u&#123;D834&#125;'</span>) <span class="comment">// ""\\uD834""</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">JSON</span>.stringify(<span class="string">'\uDF06\uD834'</span>) <span class="comment">// ""\\udf06\\ud834""</span></span></code><br></pre></td></tr></table></figure>

<p>如果遇到0xD800到0xDFFF之间的单个码点,或者不存在的匹配形式,它会返回转义字符串,留给应用自己决定下一步的处</p>
<h1 id="模板字符串"><a href="#模板字符串" class="headerlink" title="模板字符串"></a>模板字符串</h1><p>ES6中使用反引号(`)标识模板字符串</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 普通字符串</span></span></code><br><code class="highlight javascript"><span class="line"><span class="string">`In JavaScript '\n' is a line-feed.`</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 多行字符串(所有的空格和缩进都会被保留在输出之中)</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//可以使用trim方法消除行首行尾的缩进....</span></span></code><br><code class="highlight javascript"><span class="line"><span class="string">`In JavaScript this is</span></span></code><br><code class="highlight javascript"><span class="line"><span class="string">     not legal.`</span></span></code><br></pre></td></tr></table></figure>

<p>如果在模板中需要使用反引号表示,则需要使用反斜杠进行转义</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> greeting = <span class="string">`\`Yo\` World!`</span>;</span></code><br></pre></td></tr></table></figure>

<p>模板字符串中嵌入<code>变量(js表达式)</code>,需要将<code>变量名(js表达式)</code>写在<code>${}</code>之中</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">//嵌入变量</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>(<span class="params">name,age</span>)</span>&#123;</span></code><br><code class="highlight javascript"><span class="line">	<span class="built_in">console</span>.log(<span class="string">`姓名:<span class="subst">$&#123;name&#125;</span>,年龄:<span class="subst">$&#123;age&#125;</span>`</span>);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//js表达式</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> x = <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> y = <span class="number">2</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="string">`<span class="subst">$&#123;x&#125;</span> + <span class="subst">$&#123;y * <span class="number">2</span>&#125;</span> = <span class="subst">$&#123;x + y * <span class="number">2</span>&#125;</span>`</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// "1 + 4 = 5"</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> obj = &#123;<span class="attr">x</span>: <span class="number">1</span>, <span class="attr">y</span>: <span class="number">2</span>&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="string">`<span class="subst">$&#123;obj.x + obj.y&#125;</span>`</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// "3"</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//调用函数</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> <span class="string">"Hello World"</span>;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="string">`foo <span class="subst">$&#123;fn()&#125;</span> bar`</span>    <span class="comment">// foo Hello World bar</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意:<br>如果大括号中的值不是字符串,将按照一般的规则转为字符串<br>如果字符串中的变量没有声明,将报错<br>大括号内部是一个字符串,将会原样输出</p>
</blockquote>
<p>模板字符串可以嵌套:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> tmpl = <span class="function"><span class="params">addrs</span> =&gt;</span> <span class="string">`</span></span></code><br><code class="highlight javascript"><span class="line"><span class="string">  &lt;table&gt;</span></span></code><br><code class="highlight javascript"><span class="line"><span class="string">  <span class="subst">$&#123;addrs.map(addr =&gt; <span class="string">`</span></span></span></span></code><br><code class="highlight javascript"><span class="line"><span class="string"><span class="subst"><span class="string">    &lt;tr&gt;&lt;td&gt;<span class="subst">$&#123;addr.first&#125;</span>&lt;/td&gt;&lt;/tr&gt;</span></span></span></span></code><br><code class="highlight javascript"><span class="line"><span class="string"><span class="subst"><span class="string">    &lt;tr&gt;&lt;td&gt;<span class="subst">$&#123;addr.last&#125;</span>&lt;/td&gt;&lt;/tr&gt;</span></span></span></span></code><br><code class="highlight javascript"><span class="line"><span class="string"><span class="subst"><span class="string">  `</span>).join(<span class="string">''</span>)&#125;</span></span></span></code><br><code class="highlight javascript"><span class="line"><span class="string">  &lt;/table&gt;</span></span></code><br><code class="highlight javascript"><span class="line"><span class="string">`</span>;</span></code><br></pre></td></tr></table></figure>

<h1 id="标签模板"><a href="#标签模板" class="headerlink" title="标签模板"></a>标签模板</h1><p>模板字符串跟在一个函数名后面,该函数将被调用来处理这个模板字符串,这些被称为”标签模板”功能<br>标签: 指的就是函数<br>模板字符串:函数的参数</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">alert<span class="string">`123`</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//等同于</span></span></code><br><code class="highlight javascript"><span class="line">alert(<span class="number">123</span>);</span></code><br></pre></td></tr></table></figure>

<p> 如果标签模板里面有变量,就不是简单的调用了,而是将模板字符串先处理成多个参数,再调用函数,</p>
<p> 函数会接受一个数组和模板里面的变量值 </p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> a = <span class="number">5</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> b = <span class="number">10</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">tag<span class="string">`Hello <span class="subst">$&#123; a + b &#125;</span> world <span class="subst">$&#123; a * b &#125;</span>`</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于</span></span></code><br><code class="highlight javascript"><span class="line">tag([<span class="string">'Hello '</span>, <span class="string">' world '</span>, <span class="string">''</span>], <span class="number">15</span>, <span class="number">50</span>);</span></code><br></pre></td></tr></table></figure>

<p>第一个参数为数组(该数组有一个raw属性,保存的是转义后的原字符串),该数组的成员是模板字符串中那些没有变量替换的部分, 也就是说，变量替换只发生在数组的第一个成员与第二个成员之间、第二个成员与第三个成员之间，以此类推.</p>
<p>其他参数为模板字符串中各个变量被替换后的值</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">//使用函数的rest参数(形式为...变量名)</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span>(<span class="params">strArray,...values</span>)</span>&#123;</span></code><br><code class="highlight javascript"><span class="line">	......</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<h2 id="模板字符串的限制"><a href="#模板字符串的限制" class="headerlink" title="模板字符串的限制"></a>模板字符串的限制</h2><p>模板字符串默认会将字符串转义,导致无法嵌入其他语言</p>
<h1 id="字符串新增的方法"><a href="#字符串新增的方法" class="headerlink" title="字符串新增的方法"></a>字符串新增的方法</h1><h2 id="String-fromCodePoint"><a href="#String-fromCodePoint" class="headerlink" title="String,fromCodePoint()"></a>String,fromCodePoint()</h2><p>用于从Unicode码点返回对应字符,可以识别码点大于0xFFFF的字符(弥补了ES5中该方法的不足)</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">String</span>.fromCharCode(<span class="number">0x20BB7</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// "𠮷"</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">String</span>.fromCodePoint(<span class="number">0x78</span>, <span class="number">0x1f680</span>, <span class="number">0x79</span>) === <span class="string">'x\uD83D\uDE80y'</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>

<p>如果String.fromCodePoint方法有多个参数,则他们会被合并成一个字符串返回</p>
<h2 id="String-raw"><a href="#String-raw" class="headerlink" title="String.raw()"></a>String.raw()</h2><p>该方法返回一个斜杠都被转义(即斜杠前面再加一个斜杠)的字符串,往往用于模板字符串的处理方法<br>如果原字符串的斜杠已经被转义,那么该方法会进行再次转义</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">String</span>.raw<span class="string">`Hi\\n`</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 返回 "Hi\\\\n"</span></span></code><br></pre></td></tr></table></figure>

<p>该方法也可以作为正常的函数使用,这时,他的第一个参数应该是一个具有raw属性的对象,且raw属性的值应该是一个数组.</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="built_in">String</span>.raw(&#123; <span class="attr">raw</span>: <span class="string">'test'</span> &#125;, <span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>);</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 't0e1s2t'</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 等同于</span></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">String</span>.raw(&#123; <span class="attr">raw</span>: [<span class="string">'t'</span>,<span class="string">'e'</span>,<span class="string">'s'</span>,<span class="string">'t'</span>] &#125;, <span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>);</span></code><br></pre></td></tr></table></figure>

<h2 id="实例方法-codePointAt"><a href="#实例方法-codePointAt" class="headerlink" title="实例方法: codePointAt()"></a>实例方法: codePointAt()</h2><p>JavaScript 内部，字符以 UTF-16 的格式储存，每个字符固定为2个字节.对于那些需要4个字节储存的字符(Unicode码点大于0xFFFF的字符),JavaScript 会认为它们是两个字符。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> s = <span class="string">"𠮷"</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">s.length <span class="comment">// 2</span></span></code><br><code class="highlight javascript"><span class="line">s.charAt(<span class="number">0</span>) <span class="comment">// ''</span></span></code><br><code class="highlight javascript"><span class="line">s.charAt(<span class="number">1</span>) <span class="comment">// ''</span></span></code><br><code class="highlight javascript"><span class="line">s.charCodeAt(<span class="number">0</span>) <span class="comment">// 55362</span></span></code><br><code class="highlight javascript"><span class="line">s.charCodeAt(<span class="number">1</span>) <span class="comment">// 57271</span></span></code><br></pre></td></tr></table></figure>

<p>对于这种4个字节的字符,JavaScript不能正确处理,字符串长度会误判为2,而且charAt()方法无法读取整个字符,charCodeAt()方法只能分别返回前两个字节和后两个字节的值.<br>ES6提供的codePointAt()方法,能够正确的处理4个字节储存的字符,返回个一个字符的码点.</p>
<p>参数:字符在字符串中的位置(从0开始,整个字符码点的位置)</p>
<p>返回值: 码点的十进制值(可以通过toString方法进行进制的转换)</p>
<p>codePointAt()方法的参数,仍然是不正确的,比如，上面代码中，字符a在字符串的正确位置序号应该是 1，但是必须向codePointAt()方法传入 2.</p>
<p>解决办法:</p>
<p>1).使用for…of循环(它会正确识别 32 位的 UTF-16 字符。)</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> s = <span class="string">'𠮷a'</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> ch <span class="keyword">of</span> s) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(ch.codePointAt(<span class="number">0</span>).toString(<span class="number">16</span>));</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 20bb7</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 61</span></span></code><br></pre></td></tr></table></figure>

<p>2).使用扩展运算符(…)进行展开运算</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> arr = [...<span class="string">'𠮷a'</span>]; <span class="comment">// arr.length === 2</span></span></code><br><code class="highlight javascript"><span class="line">arr.forEach(</span></code><br><code class="highlight javascript"><span class="line">  ch =&gt; <span class="built_in">console</span>.log(ch.codePointAt(<span class="number">0</span>).toString(<span class="number">16</span>))</span></code><br><code class="highlight javascript"><span class="line">);</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 20bb7</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 61</span></span></code><br></pre></td></tr></table></figure>

<h2 id="实例方法-normalize"><a href="#实例方法-normalize" class="headerlink" title="实例方法:normalize()"></a>实例方法:normalize()</h2><p>许多欧洲语言有语调符号和重音符号。为了表示它们，Unicode 提供了两种方法。</p>
<p>一:直接提供带重音符号的字符，比如Ǒ（\u01D1）</p>
<p>二:提供合成符号（combining character），即原字符与重音符号的合成，两个字符合成一个字符，比如o（\u004F）和ˇ \u030C）合成Ǒ（\u004F\u030C）</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="string">'\u01D1'</span>===<span class="string">'\u004F\u030C'</span> <span class="comment">//false</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="string">'\u01D1'</span>.length <span class="comment">// 1</span></span></code><br><code class="highlight javascript"><span class="line"><span class="string">'\u004F\u030C'</span>.length <span class="comment">// 2</span></span></code><br></pre></td></tr></table></figure>

<p>上面代码表示，JavaScript 将合成字符视为两个字符，导致两种表示方法不相等。</p>
<p>normalize方法可以接受一个参数来指定normalize的方式，参数的四个可选值如下。</p>
<ul>
<li>NFC，默认参数，表示“标准等价合成”（Normalization Form Canonical Composition），返回多个简单字符的合成字符。所谓“标准等价”指的是视觉和语义上的等价。</li>
<li>NFD，表示“标准等价分解”（Normalization Form Canonical Decomposition），即在标准等价的前提下，返回合成字符分解的多个简单字符。</li>
<li>NFKC，表示“兼容等价合成”（Normalization Form Compatibility Composition），返回合成字符。所谓“兼容等价”指的是语义上存在等价，但视觉上不等价，比如“囍”和“喜喜”。（这只是用来举例，normalize方法不能识别中文。）</li>
<li>NFKD，表示“兼容等价分解”（Normalization Form Compatibility Decomposition），即在兼容等价的前提下，返回合成字符分解的多个简单字符。</li>
</ul>
<blockquote>
<p>注意: normalize方法目前不能识别三个或三个以上字符的合成.  这种情况下，还是只能使用正则表达式，通过 Unicode 编号区间判断。</p>
</blockquote>
<h2 id="实例方法-includes-startsWith-endsWith"><a href="#实例方法-includes-startsWith-endsWith" class="headerlink" title="实例方法:includes() , startsWith() , endsWith()"></a>实例方法:includes() , startsWith() , endsWith()</h2><ul>
<li><strong>includes()</strong>：返回布尔值，表示是否找到了参数字符串。</li>
<li><strong>startsWith()</strong>：返回布尔值，表示参数字符串是否在原字符串的头部。</li>
<li><strong>endsWith()</strong>：返回布尔值，表示参数字符串是否在原字符串的尾部。</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> s = <span class="string">'Hello world!'</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">s.startsWith(<span class="string">'Hello'</span>) <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line">s.endsWith(<span class="string">'!'</span>) <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line">s.includes(<span class="string">'o'</span>) <span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>

<p>这三个方法都支持第二个参数，表示开始搜索的位置。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> s = <span class="string">'Hello world!'</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//第二个参数表示从第n个位置知道字符串结束</span></span></code><br><code class="highlight javascript"><span class="line">s.startsWith(<span class="string">'world'</span>, <span class="number">6</span>) <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line">s.includes(<span class="string">'Hello'</span>, <span class="number">6</span>) <span class="comment">// false</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//第二个参数表示针对前n个字符</span></span></code><br><code class="highlight javascript"><span class="line">s.endsWith(<span class="string">'Hello'</span>, <span class="number">5</span>) <span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>

<h2 id="实例方法-repeat"><a href="#实例方法-repeat" class="headerlink" title="实例方法: repeat()"></a>实例方法: repeat()</h2><p>repeat()方法返回一个新字符串,表示将原字符串重复n次.</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="string">'x'</span>.repeat(<span class="number">3</span>) <span class="comment">// "xxx"</span></span></code><br><code class="highlight javascript"><span class="line"><span class="string">'hello'</span>.repeat(<span class="number">2</span>) <span class="comment">// "hellohello"</span></span></code><br><code class="highlight javascript"><span class="line"><span class="string">'na'</span>.repeat(<span class="number">0</span>) <span class="comment">// ""</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意:</p>
<ul>
<li>参数如果是小数,会被取整(小数的整数部分)</li>
<li>参数如果是负数或infinity,会报错</li>
<li>参数是0到-1之间的小数,则等同于0(这是因为会先进行取整运算)</li>
<li>参数NaN等同于0</li>
<li>参数是字符串,则会先转换成数字</li>
</ul>
</blockquote>
<h2 id="实例方法-padStart-padEnd"><a href="#实例方法-padStart-padEnd" class="headerlink" title="实例方法: padStart() , padEnd()"></a>实例方法: padStart() , padEnd()</h2><p>用途:字符串长度补全, padStart()用于头部补全, padEnd()用于尾部补全</p>
<p>参数:</p>
<p>第一个参数:字符串补全生效的最大长度</p>
<p>第二个参数:用来补全的字符串</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="string">'x'</span>.padStart(<span class="number">5</span>, <span class="string">'ab'</span>) <span class="comment">// 'ababx'</span></span></code><br><code class="highlight javascript"><span class="line"><span class="string">'x'</span>.padStart(<span class="number">4</span>, <span class="string">'ab'</span>) <span class="comment">// 'abax'</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="string">'x'</span>.padEnd(<span class="number">5</span>, <span class="string">'ab'</span>) <span class="comment">// 'xabab'</span></span></code><br><code class="highlight javascript"><span class="line"><span class="string">'x'</span>.padEnd(<span class="number">4</span>, <span class="string">'ab'</span>) <span class="comment">// 'xaba'</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意:</p>
<ul>
<li>如果原字符串的长度等于或大于最大长度,则字符串补全不生效,返回原字符串</li>
</ul>
<ul>
<li><p>如果用来补全的字符串与原字符串两者的长度之和超过了最大长度,则会截去超出位数的补全字符串 </p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="string">'abc'</span>.padStart(<span class="number">10</span>, <span class="string">'0123456789'</span>)</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// '0123456abc'</span></span></code><br></pre></td></tr></table></figure>
</li>
</ul>
<ul>
<li>如果省略第二个参数,默认使用空格补全长度</li>
</ul>
</blockquote>
<h2 id="实例方法-trimStart-trimEnd"><a href="#实例方法-trimStart-trimEnd" class="headerlink" title="实例方法: trimStart() , trimEnd()"></a>实例方法: trimStart() , trimEnd()</h2><p>这两个方法的行为与trim一致,trimStart()消除字符串头部的空格,trimEnd()消除尾部的空格,并返回新字符串,不会修改原始字符串.</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> s = <span class="string">'  abc  '</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">s.trim() <span class="comment">// "abc"</span></span></code><br><code class="highlight javascript"><span class="line">s.trimStart() <span class="comment">// "abc  "</span></span></code><br><code class="highlight javascript"><span class="line">s.trimEnd() <span class="comment">// "  abc"</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意:除了空格键,这两个方法对字符串头部(或尾部)的tab键 , 换行等不可见的空白符号也有效</p>
</blockquote>
<h2 id="实例方法-matchAll"><a href="#实例方法-matchAll" class="headerlink" title="实例方法: matchAll()"></a>实例方法: matchAll()</h2><p>matchAll()方法返回一个正则表达式在当前字符串的所有匹配</p>
]]></content>
      <categories>
        <category>前端开发</category>
        <category>ES6学习</category>
      </categories>
      <tags>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript6--变量的解构和赋值</title>
    <url>/2019/10/14/ECMAScript6/2%E3%80%81%E5%8F%98%E9%87%8F%E7%9A%84%E8%A7%A3%E6%9E%84%E5%92%8C%E8%B5%8B%E5%80%BC/</url>
    <content><![CDATA[<p>ES6允许按照一定模式,从数组和对象中提取值,对变量进行赋值 ,着称为结构赋值</p>
<h1 id="数组的结构赋值"><a href="#数组的结构赋值" class="headerlink" title="数组的结构赋值"></a>数组的结构赋值</h1><h2 id="基本用法"><a href="#基本用法" class="headerlink" title="基本用法"></a>基本用法</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">//数组的结构赋值</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [a,b,c] = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>];</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//上面的写法等同于</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> a = <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> b = <span class="number">2</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> c = <span class="number">3</span>;</span></code><br></pre></td></tr></table></figure>

<p>本质上,这种写法属于”模式匹配”,只要等号两边的模式相同,左边的变量就会被赋予对应的值<br>如果结构不成功变量的值等于undefined.</p>
<a id="more"></a>

<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [foo, [[bar], baz]] = [<span class="number">1</span>, [[<span class="number">2</span>], <span class="number">3</span>]];</span></code><br><code class="highlight javascript"><span class="line">foo <span class="comment">// 1      </span></span></code><br><code class="highlight javascript"><span class="line">bar <span class="comment">// 2</span></span></code><br><code class="highlight javascript"><span class="line">baz <span class="comment">// 3</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [ , , third] = [<span class="string">"foo"</span>, <span class="string">"bar"</span>, <span class="string">"baz"</span>];</span></code><br><code class="highlight javascript"><span class="line">third <span class="comment">// "baz"</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [x, , y] = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span></code><br><code class="highlight javascript"><span class="line">x <span class="comment">// 1      </span></span></code><br><code class="highlight javascript"><span class="line">y <span class="comment">// 3</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [head, ...tail] = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>];</span></code><br><code class="highlight javascript"><span class="line">head <span class="comment">// 1     </span></span></code><br><code class="highlight javascript"><span class="line">tail <span class="comment">// [2, 3, 4]</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [x, y, ...z] = [<span class="string">'a'</span>];</span></code><br><code class="highlight javascript"><span class="line">x <span class="comment">// "a"      </span></span></code><br><code class="highlight javascript"><span class="line">y <span class="comment">// undefined     </span></span></code><br><code class="highlight javascript"><span class="line">z <span class="comment">// []</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>…运算符为Iterator 提供的</p>
</blockquote>
<p>结构不完全,即等号左边的模式,只匹配一部分的等号右边的数组,这种情况下,结构依然可以成功</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [x, y] = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span></code><br><code class="highlight javascript"><span class="line">x <span class="comment">// 1</span></span></code><br><code class="highlight javascript"><span class="line">y <span class="comment">// 2</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [a, [b], d] = [<span class="number">1</span>, [<span class="number">2</span>, <span class="number">3</span>], <span class="number">4</span>];</span></code><br><code class="highlight javascript"><span class="line">a <span class="comment">// 1</span></span></code><br><code class="highlight javascript"><span class="line">b <span class="comment">// 2</span></span></code><br><code class="highlight javascript"><span class="line">d <span class="comment">// 4</span></span></code><br></pre></td></tr></table></figure>

<p>如果等号的右边不是数组(严格的来说,不是可遍历的结构),将会报错</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 报错</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [foo] = <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [foo] = <span class="literal">false</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [foo] = <span class="literal">NaN</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [foo] = <span class="literal">undefined</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [foo] = <span class="literal">null</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [foo] = &#123;&#125;;</span></code><br></pre></td></tr></table></figure>

<p>上面前五个表达式的值转为对象以后不具备Iterator接口,最后一个本身就不具备Iterator接口<br>对于set结构,也可以使用数组的结构赋值</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [x, y, z] = <span class="keyword">new</span> <span class="built_in">Set</span>([<span class="string">'a'</span>, <span class="string">'b'</span>, <span class="string">'c'</span>]);</span></code><br><code class="highlight javascript"><span class="line">x <span class="comment">// "a"</span></span></code><br></pre></td></tr></table></figure>

<p>事实上,只要某种数据结构具有 Iterator 接口，都可以采用数组形式的解构赋值。</p>
<h2 id="默认值"><a href="#默认值" class="headerlink" title="默认值"></a>默认值</h2><p>结构赋值允许指定默认值</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [foo = <span class="literal">true</span>] = [];</span></code><br><code class="highlight javascript"><span class="line">foo <span class="comment">// true</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [x, y = <span class="string">'b'</span>] = [<span class="string">'a'</span>]; <span class="comment">// x='a', y='b'</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [x, y = <span class="string">'b'</span>] = [<span class="string">'a'</span>, <span class="literal">undefined</span>]; <span class="comment">// x='a', y='b'</span></span></code><br></pre></td></tr></table></figure>

<p>ES6内部使用严格相等运算符(===)判断一个位置是否有值,所以,只有当一个数组成员严格等于undefined,默认值才会生效如果默认值是一个表达式,那么这个表达式是惰性求值的,即只有在用到的时候,才会求值(例如函数表达式)</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>)</span>&#123;</span></code><br><code class="highlight javascript"><span class="line">	<span class="built_in">console</span>.log(<span class="string">"aaa"</span>);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [x=f()] = [<span class="number">1</span>];</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//由于数组成员不为undefined,所以函数f不会执行</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>注意:默认值可以引用结构赋值的其他变量,但该变量必须已经声明</p>
</blockquote>
<h1 id="对象的解构赋值"><a href="#对象的解构赋值" class="headerlink" title="对象的解构赋值"></a>对象的解构赋值</h1><h2 id="基本用法-1"><a href="#基本用法-1" class="headerlink" title="基本用法"></a>基本用法</h2><p>结构不仅可以用于数组,还可以用于对象</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> &#123;<span class="attr">foo</span>:foo,<span class="attr">bar</span>:bar&#125; = &#123;<span class="attr">foo</span>:<span class="string">'aaa'</span>,<span class="attr">bar</span>:<span class="string">'bbb'</span>&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//简写:let &#123;foo,bar&#125; = &#123;foo:'aaa',bar:'bbb'&#125;;</span></span></code><br><code class="highlight javascript"><span class="line">foo     <span class="comment">//'aaa'</span></span></code><br><code class="highlight javascript"><span class="line">bar     <span class="comment">//'bbb'</span></span></code><br></pre></td></tr></table></figure>

<p>上面代码中,键值对的意思是    <strong>属性名: 变量名</strong><br>对象的解构赋值的内部机制,是先找到同名属性,然后再赋值给对应的变量.真正的被赋值的是后者,而不是前者</p>
<p><strong>与数组解构的不同之处:</strong></p>
<ol>
<li>数组解构中数组的元素必须按次序排列,变量的取值由它的位置决定;</li>
<li>但是对象解构赋值没有次序要求,只要变量与属性同名,才能取到正确的值</li>
<li>同数组解构一样,如果解构失败,变量的值等于undefined</li>
</ol>
<blockquote>
<p>注意:  对象的解构赋值可以取到继承的属性。</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> obj1 = &#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> obj2 = &#123; <span class="attr">foo</span>: <span class="string">'bar'</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">Object</span>.setPrototypeOf(obj1, obj2);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">const</span> &#123; foo &#125; = obj1;</span></code><br><code class="highlight javascript"><span class="line">foo <span class="comment">// "bar"</span></span></code><br></pre></td></tr></table></figure>



<h2 id="默认值-1"><a href="#默认值-1" class="headerlink" title="默认值"></a><strong>默认值</strong></h2><p>对象的解构也可以指定默认值</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> &#123;x=<span class="number">3</span>&#125; = &#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line">x   <span class="comment">//3</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">var</span> &#123;<span class="attr">x</span>:y=<span class="number">3</span>&#125; = &#123;<span class="attr">x</span>:<span class="number">5</span>&#125;;</span></code><br><code class="highlight javascript"><span class="line">y  <span class="comment">//5</span></span></code><br></pre></td></tr></table></figure>

<p>默认值生效的条件是,对象的属性值严格等于undefined</p>
<h2 id="注意点"><a href="#注意点" class="headerlink" title="注意点"></a><strong>注意点</strong></h2><p>(1)如果要讲一个已经声明的变量用于解构赋值,必须非常小心</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">//错误的写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> x;</span></code><br><code class="highlight javascript"><span class="line">&#123;x&#125; = &#123;<span class="attr">x</span>:<span class="number">1</span>&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//上面代码的写法会报错，因为 JavaScript 引擎会将&#123;x&#125;理解成一个代码块，</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//从而发生语法错误。只有不将大括号写在行首，</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//避免 JavaScript 将其解释为代码块，才能解决这个问题。</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//正确的写法</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> x;</span></code><br><code class="highlight javascript"><span class="line">(&#123;x&#125; = &#123;<span class="attr">x</span>:<span class="number">1</span>&#125;);</span></code><br></pre></td></tr></table></figure>

<p>(2)由于数组本质上是特殊的对象,因此可以对数组进行对象属性的解构</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> arr = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>];</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> &#123;<span class="number">0</span>:first,[arr.length <span class="number">-1</span>]:last&#125; = arr;</span></code><br><code class="highlight javascript"><span class="line">first    <span class="comment">//1</span></span></code><br><code class="highlight javascript"><span class="line">last     <span class="comment">//3</span></span></code><br></pre></td></tr></table></figure>

<h1 id="字符串的解构赋值"><a href="#字符串的解构赋值" class="headerlink" title="字符串的解构赋值"></a>字符串的解构赋值</h1><p>字符串在解构赋值时会被转成一个类似数组的对象</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> [a,b,c,d,e] = <span class="string">'hello'</span>;</span></code><br><code class="highlight javascript"><span class="line">a   <span class="comment">//"h"</span></span></code><br><code class="highlight javascript"><span class="line">b   <span class="comment">//"e"</span></span></code><br><code class="highlight javascript"><span class="line">c   <span class="comment">//"l"</span></span></code><br><code class="highlight javascript"><span class="line">d   <span class="comment">//"l"</span></span></code><br><code class="highlight javascript"><span class="line">e   <span class="comment">//"o"</span></span></code><br></pre></td></tr></table></figure>

<h1 id="数值和布尔值的解构赋值"><a href="#数值和布尔值的解构赋值" class="headerlink" title="数值和布尔值的解构赋值"></a>数值和布尔值的解构赋值</h1><p>解构赋值时，如果等号右边是数值和布尔值，则会先转为对象。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> &#123;<span class="attr">toString</span>:s&#125; = <span class="number">123</span>;</span></code><br><code class="highlight javascript"><span class="line">s === <span class="built_in">Number</span>.prototype.toString <span class="comment">// true</span></span></code><br></pre></td></tr></table></figure>

<p>解构赋值的规则: 只要等号右边的值不是对象或数组，就先将其转为对象。由于undefined和null无法转为对象，所以对它们进行解构赋值，都会报错。</p>
<h1 id="函数参数的解构赋值"><a href="#函数参数的解构赋值" class="headerlink" title="函数参数的解构赋值"></a>函数参数的解构赋值</h1><p>函数的参数也可以使用解构赋值</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">add</span>(<span class="params">[x, y]</span>)</span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> x + y;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">add([<span class="number">1</span>, <span class="number">2</span>]); <span class="comment">// 3</span></span></code><br></pre></td></tr></table></figure>

<p>函数参数的解构使用默认值</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">move</span>(<span class="params">&#123;x = <span class="number">0</span>, y = <span class="number">0</span>&#125; = &#123;&#125;</span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> [x, y];</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">move(&#123;<span class="attr">x</span>: <span class="number">3</span>, <span class="attr">y</span>: <span class="number">8</span>&#125;); <span class="comment">// [3, 8]</span></span></code><br><code class="highlight javascript"><span class="line">move(&#123;<span class="attr">x</span>: <span class="number">3</span>&#125;); <span class="comment">// [3, 0]</span></span></code><br><code class="highlight javascript"><span class="line">move(&#123;&#125;); <span class="comment">// [0, 0]</span></span></code><br><code class="highlight javascript"><span class="line">move(); <span class="comment">// [0, 0]</span></span></code><br></pre></td></tr></table></figure>

<h1 id="圆括号的问题"><a href="#圆括号的问题" class="headerlink" title="圆括号的问题"></a>圆括号的问题</h1><p>建议只要有可能，就不要在模式中放置圆括号。</p>
<h2 id="不能使用圆括号的情况"><a href="#不能使用圆括号的情况" class="headerlink" title="不能使用圆括号的情况"></a>不能使用圆括号的情况</h2><ol>
<li>变量声明语句</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">//全部报错</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [(a)] = [<span class="number">1</span>];</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> &#123;<span class="attr">x</span>: (c)&#125; = &#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> (&#123;<span class="attr">x</span>: c&#125;) = &#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> &#123;(x: c)&#125; = &#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> &#123;(x): c&#125; = &#123;&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> &#123; <span class="attr">o</span>: (&#123; <span class="attr">p</span>: p &#125;) &#125; = &#123; <span class="attr">o</span>: &#123; <span class="attr">p</span>: <span class="number">2</span> &#125; &#125;;</span></code><br></pre></td></tr></table></figure>

<p>2.函数参数</p>
<p>函数参数也属于变量声明,因此不能带有圆括号</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 报错</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">[(z</span>)]) </span>&#123; <span class="keyword">return</span> z; &#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 报错</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">[z,(x</span>)]) </span>&#123; <span class="keyword">return</span> x; &#125;</span></code><br></pre></td></tr></table></figure>

<p>3.赋值语句的模式</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 全部报错</span></span></code><br><code class="highlight javascript"><span class="line">(&#123; <span class="attr">p</span>: a &#125;) = &#123; <span class="attr">p</span>: <span class="number">42</span> &#125;;</span></code><br><code class="highlight javascript"><span class="line">([a]) = [<span class="number">5</span>];</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//将整个模式放在圆括号之中，导致报错。</span></span></code><br></pre></td></tr></table></figure>

<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 报错</span></span></code><br><code class="highlight javascript"><span class="line">[(&#123; <span class="attr">p</span>: a &#125;), &#123; <span class="attr">x</span>: c &#125;] = [&#123;&#125;, &#123;&#125;];</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//将一部分模式放在圆括号之中，导致报错。</span></span></code><br></pre></td></tr></table></figure>

<h2 id="可以使用圆括号的情况"><a href="#可以使用圆括号的情况" class="headerlink" title="可以使用圆括号的情况"></a>可以使用圆括号的情况</h2><p>可以使用圆括号的情况只有一种：赋值语句的非模式部分，可以使用圆括号。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">[(b)] = [<span class="number">3</span>]; <span class="comment">// 正确</span></span></code><br><code class="highlight javascript"><span class="line">(&#123; <span class="attr">p</span>: (d) &#125; = &#123;&#125;); <span class="comment">// 正确</span></span></code><br><code class="highlight javascript"><span class="line">[(<span class="built_in">parseInt</span>.prop)] = [<span class="number">3</span>]; <span class="comment">// 正确</span></span></code><br></pre></td></tr></table></figure>

<h1 id="用途"><a href="#用途" class="headerlink" title="用途"></a>用途</h1><p>1.交换变量的值</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> x = <span class="number">1</span>;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> y = <span class="number">2</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">[x, y] = [y, x];</span></code><br></pre></td></tr></table></figure>

<p>2.从函数返回多个值<br>将函数返回值放在一个数组或对象里返回,用解构赋值进行取值</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 返回一个数组</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">example</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> [a, b, c] = example();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 返回一个对象</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">example</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">return</span> &#123;</span></code><br><code class="highlight javascript"><span class="line">    foo: <span class="number">1</span>,</span></code><br><code class="highlight javascript"><span class="line">    bar: <span class="number">2</span></span></code><br><code class="highlight javascript"><span class="line">  &#125;;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> &#123; foo, bar &#125; = example();</span></code><br></pre></td></tr></table></figure>

<p>3.函数参数的定义<br>解构赋值可以方便地将一组参数与变量名对应起来。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 参数是一组有次序的值</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">[x, y, z]</span>) </span>&#123; ... &#125;</span></code><br><code class="highlight javascript"><span class="line">f([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 参数是一组无次序的值</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">&#123;x, y, z&#125;</span>) </span>&#123; ... &#125;</span></code><br><code class="highlight javascript"><span class="line">f(&#123;<span class="attr">z</span>: <span class="number">3</span>, <span class="attr">y</span>: <span class="number">2</span>, <span class="attr">x</span>: <span class="number">1</span>&#125;);</span></code><br></pre></td></tr></table></figure>

<p>4.提取JSON数据<br>解构赋值对提取 JSON 对象中的数据</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">let</span> jsonData = &#123;</span></code><br><code class="highlight javascript"><span class="line">  id: <span class="number">42</span>,</span></code><br><code class="highlight javascript"><span class="line">  status: <span class="string">"OK"</span>,</span></code><br><code class="highlight javascript"><span class="line">  data: [<span class="number">867</span>, <span class="number">5309</span>]</span></code><br><code class="highlight javascript"><span class="line">&#125;;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> &#123; id, status, <span class="attr">data</span>: number &#125; = jsonData;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">console</span>.log(id, status, number);</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 42, "OK", [867, 5309]</span></span></code><br></pre></td></tr></table></figure>

<p>5.函数参数的默认值</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 参数是一组无次序的值</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">&#123;x:x=<span class="number">1</span>, y:y=<span class="number">2</span>, z:z=<span class="number">3</span>&#125;</span>) </span>&#123; ... &#125;</span></code><br><code class="highlight javascript"><span class="line">f(&#123;<span class="attr">z</span>: <span class="number">3</span>, <span class="attr">y</span>: <span class="number">2</span>&#125;);    <span class="comment">//x=1 y=2 z=3</span></span></code><br></pre></td></tr></table></figure>

<p>6.遍历Map结构</p>
<p>任何部署了 Iterator 接口的对象，都可以用for…of循环遍历。Map 结构原生支持 Iterator 接口，配合变量的解构赋值，获取键名和键值就非常方便。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> map = <span class="keyword">new</span> <span class="built_in">Map</span>();</span></code><br><code class="highlight javascript"><span class="line">map.set(<span class="string">'first'</span>, <span class="string">'hello'</span>);</span></code><br><code class="highlight javascript"><span class="line">map.set(<span class="string">'second'</span>, <span class="string">'world'</span>);</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> [key, value] <span class="keyword">of</span> map) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(key + <span class="string">" is "</span> + value);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// first is hello</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// second is world</span></span></code><br></pre></td></tr></table></figure>

<p>只想获取键名,或者获取键值,可以写成下面的形式:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 获取键名</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> [key] <span class="keyword">of</span> map) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">// ...</span></span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// 获取键值</span></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> [,value] <span class="keyword">of</span> map) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="comment">// ...</span></span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br></pre></td></tr></table></figure>

<p>7.输入模块的指定方法</p>
<p>加载模块时，往往需要指定输入哪些方法。解构赋值使得输入语句非常清晰。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">const</span> &#123; SourceMapConsumer, SourceNode &#125; = <span class="built_in">require</span>(<span class="string">"source-map"</span>);</span></code><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>前端开发</category>
        <category>ES6学习</category>
      </categories>
      <tags>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript6--let和const命令</title>
    <url>/2019/10/12/ECMAScript6/1%E3%80%81let&amp;&amp;const/</url>
    <content><![CDATA[<h1 id="let命令"><a href="#let命令" class="headerlink" title="let命令"></a>let命令</h1><ul>
<li>let命令只在所在的代码块内有效</li>
<li>let命令很适合for循环的计数器</li>
<li>let命令不存在变量提升,在变量声明前使用会报ReferenceError错误</li>
<li>let不允许在相同的作用域内,重复声明同一个变量</li>
</ul>
<a id="more"></a>

<h2 id="块级作用域"><a href="#块级作用域" class="headerlink" title="块级作用域"></a>块级作用域</h2><p>ES5只有全局作用域和函数作用域,没有块级作用域,这带来很多不合理场景。<br>第一种场景:内层变量可能会覆盖外层变量。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> tmp = <span class="keyword">new</span> <span class="built_in">Date</span>();</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(tmp);</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">if</span> (<span class="literal">false</span>) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="keyword">var</span> tmp = <span class="string">'hello world'</span>;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">f(); <span class="comment">// undefined</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">//解释</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">/*函数内部if代码块外部使用外层的tmp变量,内部使用内层的tmp变量,但是由于存在</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">*变量提升,内层的tmp覆盖了外层的tmp变量</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">*/</span></span></code><br></pre></td></tr></table></figure>



<p>第二种场景:用来计数的循环变量泄露为全局变量</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">var</span> s = <span class="string">'hello'</span>;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; s.length; i++) &#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="built_in">console</span>.log(s[i]);</span></code><br><code class="highlight javascript"><span class="line">&#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="built_in">console</span>.log(i); <span class="comment">// 5</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">/*</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">*循环变量i只用来控制循环,但是循环结束后,他并没有小时,泄露成了全局变量</span></span></code><br><code class="highlight javascript"><span class="line"><span class="comment">*/</span></span></code><br></pre></td></tr></table></figure>



<h2 id="ES6的块级作用域"><a href="#ES6的块级作用域" class="headerlink" title="ES6的块级作用域"></a>ES6的块级作用域</h2><p>let和const为JavaScript提供了块级作用域<br>ES6允许块级作用域的多级嵌套</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line">&#123;&#123;&#123;</span></code><br><code class="highlight javascript"><span class="line">	<span class="keyword">let</span> test = <span class="number">12</span>;</span></code><br><code class="highlight javascript"><span class="line">    &#123;<span class="keyword">let</span> text = <span class="number">12</span>;&#125;</span></code><br><code class="highlight javascript"><span class="line">&#125;&#125;&#125;</span></code><br></pre></td></tr></table></figure>

<p>块级作用域的出现,使用立即执行函数来防止污染全局变量已经不再必要了</p>
<h2 id="块级作用域域函数声明"><a href="#块级作用域域函数声明" class="headerlink" title="块级作用域域函数声明"></a>块级作用域域函数声明</h2><p>ES5规定,函数只能在顶层作用域和函数作用域之中声明,不能在块级作用中声明函数,但是浏览器没有遵守这个规定,为了兼容以前的旧代码,还是支持在块级作用域之中声明函数</p>
<p>例子:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>) </span>&#123; <span class="built_in">console</span>.log(<span class="string">'I am outside!'</span>); &#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">if</span> (<span class="literal">false</span>) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="comment">// 重复声明一次函数f</span></span></code><br><code class="highlight javascript"><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>) </span>&#123; <span class="built_in">console</span>.log(<span class="string">'I am inside!'</span>); &#125;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">  f();</span></code><br><code class="highlight javascript"><span class="line">&#125;());</span></code><br></pre></td></tr></table></figure>

<p>在ES5环境中运行,if内声明的函数会被提升到函数头部:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// ES5 环境</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>) </span>&#123; <span class="built_in">console</span>.log(<span class="string">'I am outside!'</span>); &#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>) </span>&#123; <span class="built_in">console</span>.log(<span class="string">'I am inside!'</span>); &#125;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">if</span> (<span class="literal">false</span>) &#123;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line">  f();</span></code><br><code class="highlight javascript"><span class="line">&#125;());</span></code><br></pre></td></tr></table></figure>

<p>ES6引入块级作用域后,明确允许在块级作用域之中声明函数.函树声明语句的行为类似于let,在块级作用域之外不可引用.<br>但是ES6规定,浏览器的实现可以不遵守上面的规定,有自己的行为方式(为了兼容以前的老代码).</p>
<ul>
<li>允许在块级作用域内声明函数</li>
<li>函数声明类似于var,即会提升到全局作用域或函数作用域的头部。</li>
<li>同时，函数声明还会提升到所在的块级作用域的头部。</li>
</ul>
<blockquote>
<p>注意:上面的三条规则只对ES6的浏览器实现有效,其他浏览器环境的实现不用遵守</p>
</blockquote>
<p>上面的例子在ES6环境中运行如下:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="comment">// 浏览器的 ES6 环境</span></span></code><br><code class="highlight javascript"><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>) </span>&#123; <span class="built_in">console</span>.log(<span class="string">'I am outside!'</span>); &#125;</span></code><br><code class="highlight javascript"><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">var</span> f = <span class="literal">undefined</span>;</span></code><br><code class="highlight javascript"><span class="line">  <span class="keyword">if</span> (<span class="literal">false</span>) &#123;</span></code><br><code class="highlight javascript"><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>) </span>&#123; <span class="built_in">console</span>.log(<span class="string">'I am inside!'</span>); &#125;</span></code><br><code class="highlight javascript"><span class="line">  &#125;</span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line">  f();</span></code><br><code class="highlight javascript"><span class="line">&#125;());</span></code><br><code class="highlight javascript"><span class="line"><span class="comment">// Uncaught TypeError: f is not a function</span></span></code><br></pre></td></tr></table></figure>

<blockquote>
<p>综上所述,应该避免在块级作用域内声明函数,如果确实需要,也应该写成函数表达式,而不是函数声明语句。<br>注意:ES6块级作用域必须有大括号,如果没有大括号,JavaScript引擎就认为不存在块级作用域</p>
</blockquote>
<h1 id="const命令"><a href="#const命令" class="headerlink" title="const命令"></a>const命令</h1><p>const声明一个只读的常量,一旦声明,常量的值就不能改变。<br>const一旦声明了变量,就必须立即初始化,不能留到以后赋值,不然会报错。<br>const声明的变量也是不存在变量提升的,不能重复声明。</p>
<h2 id="本质"><a href="#本质" class="headerlink" title="本质"></a>本质</h2><p>const定义的常量不是变量的值不得改动,而是变量指向的的那个内存地址保存的数据不得改动</p>
<ul>
<li>对于简单类型的数据(数值,字符串,布尔值),值就保存在变量指向的那个内存地址,因此等同于常量</li>
<li>对于复合类型的数据(主要是对象和数组),变量指向的内存地址,保存的只是一个指向实际数据的指针,const只能保证这个指针是固定的,但是它指向的数据结构是不是可变的就不能控制了</li>
</ul>
<h1 id="顶层对象的属性"><a href="#顶层对象的属性" class="headerlink" title="顶层对象的属性"></a>顶层对象的属性</h1><p>在ES5中,全局变量就等同于顶层对象(window)的属性,这非常不利于模块化编程,因为顶层对象的属性到处都可以修改。<br>ES6中的let命令、const命令、class命令声明的全局变量，不属于顶层对象的属性。</p>
<h1 id="this指向"><a href="#this指向" class="headerlink" title="this指向"></a>this指向</h1><ol>
<li>全局环境中，this会返回顶层对象。但是，Node模块和ES6模块中，this返回的是当前模块</li>
<li>函数里面的this，如果函数不是作为对象的方法运行，而是单纯作为函数运行，this会指向顶层对象，否则会指向当前对象（运行环境）</li>
</ol>
<h1 id="暂时性死区"><a href="#暂时性死区" class="headerlink" title="暂时性死区"></a>暂时性死区</h1><p>概念:使用let或const命令声明变量之前,该变量都是不可用的,这在语法上,称为”暂时性死区”.<br>暂时性死区意味着typeof不再是一个百分之百安全的操作.</p>
<p>例子:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="highlight javascript"><span class="line"><span class="keyword">typeof</span> x;       <span class="comment">//ReferenceError</span></span></code><br><code class="highlight javascript"><span class="line"></span></code><br><code class="highlight javascript"><span class="line"><span class="keyword">let</span> x;</span></code><br></pre></td></tr></table></figure>



]]></content>
      <categories>
        <category>前端开发</category>
        <category>ES6学习</category>
      </categories>
      <tags>
        <tag>ES6</tag>
      </tags>
  </entry>
</search>
